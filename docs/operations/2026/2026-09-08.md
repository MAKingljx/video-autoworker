# 2026-09-08 运维记录

## 已确认恢复部署的首次执行

用户提供新的 SSH 入口后，通过原已登记主机密钥验证连接到同一第二台设备。沿用同项目、同环境和已明确确认的恢复范围。执行前核对历史 3db、控制 CBC、独立 QA f6 源码均 clean；两库备份摘要及完整性通过，当前 Mission Control 原 53 张业务表及 schema_migrations 的逐表内容与原一致性备份相同，519 条视频任务保持不变，普通任务为空。应用端口尚未监听，n8n 与三套 Gateway 身份保持不变。

CBC execve adapter 已按四项当前摘要 CAS 安装到 router、blue、green，安装器保存事务备份，安装后正式校验通过。官方当前 9.2 收敛 apply 成功，配置摘要前后相同，生成的新运行证明通过官方校验。没有升级或降级 OpenClaw，没有启用视频 lane。

首次 successor 执行在源码权限校验处拒绝：兼容性 CLI 在 Git 中为 100644，运行器却要求 0755。失败发生在 attempt 写入、授权、能力消费和数据库迁移之前；attempt 保持为空。不能把此时的 adapter 安装成功视为应用恢复完成。

## 权限合同修复与继续方案

兼容性 CLI 使用固定 Node 显式执行，因此 runner、controller 首次授权及后续 receipt 复验的三处权限要求统一采用真实 Git 0644。其余属主、单链接、物理路径、内容摘要和源码提交绑定保持不变。增加真实临时 Git 仓库回归：确认 index mode 为 100644，0644 可绑定，错误的 0755 仍被拒绝；controller fixture 同步采用实际权限。Node 22 定向两文件 12 项测试、ESLint 和 diff 检查通过。

仅控制源码需要前进至本修复提交，使用独立 recovery-20260908 来源目录并保留旧 CBC 源码及安装备份。应用仍为已验证的 d96 制品，独立 QA 仍为 f6，历史 3db 不移动。新控制来源完成 Quality Gate、重新绑定 adapter 和 fresh proof 后继续同一批准计划。控制程序的同根因修复不扩大生产数据范围。

后续仍需完成 successor、迁移、入口恢复和实际验收；本记录不表示生产恢复成功。失败时保留保护状态、收据及数据库，不重放历史 resume 或 n8n，不自动重试历史业务任务。

## 完整检查与真实恢复入口差异

控制 d3f8d3d 的完整 Quality Gate 34176021075 已全部通过。正式执行前重新核对数据库、任务、原进程和恢复凭据均通过。首次 readiness 暴露独立控制 Git 交付缺少源码解析依赖；补齐锁文件一致的 TypeScript 5.9.3、better-sqlite3 12.6.2 及其两个运行依赖，逐文件比对原已验目录并保留无共享 inode 的独立副本。真实源码闭包与 Node 22 原生 SQLite 查询通过，控制 Git 保持 clean，应用制品未变。

随后授权校验暴露历史 bootstrap CLI 的反向模式错误：3db 的 Git 100755 与现场 0755 一致，授权和 receipt 校验却要求 0644。修复须以历史提交的实际 Git 模式为准，不能靠修改现役源权限规避。此时只有四份预检报告，无 successor receipt、token 或 consumed，未做数据库迁移。旧预检 attempt 保留，修复后的控制源使用新的空 attempt，继续引用原不可变历史恢复链。

验证依据为刚完成的 d3 全量 Quality Gate，加上这次权限合同的定向回归和现场验证。按 SAFE-EFFICIENT-EXECUTION-001 复用不受模式修复影响的应用、依赖、全量业务测试、构建与 E2E 证据；不因控制提交号变化机械重跑所有检查，也不将此复用表述成新提交已经完成全量 CI。

模式核对覆盖当前 runner/deploy/successor-controller/execve-adapter（100755）、compatibility/readiness/runtime-contract（100644），以及历史 3db bootstrap-controller（100755）和 freeze-guard（100644）。本次仅修正历史 controller 的授权及 receipt 复验两处，fixture 使用真实临时 Git tree 验证对应模式。源码敏感内容扫描通过。

最终 Node 22 定向 entry 6 项与 controller 8 项共14/14通过，包含错误100644历史源码在clean Git下仍被拒绝且不生成receipt/token的负例。ESLint、diff检查通过；控制修复只有两处权限常量，runner与应用实现不变。

## 历史恢复工具的实际权限

历史保护脚本的 Git tree 为 100644，现场为当前用户私有的 0600；内容摘要、物理路径、属主、单链接及 clean Git 均一致。用户明确允许按实际使用需要扩大权限，因此保留已验证的控制 eac408a，只将仍在恢复链中使用的这一个保护脚本恢复为 Git 标准 0644。调整前保存源码和原权限记录，调整后确认内容摘要、inode、Git提交及clean状态不变，保护进程未重启。数据库、Token、备份和其他私有状态的权限不在此次调整范围内。

交付过程另发现私有 umask 会使全新 Git 源副本采用 0600/0700；仅新副本按已核对 Git 成员恢复声明模式，内容与提交不变。后续源码 checkout 显式使用 022，私有计划、证据和 Token 则按各自受控模式创建。新控制的真实来源依赖、权限、adapter安装与fresh proof分别核验后，继续同一批准恢复流程。

## 授权后的启动环境与历史证据引用修复

权限恢复后 successor 授权已生成，但尚未消费。runner 清理 PATH 时漏掉固定 Node 目录，导致历史安装器的 node 命令预检失败；使用固定 Node 22 PATH 的公开 bootstrap-successor 命令继续后，安装器与 manager 通过，随后发现 inline 冻结证据校验仍传入新应用目标，而前面的官方生成器验证已正确使用历史目标。修复应统一使用历史授权的 release ID、root 和 manifest 校验历史证据，同时单独保留新应用来源校验。

由于控制源码需要再次前进，先停用相关已退出的一次性 job，确认本次能力未消费并由旧控制器成功验证，再移除其唯一 token，保留原不可变 receipt、作废意图和完成记录；旧控制器再次验证确认已拒绝该 token。未重新消费、重签或删除历史 f78 恢复收据，未修改业务数据库。新的控制版本以新的空 attempt 继续恢复。

同类核查还修正工作流兼容性结果的来源比较：successor 模式下 n8n 工作流仍由历史3db运行，验证应使用 n8n_source_commit，而非新应用source_commit。新应用d96、历史工作流3db与新控制提交继续分别绑定；正常bootstrap模式的别名取值不变。

最终新增回归真实执行冻结证据与工作流的 inline 校验，证明历史绑定通过、误传新目标被拒绝；固定 Node PATH 回归使用恶意调用方 PATH 验证不可注入。Node 22 定向 entry 8 项、controller 8 项共16/16通过，ESLint、shell语法、敏感内容扫描及diff检查通过。本次继续复用 d3 的完整 Quality Gate 与未改动 d96 应用验收证据。

## 托管启动器与制品校验器的来源分离

控制8f2a5ae通过授权与一次性消费，完成新目标映射后，blue启动器在应用server执行前被历史制品校验器拒绝。历史3db的允许成员集合不认识新制品必要的两个共享OpenClaw模块。完整性检查正确阻止启动；manager移除marker并卸载精确blue服务，baseline与最终恢复结果均未生成。

失败后的只读核验确认数据库仍为050，普通任务为空，视频历史仍为519条（失败29、成功490），原有非sqlite_表逐表与一致性备份相同，原n8n、Gateway与recovery-hold进程保持。按实际表名枚举，原有53张业务表加schema_migrations共54张非sqlite_表；后置检查必须按包含迁移记录的54张完整集合核验，不能将业务表数量用于完整表集合断言。

此次修复应保留历史manager及其安装记录身份，将真正启动slot的脚本和制品校验器明确绑定到新控制源码，并在授权前用同一校验器检查目标制品。新旧slot脚本当前字节一致，但必须通过源码根明确选择正确的校验器与配置。应用制品内容、清单和来源不变，不删除必要模块或扩宽历史校验器的允许集合。

已消费的attempt3及其原receipt、consumed、target mapping完整保留；其退出job已卸载，旧控制器verify-consumed通过。新控制安装前保存退役意图与当前状态证据，重绑后明确验证旧链失效，再以新的空attempt恢复同一历史pending和同一应用目标。不得重新消费旧能力、改写原收据或直接绕过部署控制器启动服务。

修复采用adapter v2，现有v1安装可由官方安装器直接CAS升级并自动备份；不再人工恢复原始plist。明确绑定当前slot launcher、auditor及四个源码依赖，保留历史router及安装管理身份。Node22三文件22/22定向回归通过，覆盖真实v1升级与未修改历史manager preflight；MJS语法、ESLint、源码敏感扫描和diff检查通过。新control的私有配置仅保存原数据库三路径，部署前与历史配置加外部platform.env的有效结果逐项比较，不复制密钥。


## 应用已启动后的收尾时序

控制5c0c6a6的adapter v2与授权前同源制品验证通过，新的attempt4完成一次性消费、目标映射、蓝槽启动与路由建立。实际应用仍为d96不可变制品，当前controller保持5c0、历史manager和n8n来源保持3db。蓝槽及路由健康，baseline已写入，数据库完成051–059共9项迁移并新增12张表。

迁移后只读核对确认两库仍为原文件身份且完整性检查通过，519条视频任务状态仍为失败29、成功490，普通任务为空。原54张非sqlite_表中，51张内容与原一致性备份相同；agents的现有2行发生启动同步更新，audit_log因入口暂停新增1行，schema_migrations从48增至57。不能把正常启动变更表述为所有历史业务表字节未变。

现有部署脚本在收到历史guard的revoke确认后立即要求socket和token消失，而guard需要异步关闭后清理，存在退出时序竞争。本次保护进程已按官方命令退出、socket和token已消失，但pending尚在、completion尚未发布、入口仍暂停revision1。应用本身已健康，不应通过重新授权、重建attempt、重绑runtime或重跑迁移收尾。

正在补充独立Git约束的收尾工具，仅接受“baseline已建立、同一已消费successor、同一运行实例、guard确已退出、入口仍暂停”的状态。校验成功后发布既有completion、CAS移除原pending，调用原5c0 attestation和runner的完成分支恢复入口。当前仍处于待收尾与真实验收状态。


## 收尾工具与检查补充

后续自动Quality Gate 34181091244在Typecheck拒绝新增恢复入口测试：Node的getuid类型在跨平台定义中可选，Next扩展的ProcessEnv要求NODE_ENV，而测试夹具未声明。失败仅涉及新增测试的类型，源码扫描与lint已通过，不影响当前5c0生产脚本字节。修复测试夹具，并将新收尾工具一并纳入全项目类型检查及定向真实Git入口回归；不把失败CI表述为通过。

现场浏览器确认页面可加载、SSE显示实时、深浅主题背景正确且无横向溢出，观察到的浏览器错误为零。另确认两项现存显示限制：server_managed连接模式不更新仅由前端WebSocket维护的isConnected，网关标记持续显示离线；会话列表仍读取旧sessions.json，不能作为OpenClaw9.2 SQLite当前会话的验收依据。服务端状态API报告网关运行且端口可达；真实会话验收由9.2官方接口及单独QA工具完成。这两项限制需按应用适配层迁移处理，不能用虚假在线状态或复制历史会话数据掩盖。

收尾工具定向3/3通过，包含真实Git入口的verify-only无写、消费后readiness篡改拒绝、apply及completion后崩溃重放；既有入口测试9/9通过。全项目TypeScript检查、三文件ESLint、MJS语法、源码敏感扫描和diff检查均通过。独立finisher不移动现役5c0来源，生产仍需先执行其只读预检再收尾。

独立finisher首次现场verify-only在写入前识别出历史版本语义差异：baseline.legacyReleaseId记录冻结前实际旧应用，必须匹配不可变pending.legacyReleaseId；receipt.historical.target记录历史待恢复目标，二者不是同一字段。修正该比较并补互不相同的夹具及误传目标负例。定向3/3、全类型检查、ESLint及语法检查再次通过。预检拒绝期间未写completion或移除pending。

现场只读预检进一步核实release-readiness中的intake是摘要DTO，globalScope位于readiness顶层及独立intake-control.control，摘要本身没有该字段。收尾校验与夹具按真实共享合同对齐，保留顶层授权、独立control权限、revision、paused与active计数校验。修正后定向3/3、全类型及ESLint通过；两次预检拒绝均发生在任何生产写入之前。


## 生产恢复完成，进入真实QA

独立finisher f26a128现场完整verify-only通过，确认原pending摘要、同一已消费attempt4、baseline、DB身份与打开句柄、应用cwd、监听PID、adapter及guard消失均匹配。官方5c0收敛apply生成新证明，配置摘要前后不变。随后finisher发布completion并CAS移除原pending，原5c0 deploy显式loopback身份完成attestation，原runner完成入口恢复并发布result。过程中未重绑或重启应用、router、n8n、Gateway，未重做迁移或消费旧恢复能力。

正式只读后验ok：应用d96，控制5c0 clean，历史3db clean；router3017 PID85973 generation1 active blue，蓝槽3317 PID84354，绿槽3417无监听。n8n30678及三套Gateway91397/2734/38814保持。入口由paused revision1恢复为active revision2，accepting=true、activeCount=0，恢复reason绑定completion摘要。历史pending和guard已退出，completion、attestation、intake-resumed及最终result完整。

当前进入单个owned合成会话五轮对话验收，随后做隔离压缩canary；尚不宣称两项QA完成。视频lane继续停用，不重试519条历史视频任务，不发送用户渠道消息。


## 部署后真实对话验收与9.2输出合同

原批准的单个合成会话已创建，首轮验收被旧QA解析器拒绝；官方sessions.delete完成精确清理，activeSessionAbsent=true、deleted=true，平台报告归档1份，无外送。原stdout在内存清零，不能推断当次回答或模型本身已经通过。

只读核对已安装OpenClaw2026.9.2官方CLI源码，确认无--deliver时返回顶层payloads与meta，且不会输出deliverySucceeded=false。旧wrapper漏读顶层回答并强制要求该字段，因此必然拒绝正常的不外送结果。新增纯QA解析helper，依据实际argv无--deliver和响应中无投递证据验证边界，仅读meta.agentMeta模型身份，按官方可见文本语义解析；拒绝--deliver=、任何deliveryStatus和非权威字段伪造。6项正负例、ESLint及Node语法检查通过。该修复不改变现役app、controller或正在运行的独立f6压缩canary。

新wrapper仅预备，增加失败时无正文诊断并保留原失败凭据。因原确认范围限定一个新合成会话且该会话已清理，另一次实际对话重测须取得再创建一个owned五轮会话的明确许可；不借解析器修复盲目重复创建。独立future-clean canary仅使用隔离fixture，不读取生产会话，继续完成。


## 隔离压缩验收结果与收尾复核

f6独立future-clean canary完成并生成报告，isolatedCleanupComplete=true、realSessionRead=false、externalDelivery=false，隔离Gateway已退出。19组真实工具调用与投影回写通过；活动上下文从155871字节降至65249字节，低于131072限制，仅发生1次压缩，下一轮无重复压缩，内建safeguard和checkpoint store/RPC一致性通过。累计存储增长包含非活动历史，不能据此否定活动上下文下降。

整体验收仍为false。其一，SQLite原地压缩的checkpoint文件引用可能按官方合同省略，旧QA却要求该字段等于session key或active-store marker；当前sessionId、活动leaf与store/RPC均一致，应修正这个字段必有的假设并保留完整身份校验。其二，压缩后第二轮没有再次调用导演脑工具，因此“重新读取权威来源”目标未满足。首两轮回答另使用全文严格相等，不能仅凭该断言判断事实是否遗漏；后续应建立结构化事实锚点，而非将有回答直接算通过。其三，本次启用了mid-turn precheck，未满足独立complete-turn策略门槛。修复QA不能代替运行策略修复和重新验收，亦不能将隔离fixture升级为生产权威源验证。

两个已结束的QA job确认无运行PID后分别禁用并卸载，临时测试网关无监听；预备的对话重测仍未armed。随后重新执行同一只读生产验收，应用d96、控制5c0及历史3db来源绑定通过，3017/3317保持原PID与generation1，intake active revision2、activeCount=0。未重启应用、router、n8n或Gateway，未改动模型配置、历史数据、视频lane或自动清理策略。

本轮生产恢复已完成；剩余验收与应用兼容缺口保持显式记录。后续依次完成获准的单会话对话重测、隔离压缩合同与策略验收，再按平台适配器边界迁移托管Gateway健康状态和9.2会话索引。数据库回退仍需先保护迁移后新增数据并按已审流程进行，不能直接覆盖原备份。


## 独立QA检查点合同修复

仅调整独立QA：SQLite snapshot的agent、session与store marker必须自洽；对于官方省略postCompaction.sessionFile的原地压缩，继续由exact sessionId、当前活动leaf和checkpoint store/RPC一致性完成绑定。显式引用仍需匹配当前session key或合法marker，错误agent/session/store、外部JSONL路径、null以及非原地压缩的缺失引用继续拒绝。generation-rotation仍要求有效显式引用。主脚本保留语义、权威工具重读、complete-turn策略及无泄漏等原验收门槛，没有把本次失败结果改写成成功。原fixture报告没有序列化postCompaction原始DTO，不能单凭报告证明该字段缺省；本修复依据已安装官方9.2的SQLite marker转存分支与正负例合同，尚未重跑现场canary验证。

新增两项正负例并同步既有matrix合同，9.2 agent JSON解析的6项测试纳入正式CI步骤。Node22完整四文件QA合同49/49通过，ESLint、源码敏感扫描及diff检查通过。该修复与脱敏记录推送canonical Git，未在生产重跑canary，现役app/control/finisher与待许可的8ffa5d8对话包保持各自原有绑定。本次复用此前d3完整Quality Gate及未改变的d96制品验收；自动CI的进行中或取消状态不记为通过。
