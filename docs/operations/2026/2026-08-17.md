# 2026-08-17 Qwen3.8 独立部署与 OpenClaw 模型切换

## 目标与范围

在默认生产节点 `heisenbergs-1` 增加 Qwen3.8-27B BF16，保留现有 Qwen3.6 视觉服务，接入 `qwen-current / second-original`，并提供可验证、可回滚的 `qwen36` / `qwen38` 切换。此次没有改动视频队列、SQLite、n8n execution、媒体目录或现有 Qwen3.6 服务。

## 生产部署事实

- 远端身份：`HEISENBERGS-1deMac-Studio.local / heisenbergs-1`；SSH 使用当前受信任别名 `heisenbergs-1`。
- 官方 BF16 权重：`/Users/heisenbergs-1/models/Qwen3.8-27B`，18/18 个 safetensors 分片齐全，索引声明总大小 `55,562,855,904` bytes，目录存在 `.download-complete`。
- Apple Silicon 运行目录：`/Users/heisenbergs-1/models/Qwen3.8-27B-MLX-BF16`，由远端 `mlx-lm 0.31.3` 转换并成功加载。
- 独立服务：`http://127.0.0.1:18092/v1`，LaunchAgent `ai.aiworker.qwen38-server`，前台命令为 `~/ai-worker/bin/aiworker-qwen38-server`。
- OpenClaw provider：`qwen38-local/default_model`；profile 配置和 agent 模型目录均已登记，模型声明为 `text`、`tools`、`structured-output`，上下文按现有稳定性策略限制为 `contextWindow=131072`、`contextTokens=98304`。
- OpenClaw 别名：`qwen36 -> qwen36-tools-local/default_model`、`qwen38 -> qwen38-local/default_model`。
- 当前切换目标：`qwen-current` 的 profile 默认和 `second-original` agent 使用同一模型；本轮切换后目标为
  `qwen38-local/default_model`，并将 `second-original.thinkingDefault` 固定为 `high`。回切 Qwen3.6 时脚本会
  同步恢复 `thinkingDefault=off`，因为 Qwen3.6 生产链只验证过关闭思考。

## 视觉运行时与兼容性边界

Qwen3.8 原始权重的视觉塔已用独立 Transformers + MPS 运行时完成部署：

- 原始目录：`/Users/heisenbergs-1/models/Qwen3.8-27B`；运行服务：`http://127.0.0.1:18094/v1`。
- LaunchAgent：`ai.aiworker.qwen38-vl-server`；模型 ID：`qwen38-27b-vl`。
- OpenClaw provider：`qwen38-vl-local/qwen38-27b-vl`，别名 `qwen38-vl`。
- 视频 binding `2` 的主路由为 `local-qwen38-vl-direct`，回退为 `local-qwen36-direct`；原有 Qwen3.6 `18091` 保留为可用回退。
- 原有 Stable Diffusion `18093` 保持独立，不由视觉服务占用。

`18092` 的 MLX 转换服务仍然只负责 Qwen3.8 文本/工具模型；它与 `18094` 视觉服务不共享转换目录，也不替代视觉路由。后续升级按仓库内的
[`qwen38-vision-runtime-runbook.md`](../../qwen38-vision-runtime-runbook.md) 固定顺序执行。

## 切换与回滚

远端已安装：

```text
~/ai-worker/bin/aiworker-openclaw-qwen-model-switch qwen38
~/ai-worker/bin/aiworker-openclaw-qwen-model-switch qwen36
```

脚本在切换前备份 `~/.openclaw-qwen-current/openclaw.json` 和
`~/.openclaw-qwen-current/agents/second-original/agent/models.json`，校验 provider 端点、配置、Gateway 健康和最小推理；失败时恢复同一备份。切换同时更新 profile 默认模型和 `second-original` 的显式 agent 模型，避免 agent 级覆盖导致“默认已切换但实际对话未切换”。切换备份家族当前保留两份已验证恢复点：

- `~/ai-worker/backups/qwen-model-switch/20260817-092506`
- `~/ai-worker/backups/qwen-model-switch/20260817-092658`

provider 首次安装备份为 `~/ai-worker/backups/qwen38-openclaw/20260817-091927`。模型权重、配置备份和运行日志均不进入 Git。

## 验证证据

- 直连 Qwen3.8 文本请求返回 `好`（HTTP 200）。
- 直连 Qwen3.8 工具请求返回结构化 `demo_tool` 调用，`finish_reason=tool_calls`。
- `openclaw --profile qwen-current infer model run --gateway --model qwen38` 返回 `provider=qwen38-local`、文本 `好`。
- 真实 OpenClaw `second-original` 无投递会话以 Qwen3.8 调用一次 `aiworker_analyze_video(status)`；轨迹记录 provider 为 `qwen38-local`，工具结果为只读状态摘要，没有调用 `exec`、文件或记忆工具，也没有创建、修改或重提任务。
- 双向切换测试均通过：切到 Qwen3.8 后 profile 与 agent 实际路由均为 `qwen38-local`，切回后均为 `qwen36-tools-local`。
- 收口时 `18091`、`18092`、`18889`、`18989`、`3017`、`5678`、`5679` 均保持 loopback 监听；OpenClaw 配置校验通过，现有 Qwen3.6 与 n8n/控制台监听未被替换。

## 已知状态

OpenClaw health 同时报告历史 delivery queue dead-letter 警告（outbound 99，最早约 79 天）；该状态在本次部署前已存在，本轮没有清理或改变它，需作为独立运维事项处理。

## canonical 与 standalone 收口（2026-08-17）

- 项目档案确认使用 `PROJ-VIDEO-AUTOWORKER`；canonical GitHub 与
  `heisenbergs-1:/Users/heisenbergs-1/Documents/Phoenix/video-autoworker` 均已对齐到
  `ea42b0cee711ba1e3814c4cf3b14cb3c0210ceac`，两边工作树干净。
- pnpm 配置收敛到仓库根 `pnpm-workspace.yaml`：pnpm 11 使用 `allowBuilds`，只允许
  `better-sqlite3` 构建；同时保留同一策略的 pnpm 10 兼容键，不强制开发机降级。旧的
  `package.json.pnpm.onlyBuiltDependencies` 和 `.npmrc.onlyBuiltDependenciesFile` 已移除。
- video-lane 校验器现在忽略 canonical task-flow 中仅用于测试的 `test/` 目录，安装载荷仍只接受
  `SKILL.md`、`lib/`、`scripts/`，避免测试文件进入生产 skill。
- Next/Turbopack 根固定为 canonical 仓库目录，消除了上层无关 `package-lock.json` 导致的 workspace
  root 误判。
- 本地 Node 22 + pnpm 11.19.0 验证：主测试 `1048/1048`、视频插件 `335/335`、task-flow `3/3`、
  typecheck 通过、lint 0 错误（12 条既有 warning）、生产 build 通过。
- 首次远端部署因非交互 pnpm 删除 `node_modules` 被 TTY 保护中止；部署脚本已改为
  `CI="${CI:-true}" pnpm install --frozen-lockfile`，随后重新部署成功。最终 standalone 进程
  PID `58725` 监听 `127.0.0.1:3017`，登录页 CSS 返回 `text/css`，应用 `/api/status`、n8n、任务代理、
  Qwen3.6、Qwen3.8 均返回 HTTP 200。
- 失败恢复期间曾临时启动旧 standalone 进程，随后已由正式部署进程替换；正式进程加载 `.env.local`
  指向外部受控数据目录，没有提交任务或调用视频队列。应用启动执行了既有数据库迁移检查；本轮不清理
  任何数据库、媒体、队列或 OpenClaw 运行资产。
- OpenClaw Gateway 与 qwen-current 在本记录生成时切换为 `qwen38-local/default_model`；Telegram
  probe 首次出现一次 `ECONNRESET`，3 秒后复验为 `works`，未修改通道配置或凭据。

## Qwen3.8 思考模式收口（2026-08-17）

- Qwen3.8 的 `mlx-lm` 服务默认启用 `enable_thinking=true`，同时保留
  `QWEN38_CHAT_TEMPLATE_ARGS` 环境变量作为明确的运维覆盖口；视觉能力仍不启用。
- OpenClaw provider 声明 `reasoning=true`、`compat.thinkingFormat=qwen-chat-template`，让 Gateway 的
  `thinkingDefault=high` 转换为 Qwen chat-template 的思考开关；该模型在当前 MLX 运行时可验证的思考能力是
  二值开关，`high` 是 OpenClaw 侧最高标准级别，不是额外的视觉或无限 token 预算。
- `aiworker-openclaw-qwen-model-switch` 同时更新 profile 默认、`second-original` agent、provider 元数据和
  `chat_template_kwargs`，失败自动恢复同一份备份，避免只改 profile 默认而 agent 仍指向旧模型。

## 任务链认证恢复修复（2026-08-17）

- 根因：3017 standalone 虽已带有 `MC_DESKTOP_MODE=1`，但默认绑定 `0.0.0.0` 使 Next 的回环请求内部 URL 被识别为非回环主机；task-flow 对 `/api/n8n/trigger` 和 `/api/n8n/runs` 因此返回 `401 Authentication required`，队列状态保留在原 task ID，没有重提。
- 修复：`scripts/start-standalone.sh` 现在自身加载仓库 `.env` / `.env.local` 并默认绑定 `127.0.0.1`；`scripts/deploy-standalone.sh` 将同一 loopback 默认值和 HTTP 状态码探测固化为部署门禁，发现认证失败立即失败，不再把“登录页可打开”误判为任务链可用。
- 受影响任务保留为原 task ID `a5ecd8c0-ba83-42ab-ad7c-383aa0b89e6f`，重启持久 video lane 后由同一任务恢复；不创建新任务、不删除素材、不扫描或修改数据库。
