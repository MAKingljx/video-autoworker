# 2026-08-19 OpenClaw Feishu 通道生产接入

## 需求

- 通过当前 cpolar SSH 入口连接默认生产节点，完成 `qwen-current / second-original` 的 Feishu 对话通道配置。
- 复用现有官方插件和本地 Qwen3.8 模型路由，保护应用凭据，并完成真实私聊、原有通道和回滚能力验收。
- 不改动视频任务、n8n、媒体、数据库、索引、模型权重或 canonical 产品源码。

## 生产变更

- 当前入口 `6.tcp.cpolar.cn:13528` 已使用固定 ED25519 信任记录严格校验，远端主机和登录用户与默认生产节点一致。
- 已发布的飞书自建应用启用了机器人能力、长连接、消息接收事件和机器人发消息权限。OpenClaw 官方 Feishu 插件通过长连接启用，并绑定到 `second-original`。
- 私聊策略为 `pairing`；群聊策略为 `allowlist` 且要求提及机器人。授权配对已完成，未放宽为任意群聊可用。
- App Secret 未写入配置、仓库或记录。`openclaw.json` 仅保存官方文件型 SecretRef，实际密钥文件权限为 `0600`。

## 验证证据

- OpenClaw `2026.7.1-2` 的 `qwen-current` Gateway 在 `127.0.0.1:18889` 正常监听；深度检查显示配置审计、RPC 和插件版本一致性均通过。
- Feishu 为 `configured/running/connected` 且实时 probe 通过；待处理配对为零。Safari 中完成纯 ASCII 固定结果私聊验收，实际路由为 `second-original -> qwen38-local/default_model`。
- Telegram 为 `configured/running/connected` 且实时 probe 通过；WhatsApp Official 保持 `configured/running` 且无状态错误。WhatsApp 本轮未主动发送测试消息。
- 本地 canonical、GitHub `main` 和生产仓库在记录前均为 `b99ef27`，分支为 `main`、工作树干净。本次没有产品代码或生产仓库运行文件变更。

## 凭据、备份与回滚

- 非交互 SSH 会话不能解锁远端登录钥匙串，因此使用 OpenClaw 官方支持的单值文件 SecretRef；没有修改 npm `dist/`，没有把凭据迁入仓库、日志或聊天记录。
- 变更前配置恢复点为 `openclaw-feishu/20260819-142449/openclaw.json`，SHA-256 为 `34575ebf53f88b2291261641b3c16b42d17528e290e7f371db1a3b65f2ddb2b1`，已完成 JSON 解析和可读性验证。
- 该备份家族只保留最近两个有效历史版本；最旧版本已移入远端废纸篓，仍可恢复。回滚时恢复配置恢复点、重启 `ai.openclaw.qwen-current`，再复核 Gateway 和原有通道。

## 边界与已知事项

- canonical 仓库仅保存本脱敏运维记录；应用标识、用户标识、配对码、会话正文、凭据、原始日志和备份内容均不进入 Git。
- cpolar 地址会轮换，后续使用前必须重新严格验证主机密钥、主机名和登录用户。
- 群聊当前采用最小开放策略；新增允许群范围属于后续独立权限变更，必须按实际对象另行审核。

## 视频结果候选元数据与思考强度调优

### 问题与决策

- Feishu 中文只读验收按名称查询一个历史视频时，`result` 命中两条同名完成任务，但候选回执只包含标题和状态。`second-original` 无法判断哪条是最新完成记录，因而连续尝试多个同义关键词，最终仍要求用户提供任务编号。
- 会话时间戳证明工具子进程每次约百毫秒内返回，主要等待发生在 `qwen38-local/default_model` 的高强度多轮推理。修复边界确定为：补全受控候选元数据，避免无效改写查询；同时把 `second-original` 的默认思考强度从 `high` 降为 `medium`。不增加自动思考路由，保留 OpenClaw 官方的单消息和会话级 `/think` 覆盖能力。

### 本地候选

- 插件候选版本为 `0.5.6`。`aiworker-task-flow` 的名称/关键词搜索继续只读受控任务登记，但多命中 `result` 候选现在保留 `taskId`、适用时的 `batchId` 与 `index`、`completedAt` 和 `updatedAt`。
- 插件运行边界对上述字段执行任务/批次 ID、索引、状态和时间戳校验；用户可见回执按匹配度与更新时间倒序列出候选，并明确让 Agent 使用返回的任务编号继续读取，不再要求用户重复提供接口已经返回的编号。
- 多命中仍不读取任意文件、SQLite、n8n execution、媒体目录、聊天历史或旧学习材料；完整报告仍只来自正式 `output.summary`，缺失时才回退 `combinedText`。

### 本地验证与待完成项

- 在 Node `22.23.2` 下，根级 Vitest `1054/1054`、插件全量测试 `338/338`、task-flow Node 测试 `4/4`、任务链脚本测试 `27/27` 全部通过；`pnpm build` 完成 Next.js 编译、TypeScript 检查和 127 个页面生成。lint 为 0 错误、12 条既有警告；变更文件另通过 Node 语法检查、Shell 语法检查和 `git diff --check`。
- 本机默认 Node `25.8.1` 与现有 `better-sqlite3` ABI 不匹配，曾造成 14 项环境性失败；切换到项目要求的 Node 22 后未再出现失败，未为此重建原生依赖。
- 本节记录的是本地候选，不代表 GitHub 或生产已生效。发布前还需完成 GitHub `main` 对齐、远端 dry-run/apply、配置备份与回滚验证，并在 Feishu 用中文按名称复验完整报告和耗时。

### 发布与生产部署

- 修复提交为 `a12d956`，已推送 `MAKingljx/video-autoworker` 的 `main`；生产仓库通过 `--ff-only` 从 `b54cc1d` 快进到同一提交，remote、分支和工作树均复核正确。
- task-flow 安装器先 dry-run，确认 skill、工作区规则、备份、队列、n8n 和 Gateway 均不变，再通过事务 apply 更新受管 Skill、`AGENTS.md` 和 `MEMORY.md` 片段并建立恢复点。
- 插件升级器先 dry-run，再将 `qwen-current` 从 `0.5.5` 升级到 `0.5.6`；安装后 `package.json`、manifest 和 runtime 版本一致，仅刷新该插件 payload 与 Gateway。
- `second-original.thinkingDefault` 使用 OpenClaw 官方 `config set` 由 `high` 调整为 `medium`，变更前建立权限受控的单一配置恢复点，配置 schema 校验和 Gateway 重启均通过。OpenClaw 的 `adaptive` 是受支持模型的 provider-managed 模式，当前本地 Qwen3.8 路由没有该能力证明，因此不启用；临时高强度任务仍可使用单消息或会话级 `/think` 覆盖。

### Feishu 中文真实验收

- Safari 飞书页面使用中文、仅提供名称 `S03E03` 发起只读完整结果查询，没有向模型提供任务 ID。安装后的 `result` 直接查询返回两条有序候选，每条均包含任务 ID、适用时的批次 ID/批次项、完成时间和更新时间。
- `second-original` 随后使用最新完成候选的任务 ID 再调用 `result`，最终返回正确标题、`已完成` 状态和一句中文摘要；同一会话轨迹只有 `aiworker_analyze_video.result`，没有 `exec`，也没有任何创建、重提、恢复、取消或修改动作。
- 该轮从用户消息入站到最终回复约 `263` 秒；五次工具子进程均约百毫秒完成，主要耗时仍是 Qwen3.8 在三个工具回合和最终整理中的推理。候选接口缺字段导致的阻塞已经解除，但 `medium` 不等同于按任务自动调节，模型在拿到候选前仍做了多次名称改写，这是后续性能优化点。
- 测试消息已由 Safari 页面发出并看到机器人进入输入状态；最终回复生成前本机自动锁屏，最终内容与调用链由同一 Feishu/OpenClaw 会话记录确认，未把会话正文或标识写入 Git。

### 状态保护与回滚证据

- 部署后生产仓库干净且为 `a12d956`；插件为 `0.5.6`，`second-original` 默认思考强度为 `medium`。`3017`、`5678`、`5679`、`18091`、`18092`、`18094` 和 `18889` 均正常监听。
- n8n execution 仍为 `32` 条、最大 ID `41`、活跃数 `0`；Mission Control `n8n_task_runs` 仍为 `159` 条、最大 ID `159`；视频状态文件仍为 `13`，媒体收件箱与任务工作区文件均为 `0`。两库 `quick_check=ok`，与部署前基线完全一致。
- task-flow 与插件均保留各自受控恢复点；思考强度配置备份家族当前只有一个恢复点，未触发历史清理。回滚时分别使用对应安装器恢复受管 Skill/插件，或恢复思考强度配置后重启 `qwen-current`，不得回滚或删除视频任务、数据库、媒体和 n8n 运行资产。

## 历史视频短句查询默认契约

### 需求与决策

- 用户不应每次复述“先按名称调用 `result`、多候选时选最新完成项、再按任务编号调用 `result`、只返回中文三字段”的长提示。日常入口统一收敛为“查 S03E03 分析”这类短句。
- 该行为同时写入受管 Skill、工作区规则、工作区记忆、工具描述和 Qwen 分类器 system prompt，并以自动测试锁定一致性。这是内部默认，不得再将运维提示返过来要求用户照抄。
- 首次名称查询只提取当前消息中最小且明确的原始标题、文件名或季集号，一次一个地等待结果；不追加旧上下文，不改写、翻译、并行搜索或用同义变体重试。
- 多候选时默认选择 `completedAt` 最新的已完成记录，下一次只用其精确 `taskId` 调用 `result`。除非用户明确要正文、全文、逐页内容或其他格式，默认只用中文回复视频标题、当前状态和一句分析摘要。

### 本地候选与验证

- 插件候选版本由 `0.5.6` 升为 `0.5.7`；运行边界仍只读受控任务登记和正式结果，不使用 `exec`、文件搜索、SQLite、n8n、媒体目录或聊天记录，也不触发创建、重提、恢复、取消或修改任务。
- Node `22.22.3` 下主项目 `1054/1054`、插件 `341/341`、task-flow `4/4`，合计 `1399/1399` 测试通过；TypeScript 检查通过，lint 为 0 错误和 12 条既有警告，Next.js 生产构建及 127 个页面生成通过。
- 本机默认 Node `25.8.1` 与现有 `better-sqlite3` ABI 不匹配，首次全量测试出现 14 项环境性失败；切换项目规定的 Node 22 后全部消失，未重建或改写任何生产依赖。

### 发布边界与回滚

- 本节当前记录本地候选；GitHub 与生产生效状态需在提交推送、task-flow 与插件 dry-run/apply、Gateway 检查和 Feishu 中文短句验收完成后另行回填。
- 部署仅允许替换受管 Skill/工作区契约和 `qwen-current` 的该插件 payload，保持队列、n8n、视频状态、媒体、数据库和思考强度配置不变。使用既有事务安装器和版本化插件恢复点回滚，每个备份家族最多保留两个已验证恢复点。

### 生产发布与状态保护

- 功能提交 `4ba45af` 已推送 GitHub `main`，生产仓库在复核 remote、`main` 分支和干净工作树后快进到同一完整 SHA `4ba45af88e2513329cb248b2c9c9b29820d0547c`。首次 GitHub fetch 遇到一次 HTTP/2 framing 错误，使用单次 `http.version=HTTP/1.1` 重试成功，未改全局 Git 配置。
- task-flow 安装器 dry-run 确认三份契约待更新且零外部改动，随后事务 apply 成功；恢复点为 `aiworker-task-flow-skill/20260819-164023.eYkGCx`。插件升级器 dry-run 通过后将 `qwen-current` 从 `0.5.6` 更新到 `0.5.7`，仅刷新该插件 payload 和 Gateway。
- 生产 runtime 为 `loaded|0.5.7|before_dispatch|aiworker_analyze_video`，Gateway RPC 为 `true`；`3017/5678/5679/18091/18092/18094/18889` 全部保持监听。`second-original.thinkingDefault` 仍为已调低的 `medium`，本次未再改配置，也未对当前无能力证明的本地 Qwen 开启 `adaptive`。
- 部署前后 n8n execution 均为 `32`、最大 ID `41`、运行数 `0`；Mission Control `n8n_task_runs` 均为 `159`、最大 ID `159`，状态分布仍为 `accepted=1/failed=6/succeeded=152`；视频状态文件仍为 `13`，媒体收件箱/任务工作区仍为 `0/0`，两库 `quick_check=ok`。未创建、重提、恢复、取消或修改视频任务。
- Safari 解锁后在 Feishu 真实发送了仅含“查 S03E03 分析”的中文短句。当前会话轨迹只有两次 `aiworker_analyze_video.result`：首次按 `S03E03`，第二次按最新完成候选的任务编号；没有 `exec`、提交、重提、恢复、取消或修改动作。标题、完成状态和中文摘要均读取正确。
- 首轮页面回复在三字段后多出一句“如需全文”类引导，因而格式验收未通过。后续 `0.5.8` 候选将“恰好三行、第三行后立即结束、禁止解释/问句/建议/后续引导”同步写入 Skill、工作区规则、工作区记忆、工具描述和只向 Agent 可见的成功报告回执；直接 `before_dispatch` 回执不暴露该内部约束。
- `0.5.8` 核心与契约定向测试 `28/28`、task-flow 安装器 `15/15`、task-flow 库 `4/4`通过；插件全量首轮 `340/341`，唯一失败是一个旧安装器用例在机器高负载下超过 5 秒，该文件独立复跑 `18/18` 通过；其余插件用例无失败。lint 为 0 错误和 12 条既有警告，Next.js 生产构建与 127 个页面生成通过。
- 首轮网页验收后的全局运行数据不能与 16 时部署基线直接作零增量比较：当日 18:59 另有一条早于本次 19:10 短句查询的独立目录学习请求，它的会话轨迹明确是 `submit_directory`，导致后续 n8n、任务和视频状态数量变化。本次只读查询的无写入结论以其独立会话工具轨迹为证据，不得把并发任务的增量归因给查询，也不得为验收擅自取消或修改该目录任务。

## `/tasks` 任务链列表与统一配色（已部署生产）

### 需求与决策

- `/tasks` 默认展示 Mission Control `n8n_task_runs` 中的顶层任务链运行记录，并保留原协作任务看板作为次级视图。任务链记录只读，不提供拖拽、编辑或删除操作。
- `/api/n8n/runs` 新增显式 `view=list`，保留既有默认列表与 `taskId` 详情契约，避免影响 OpenClaw 和现有调用方。列表只接受当前 workspace/tenant，且只包含 `openclaw`、`video-autoworker` 两类顶层来源；`n8n-node`、`n8n-media-node` 等内部节点不进入用户任务列表。
- 列表投影只返回标题、任务/批次编号、任务链、类型、状态、尝试次数、时间、脱敏错误摘要和结果可用性，不返回完整 `input`、`routing`、`output`、交付配置或请求者信息。未来视频提交在受控 input 中写入去路径化的 `displayName` 和适用的批次元数据；历史记录使用任务链名称、类型与短任务编号回退。
- 全站遗留蓝、青、靛、紫、粉等装饰色统一映射到 GitHub 蓝主色；绿、黄、红分别只作为成功、警告和错误语义。核心页面移除霓虹发光、装饰性渐变和任务看板彩虹列头，主体改为中性色表面。

### 实际改动

- 新增任务中心视图切换、任务链紧凑表格、搜索、状态筛选、刷新、分页及只读详情抽屉；详情包含批次编号/序号、时间线、耗时和结果是否已保存。
- n8n 任务运行库新增安全投影、来源隔离、状态/关键词过滤和分页总数查询；查询以参数绑定执行，并对路径、URL 和控制字符做展示层压缩/脱敏。
- 视频任务提交载荷新增安全文件名、`batchId` 和 `batchIndex`，不保存原始源路径。协作看板普通列头、标签和优先级配色已改为统一中性色或语义色。
- Tailwind 旧颜色族统一映射到产品/状态变量，GitHub 深浅主题继续保留；全局 legacy glow 类兼容保留但不再产生发光。浏览器验收中发现的两个旧 `taskBoard` 缺失翻译键调用已改用现有标题键。

### 隔离验证证据

- 本机固定 Node `22.22.3` 下，Vitest `103` 个文件、`1060/1060` 用例通过；task-flow Node 测试 `5/5` 通过；TypeScript 检查通过。lint 为 `0` 错误、`12` 条既有 warning；`git diff --check` 通过。
- Next.js `16.1.6` 生产构建成功，完成 TypeScript 校验与 `127` 个页面生成。首次沙箱构建因 Turbopack 内部处理器无权绑定临时端口而失败，允许本地构建进程使用回环端口后成功；不是 CSS 或源码失败。
- 浏览器验收使用 `/private/tmp/video-autoworker-tasks-ui.*` 下全新 SQLite 数据库和 4 条虚拟记录：页面只显示 3 条顶层任务，内部节点被排除；名称搜索、失败状态筛选、批次详情、任务链/协作任务切换及 GitHub 深浅主题均通过。最终新标签页完成两视图切换后控制台为 `0` 条 error/warning。
- 验收服务已停止，临时数据库目录已精确删除。没有读取或修改现有 Mission Control 数据库、n8n execution、视频队列、媒体、索引或生产运行状态。

### 生产部署与真实验收

- 功能提交 `06c630b` 已推送 GitHub `main`，`heisenbergs-1` 真实生产仓库 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker` 已从 `76d3412` 以 `--ff-only` 快进到该提交，工作树干净。远端固定 Node `22.22.3` / pnpm `10.33.0` 在隔离 worktree 完成生产构建，生成 `127` 个页面；候选服务使用隔离数据库在 `127.0.0.1:3018` 约 1 秒就绪。
- 发布时生产正在执行一个 12 项视频批次。先等待当前项完成，再预构建并在 batch worker 一次成功轮询后的空窗切换：3017 从旧 PID `10518` 切换到新 PID `34153`，Next.js 日志显示 `78ms` Ready。切换后 n8n execution 继续运行，批次保持 `running`、`error=null`，没有进入 `recovering`；未手工创建、重提、恢复、取消或修改任何任务。
- task-flow 安装器先 `--dry-run` 后 `--apply`，已将安全 `displayName`、`batchId`、`batchIndex` 载荷实现同步到 `second-original` 工作区；复验为 `skill_matches=1`、`agents_matches=1`、`memory_matches=1`。新式可恢复备份集合保持两份，本次恢复点为 `20260819-215647.SND3QF`。
- 为使常驻进程立即加载更新后的 task-flow，使用官方 video-lane supervisor 完成受控重启：worker 从 PID `66663` 切换为 PID `47879`，全局 worker 锁同步归属 PID `47879`。重启后的官方安装器 dry-run 通过，Gateway、n8n 与受保护监听均未改变；原有批次继续保持 `running`、`error=null`，无需等待下次 worker 启动才生效。
- 生产 API 真实只读验收返回 `39` 条顶层任务，`n8n-node` / `n8n-media-node` 内部记录为 `0`，列表字段齐全；旧默认接口仍只返回 `runs`，兼容性保持。页面真实显示 `39` 条，失败筛选得到 `4` 条，任务编号搜索准确命中 `1` 条，批次详情可见第 3 项，任务链/协作任务切换正常。
- GitHub 深浅主题实测背景分别为中性深灰和白色，任务页可见元素的渐变计数均为 `0`，浅色主操作为 GitHub 蓝。通过 SSH 转发的新浏览器来源没有 Gateway token，因此全局 WebSocket 报一条鉴权错误；该错误不影响 `/tasks` 与列表接口，也不是本次任务列表或配色变更引入的服务端错误。
- 部署后 Mission Control 与 n8n SQLite 均 `quick_check=ok`，3017 `/tasks` 为 HTTP `200`。生产任务仍在自然运行，验收只读，没有把并发任务产生的内部记录增量归因于部署。

### 回滚与已知边界

- 本次无数据库 schema 迁移。旧构建保留在生产仓库忽略目录 `.runtime/releases/76d3412/.next`；若新服务回归异常，可停止 3017、恢复该构建并重新启动，无生产数据回滚步骤。task-flow 可使用安装器生成的已验证恢复点回退。
- 历史运行记录若没有 `displayName`，只能显示任务链名称加短任务编号；新提交记录会逐步补齐真实视频名。内部子阶段暂不聚合到父任务，因为现有记录没有可靠、统一的父子关联字段。
- 当前状态统计明确标注为“本页”，分页总数为过滤后的顶层任务总数。回滚代码只需恢复本节涉及的前端、列表投影和视频载荷改动；没有生产数据回滚步骤。
