# 2026-09-05 导演脑统一发布候选收口

## 需求与范围

- 按 `PROJ-VIDEO-AUTOWORKER` 飞书工作大脑规则，把导演脑非剪辑能力、单视频链、OpenClaw 唯一外部鉴权和首次发布流程整理成一套可执行架构。
- 本轮先完成统一发布候选、测试与不可变制品门禁，再按门禁结果决定是否提交、推送和进入远端发布；记录必须持续区分本地候选、隔离验收与生产事实。
- DaVinci、剪辑软件时间线、自动剪辑、渲染和导出继续不在当前交付范围。

## 飞书阶段门

- 项目身份：`PROJ-VIDEO-AUTOWORKER`。
- 正式文档阶段读取结果：项目档案唯一命中，`registry_verified=true`、`persisted=true`、`gaps=[]`。
- 独立飞书导演脑仍属于同一项目；项目只保留一条视频任务链。外部用户鉴权由 OpenClaw 统一完成，3017、n8n 和导演脑只通过受控 loopback 协作。

## 本地候选改动

- 修复 standalone 工件漏带两项 shell 动态依赖：受管 Markdown 区段渲染 helper 和 OpenClaw 私有 Gateway RPC helper；显式动态闭包收敛为 26 项，并纳入 artifact/manifest 检查。
- standalone 只能从干净 Git payload 构建，必须生成绑定完整 40 位提交与 `<commit>-runtime` release 的 provenance、源码闭包摘要和内容清单；`.PhoenixBrain`、`.run`、数据库、密钥、缓存、日志及其他运行资产均为制品禁入成员。
- 三个 OpenClaw installer 统一复用 shared runtime gate、deployment lock 和 canonical batch root；同一批次必须绑定相同 commit/release，禁止各组件自行生成批次根、并行安装或跨 release 混装。
- 五项安装器高风险路径完成统一收口：锁前 marker/接管同步、失败后的进程树收敛、已验证回滚候选筛选、目标 Gateway fresh restart 合并、component journal 与 terminal handoff 的原子交接。
- 首次 legacy 发布新增唯一 preinstall orchestrator/controller，按 task-flow、video-command、director-brain 的受管顺序安装，只 fresh restart `qwen-current` 一次，再执行 runtime convergence 与 readiness；禁止逐项手工安装或从 bootstrap 绕过 preinstall handoff。
- 生产鉴权统一为 OpenClaw loopback：旧 cookie、API key 和 proxy credential 在该模式下不能提权；幂等、lease、fencing、revision/CAS 与发布锁继续作为一致性门禁保留。
- 视频入口静态审计确认 Telegram/OpenClaw、控制台和 CLI 均汇入唯一 `scheduler-runner -> submit-task -> /api/n8n/trigger`，没有第二套可达派发实现。
- 外部技能目录请求的超时现在覆盖响应正文读取阶段；即使 transport 已返回 header 但 `text()`/`json()` 永不结束，也会在同一有界 deadline 内失败关闭，避免构建或页面请求无限挂起。
- 补强 standalone、蓝绿、导演脑、鉴权、单链和安装器合同测试，覆盖动态 helper、来源闭包、候选 schema、锁/回滚、release 绑定及旧鉴权不可提权行为。
- 正式文档统一首次发布顺序：干净提交构建不可变 standalone，隔离验收，飞书 v2 -> v3 追加迁移，n8n transition/attestation，真实会话工具基线，统一 preinstall orchestrator，legacy controller，blue/green bootstrap，3017 精确切换，最终自然语言与真实视频闭环。
- 统一 preinstall 固定安装 task-flow、video-command、director-brain，只 fresh restart `qwen-current` 一次，再执行 runtime convergence 和 readiness；成功 handoff 后才能进入 bootstrap。失败按 runtime convergence、director-brain、video-command、task-flow 逆序补偿，必要时仅做一次 recovery restart。
- 按飞书决策 `DEC-VAW-DIRECTOR-BRAIN-INTERACTION-001` 收紧 OpenClaw 对话面：公开工具只保留只读 `extraction_status`，从 schema、参数归一化、处理器、Skill 与架构文档中移除 `start_extraction`、`backfill_extraction`；插件侧 loopback 客户端也拒绝任何非状态查询。3017 内部 API、共享应用服务和后台 scheduler 的启动/回填能力继续保留，不新增第二条任务链。
- 导演案例的人工结果门升级为硬合同：候选阶段仍允许缺少“最终使用”“成片位置”“最终效果”，模型提案继续禁止写入三个人工字段；进入“已确认”前必须三项全部由人工填写。历史上状态为已确认但缺少任一项的案例不再视为 reviewed，也不能支撑技法学习。
- 修复管理员审核内部视图与公共输出清洗冲突：未确认案例的“成片位置”仍不会返回普通工具调用，但管理员审核的两次 CAS 快照保留该人工字段，避免人工已填写却因清洗而永远无法确认。
- 同步刷新证据 outbox 对导演脑领域服务的源码闭包摘要，确保 release-readiness 仍绑定本次真实服务实现，旧摘要不能误放行。
- 修复 video-command 安装器在 SIGKILL 后恢复 stale journal 时误用恢复前目标摘要的问题：先完成旧半事务恢复并释放 shared deployment lock，再以原参数重新执行，重新采集目标摘要、复核 shared gate 并建立新事务；不得通过放宽断言掩盖恢复顺序错误。
- 独立安全审计发现 Docker `COPY . .` 的上下文仍可能携带 Git 已忽略的测试数据库、OpenClaw 测试状态、模型结果和 Playwright trace；`.dockerignore` 现递归排除 canary/tmp/test-results/backups、数据库及 WAL/SHM、日志和 PID，同时保留构建所需 `scripts/`、`ops/` 与 Markdown，并增加 release boundary 回归测试。

## 环境与验证证据

- 本地环境：Node.js `22.22.3`，pnpm `10.33.0`。
- 本轮语义边界定向验证：OpenClaw director-brain `261/261`、飞书导演脑合同 `115/115` 通过；新增覆盖对话拒绝启动/回填、插件 loopback 写动作失败关闭、三个人工结果字段逐项缺失拒绝、完整结果确认成功、历史不完整案例及其技法从学习上下文排除。
- 三个 installer 与 shared gate：`149/149` 通过；其余针对性鉴权、单链、制品、蓝绿和 preinstall 审计合计 `593` 项通过。
- 完整静态门禁通过：ESLint `0 error / 12` 条既有 warning，TypeScript、API parity `287 = 279 + 8`、Node/Bash 语法和 `git diff --check` 均通过；源码敏感扫描覆盖 `1135` 个文件，`0 findings`。
- Docker context 修复后，无并发重复 runner 的标准 `pnpm test` 最终通过 `2815/2815`：根常规 `2105/2105`、导演脑安装器 `82/82`、运行时收敛 `50/50`、遗留媒体 CAS/恢复 `96/96`、video-command `120/120`、director-brain `261/261`、task-flow `101/101`。此前唯一 video-command stale journal 失败点已在完整回归中通过。
- Docker context 修复后的定向测试 `33/33`、目标 ESLint、TypeScript 和 `git diff --check` 通过。提交前 dirty 工作树生产编译成功并生成经过成员/敏感审计的 standalone，但 provenance 明确为 `gitDirty=true`，只作为编译证据，不能发布。
- 本轮最终全量测试只证明本地候选；clean commit 构建、standalone 制品审计和浏览器 E2E 仍须从最终提交重新执行，不能复用旧 `.next`。

## 运行环境、部署与回滚

- 截至本地候选收敛完成，尚未执行本轮远端部署、飞书 schema v3 迁移或真实视频生产验收；不能把本地测试写成生产完成。
- 生产发布前必须重新只读核验真实仓库、remote、3017 PID/release、n8n 活跃 execution、活跃媒体节点和正式 waiting/running，并重新经过部署/运行时阶段门。
- 首次发布回滚由统一 preinstall 的 component journal、单次 finalize CAS、terminal handoff 和既有双库/工作流恢复证明共同约束；不能手工覆盖数据库、安装目录或 receipt。

## 已知问题与下一步

- 最终全量测试已经在没有重复 test runner 的条件下通过；后续若 clean build 或 E2E 失败，必须定位真实失败，不得扩大 sleep、跳过分区或忽略退出码。
- 提交前工作树为 `60` 个 tracked 变更与 `6` 个新增文件；安全审计时旧 `.next/standalone` 含 `.env` 且 provenance 为 `gitDirty=true`，后续 dirty 编译虽已由构建器移除禁入成员并通过 artifact audit，provenance 仍为 dirty，不能复用或发布。
- 完成 Git 成员、敏感边界和文档一致性独立审计后，形成干净提交并只从该提交重新构建；构建完成前仍不是可发布制品。
- 从最终干净提交重新构建，不复用 dirty 工作树产物；先在隔离端口验收，再按运行手册执行飞书迁移和首次生产发布。
- 发布完成后才进行 OpenClaw 自然对话、跨压缩恢复和一段真实视频的作品绑定、outbox、飞书证据及五阶段提炼验收。

## standalone 环境文件污染根因修复

- clean standalone 完成普通及 OpenClaw E2E 后曾出现禁入成员 `.env`。根因不是构建器遗漏，而是 Next standalone 启动时把进程工作目录切换到制品目录；安全修复接口又以 `process.cwd()` 作为环境文件落点，因此测试和生产管理员调用均可能污染不可变 release，直到下次启动审计才暴露。
- 环境配置写入现统一收口到显式 `AIWORKER_PLATFORM_ENV_FILE`。目标必须为 release 外的绝对单行路径，父目录与既有文件必须属于运行用户、不可为符号链接且不可由组或其他用户写入；写入采用同目录 `0600` 临时文件、文件与目录 `fsync` 及原子替换。安全修复接口不再回退到当前目录的 `.env` 或 `.env.local`。
- 普通 standalone、blue/green slot 和两套 OpenClaw E2E 启动器均显式传递平台环境文件与物理 standalone 根。三套 Playwright 配置在运行前固定 release manifest 摘要，结束后拒绝 `.env*`、重新核对摘要并执行完整制品审计，形成 E2E 前后不可变性门禁。
- 修复后的静态门禁为 ESLint `0 error / 12` 条既有 warning、TypeScript、API parity `287 = 279 + 8`、源码敏感扫描 `1137` 文件 `0 findings`。标准 `pnpm test` 为 `2819/2819`：根常规 `2109/2109`、导演脑安装器 `82/82`、运行时收敛 `50/50`、遗留媒体 CAS/恢复 `96/96`、video-command `120/120`、director-brain `261/261`、task-flow `101/101`。
- 本节记录时仍是未提交本地修复；旧 detached release 已经发生过污染，必须保留为失败证据且不得手工清理后发布。后续只从包含本修复的最终干净提交新建 detached worktree、重建并运行全量 E2E，以新门禁证明制品摘要不变。

## preinstall 并发不可变发布与 CI 收口

- GitHub Quality Gate run `33933551871` 在候选提交 `93744b8` 上失败，因此该提交未迁入生产。失败包含两个真实根因：orchestrator 在模块导入阶段过早解析目标 OpenClaw 命令，导致无 OpenClaw 的 CI 收集环境直接失败；不可变 CAS 临时发布文件没有携带发布者进程身份，并发发布或异常退出恢复时可能把另一活进程的临时成员误判为崩溃残留。
- orchestrator 现把受管路径与 OpenClaw 命令解析延迟到 `main()`，纯导入不再要求运行环境预装 OpenClaw。controller 的临时发布名称绑定发布进程 PID 与进程启动身份摘要；恢复逻辑保留活发布者的单链接及双链接临时成员，并对并发消失的文件容忍 `ENOENT`。无法取得启动身份但 PID 仍存在或无权探测时保守判活，不能把不可判定状态当成死进程清理。
- 回归覆盖活发布者 `nlink=1`、`nlink=2`、双进程 CAS、SIGKILL 后恢复以及进程探测失败关闭。独立安全复核结论为 GO；最终本地全量测试为 `2821/2821`，ESLint 为 `0 error / 12` 条既有 warning，TypeScript、API parity `287 = 279 + 8` 和源码敏感扫描 `1137` 文件、`0 findings` 均通过。
- dirty standalone 的生产编译与成员审计只作为候选证据；其 provenance 为 `gitDirty=true`，明确禁止发布。必须等待包含本修复的新提交通过完整 Quality Gate，再从该提交的新建 detached clean worktree 构建并确认 `gitDirty=false`。
- 本节记录时生产仍未切换：3017 继续运行 `57f6e6c-runtime`，未安装本轮 OpenClaw 组件，未迁移飞书 v3，也未执行真实视频闭环。后续仍须逐次满足活跃媒体节点、n8n 活跃 execution、正式 waiting 和 running 全部为零的生产硬门。

## 概览组件容器响应式优化

- 用户补充要求：普通概览组件继续盛满所在网格，卡框不随内容多少缩小；页面必须按可用空间自动适应，不能在中等宽度仍全部堆成单列。
- `WidgetGrid` 改为基于实际内容区的容器查询，而不是只依赖浏览器视口宽度。窄内容区保持单列，中等内容区自动两列，宽内容区恢复 12 栏比例布局；奇数尾卡自动横跨两列，宽屏尾行继续均分剩余栏，因此各行均无无效空洞。
- 普通卡继续保持 `320px` 稳定高度，动态长内容只在卡内滚动；响应式变化只调整列数和卡宽，不让实时内容改变外框。
- Node.js `22.22.3`、pnpm `10.33.0` 下目标 ESLint、TypeScript、生产构建及 standalone 成员/敏感审计通过；Playwright 几何回归 `5/5` 通过。真实浏览器复验：`1440px` 为三列、`900px` 为两列且奇数尾卡铺满、`390px` 为单列，三种宽度页面横向溢出均为 `0`。
- 本节记录时仍是本地候选。生产 `3017` 仍运行 `57f6e6c-runtime`；本次 dirty 构建只用于验证，不得直接发布。后续须从最终干净提交重建不可变 release，并按 3017 单服务蓝绿流程隔离验收和切换。


## OpenClaw 动态配置完成证据与发布流程优化

- 需求：接续历史运行时收敛候选，解决明确失败仍重复完整 RPC 导致 CI 超时，以及日志轮转/截断、重启信号处理和首次发布手册不完整的问题。本轮继续保持唯一视频链、OpenClaw 单一外部鉴权及非剪辑边界。
- 精确核验实际 OpenClaw `2026.7.1-2`：compaction 通过 `agents` 的 `none` 规则动态读取，纯 compaction 更新不保证输出 hot-reload applied 文案。官方 config.patch 成功返回前已刷新运行时快照；reloader 所有动态配置回调成功后才把同一 persisted hash 晋升至 `.last-good`。本次替换了旧测试虚构的完成信号，未修改 OpenClaw npm dist。
- 安装验证先核对 `.last-good` 与 CAS base hash 一致，patch 回执要求无重启；随后仅对尚未出现的日志通知和 last-good 晋升进行有界等待。明确失败首轮进入既有精确恢复，不再重复完整 health/config/listener 探测。完成仍要求配置、hash、原 Gateway PID、health 和完整工具能力共同一致。
- 日志采用真实字节 cursor，限定受管 compaction 与自动 meta 变更；拒绝失败/重启、轮转、重置、截断、行数饱和、游标回退。以安全物理文件身份和固定前缀的流式 SHA-256 检查同路径替换及 copytruncate 后回长；允许正常追加，拒绝最终 RPC 后尾段截短。last-good 晋升后重新取最后一份日志证据，卡住的日志 RPC 受总 deadline 限制并清理子进程。
- 首次发布手册补齐 bootstrap 所需 install verification 与 runtime convergence proof；preinstall 完成后在同一个 active freeze guard 下重新生成 post-install proof/evidence，不再复用安装前状态。
- Dockerfile 当前只允许 synthetic dirty 诊断构建。对应工作流已改为 Docker Diagnostic Build，删除标签发布触发、registry 登录和写包权限，显式关闭 push/load，只做构建检查，避免本次 Quality Gate 成功后自动发布不具备发布资格的镜像。正式 Mac standalone 发布路径保持原合同。
- 本地环境：Node.js `22.22.3`、pnpm `10.33.0`。最终标准 `pnpm test` 为 `2861/2861`：根常规 `2111`、导演脑安装器 `82`、运行时收敛 `90`、遗留恢复 `96`、video-command `120`、director-brain `261`、task-flow `101`。新增覆盖真实动态通知/last-good 晋升、错误日志、重启请求、延迟/永不晋升、缺失或不安全基线、轮转/截断回长和卡住 RPC 清理；此前约 20 秒超时的明确失败场景定向实测约 5–7 秒完成。
- TypeScript、API parity `287 = 279 + 8`、源码敏感扫描 `1137` 文件 `0 findings`、Node/Bash 语法、`git diff --check` 均通过；ESLint 为 `0 error / 12` 条既有 warning。Docker 工作流 YAML 与禁止镜像输出/写包权限检查通过。dirty 工作树生产编译及 standalone 成员审计通过，禁入成员为零；该构建仅为编译证据，不能发布。
- 本轮尚未安装生产插件、迁移飞书、修改生产数据或切换 3017；源码合同及独立审查不能替代生产事实。GitHub 同提交 Quality Gate、clean commit standalone 与浏览器 E2E 将继续独立验收。
- 部署与回滚：生产保持现有运行 release，无需回滚。后续必须从最终干净提交重建制品，完成隔离验收后再准备遗留父记录 CAS、飞书 v3 追加迁移、工作流 transition 和统一安装的精确对象、备份、恢复边界与本次确认；不得使用旧 dirty 制品或过期确认收据。


## 安装器 CI 分区与真实旧库发布准备收口

- `884c7f3` 的 GitHub Quality Gate 在 Node `22.23.2` 下暴露另一个安装器用例的资源竞争：task-flow 备份保留测试连续三次真实安装，在常规并发分区触发 15 秒上限；其余 2110 项通过。该文件整体纳入已有重型串行清单，保留原超时标准。分区校验为 `173 = 169 regular + 4 heavy`，不漏不重；独立 `24/24` 通过，原失败用例约 7.6 秒。
- 生产只读核验确认实际 Mission Control schema 迁移止于 `050`，未引入后续 ancillary 表；旧 parent-pre-media 工具因强制新版表存在而无法准备。修复只接受可验证的完整 through-050 legacy 或已识别现代迁移阶段；未通过核心迁移、连续 marker、完整列与相关表一致性校验时失败关闭，不创建或迁移任何生产表。
- `052` 的 intake control 与配套 event 表共同参与校验。表存在性、DDL、完整列描述及迁移 ID 摘要纳入现有 target/ancillary，贯穿 sealed prepare、权威备份复验、apply 前后及双库事务内校验；准备后建表、删表、ALTER 或修改 migration marker 都会在父记录 CAS 前拒绝。原 Gateway/lane/process/queue 冻结和四字段变更范围保持不变。
- parent 模式定向 `62/62` 通过，文件中其他 51 项由最终全量覆盖；完整标准 `pnpm test` 为 `2878/2878`：regular `2087`、task-flow installer `24`、director installer `82`、runtime convergence `90`、legacy recovery `113`、video-command `120`、director-brain `261`、task-flow `101`。Node `22.22.3` / pnpm `10.33.0` 下 TypeScript、API parity、源码敏感扫描和 diff 检查通过，ESLint `0 error / 12` 条既有 warning。
- 前序 `884c7f3` clean standalone 已通过 `515 + 4 + 4` 浏览器 E2E，测试前后制品摘要一致、禁入成员为零。新增修复将形成新提交，必须重新绑定该提交构建与验收；不能将前序 E2E 或失败 CI 记为本次最终发布证据。
- 本次候选生产编译和 standalone 审计通过，禁入成员为零；该 dirty 构建仍仅为提交前编译证据，不具备发布资格。
- 飞书迁移备份已通过一次性 Aqua 任务完成：仅认证及 GET 读取，私有快照覆盖 11 表、230 字段、22 记录，独立 `migration-backup verify` 通过；计划 v2→v3 只新增 14 字段，零新增表、零删改。SSH Keychain 的交互限制通过既有登录用户的 GUI 执行上下文解决，未复制密钥或改 ACL。一次性 job 已卸载且无 helper 残留，成功外壳与一次失败的空预检外壳单独归档，备份保留在独立私有目录。
- 生产事实仍为旧运行 release、原服务进程及未迁移 catalog；没有执行 parent apply、飞书字段迁移、OpenClaw 安装/restart、n8n 工作流变更或 3017 切换。父记录官方 prepare 仍要求短暂停止目标 qwen-current 入口并验证冻结；后续生产数据操作须展示当次备份与精确变更并取得确认。


## 真实安装测试职责归并

- `64d3097` 的 CI 常规分区仍发现一条默认 5 秒超时：submit-script 套件内的“无既有章节时追加视频规则”用例实际执行两次完整安装，并自建重复 SQLite/runtime fixture，未随安装器套件进入串行分区。
- 将完整用例迁入 `aiworker-task-flow-installer.test.ts`，复用已有 `runInstaller` 和隔离运行时准备，保留两次真实安装及全部原断言，采用该事务套件既有的 15 秒预算。移除 submit-script 文件中仅为该用例引入的数据库依赖与重复 fixture。没有删除测试或改动业务源码。
- 受影响两套测试 `62/62` 通过：submit-script `37`、installer `25`；原 CI 失败用例在新位置约 2.4 秒完成。静态调用盘点确认 regular 中不再存在真实调用 task-flow、director-brain、runtime convergence 安装器的遗漏路径；其余引用均为源码读取、摘要、成员清单或注入 fake installer。
- 最终本地标准全量仍为 `2878/2878`：regular `2086`、task-flow installer `25`、director installer `82`、runtime convergence `90`、legacy recovery `113`、video-command `120`、director-brain `261`、task-flow `101`。类型、API parity、敏感扫描、lint 与 diff 检查通过，lint 仅保留 12 条既有 warning；生产编译与 standalone 禁入成员审计通过。
- 此次仍未执行生产迁移或切换。最终 GitHub Quality Gate 与 clean commit 制品/E2E 继续绑定本次新提交独立验收，历史失败 run 及其他提交的通过记录不作为本次同提交证据。


## task-flow 清单生成的进程开销优化

- `13bec00` 的 CI 已进入串行安装器分区，但三次真实安装的备份保留测试仍触发 15 秒限制；因此确认隔离并发不是完整根因。旧 `write_tree_manifest` 在多次校验中按文件启动 BSD stat 和 Perl shasum，进程启动开销随文件数和备份扫描次数放大。
- 仅将该函数改为一次 Node 调用完成整棵树的元数据与流式 SHA-256 清单。保留所有原检查点和备份复核，不缓存跨阶段结果、不减少校验、不增加原测试超时。原始路径 Buffer 排序保持 LC_ALL=C 字节顺序，保留 ./、BSD八进制mode、shasum反斜杠文件名摘要前缀及readlink尾LF规则；旧TSV清单逐字节兼容。
- 文件以 O_NOFOLLOW 打开，64KiB块流式哈希；文件、目录、链接的完整stat快照及路径在前后重新验证，漂移会失败并删除本次partial manifest。拒绝旧行式清单无法安全表示的换行文件名；既有受管路径、备份形状、恢复与保留策略未变。
- 原安装器用例定向 `28/28` 通过，三次安装/备份保留从本地约 8.5 秒降为约 3.3 秒，仍保留 15 秒预算。额外格式/进程数/竞态定向 `6/6` 通过，覆盖跨BMP排序、空格、反斜杠、硬链接/符号链接、FIFO、排除项、单Node调用、部分输出清理和三类文件系统替换竞态。
- Node `22.22.3` / pnpm `10.33.0` 下最终标准全量为 `2884/2884`：regular `2086`、task-flow installer `31`、director installer `82`、runtime convergence `90`、legacy recovery `113`、video-command `120`、director-brain `261`、task-flow `101`。类型、API parity、敏感扫描、Node/Bash语法和diff检查通过；lint为0 error和12条既有warning；生产编译及standalone审计通过。
- 本次未改生产配置、数据库、飞书字段或运行release。后续继续从该新提交构建干净制品，并等待同提交GitHub质量门及浏览器验收；不得用前序提交结果替代。


## 并发恢复的队列锁交接修复

- `d87f5b2` 的 GitHub Quality Gate 在 regular 分区 `2085/2086` 通过后，唯一并发素材交接恢复用例报“视频队列锁无法安全读取”；本次是锁交接语义错误，不是超时。独占创建返回 EEXIST 后，原持有者可能已把 canonical 锁重命名释放，旧实现把随后 ENOENT 与所有读取异常一起折叠为无法安全读取。
- 在原有两次独占创建循环内，仅由 canonical 路径的 `lstat` 明确确认 ENOENT 时重新竞争；覆盖首次采样、第二次采样与 stale 隔离初始 rename 三个窗口。存在的新锁仍通过 `wx` 仲裁，不能覆盖；初始 rename 成功后的隔离读取、身份 CAS、恢复、删除和目录 fsync 错误继续传播。
- 新发布锁的宽限仅适用于当前用户所有、0600、regular、非符号链接、nlink=1、大小不超过4096的文件。空内容和正长度但尚未完成的 JSON 都保留原30秒发布宽限；权限/I/O错误与异常对象失败关闭，符号链接不能借由 stat 跟随目标被误认为不存在。releaseOwnedLock、失败创建清理、inode/token/CAS 与 successor 保留逻辑不变。
- 使用独立 Node 子进程的文件系统拦截精确安排竞态，不引入生产测试开关或随机 sleep。先确认旧实现的确定性失败，再验证采样/rename消失、successor先到、持续交接有界busy、部分发布、不安全mode/hardlink/目录/符号链接、EACCES/EIO和隔离后失败边界。
- Node `22.22.3` / pnpm `10.33.0` 下定向 submit-script `37/37`、batch-state `35/35` 通过；语法、TypeScript、API parity、敏感扫描和diff检查通过，lint为0 error/12条既有warning。最终标准全量 `2894/2894` 通过：regular `2086`、task-flow installer `31`、director installer `82`、runtime convergence `90`、legacy recovery `113`、video-command `120`、director-brain `261`、task-flow `111`。生产编译和standalone成员审计通过，禁入成员为零；提交前制品的provenance仍为dirty，仅作编译证据。最终clean构建/E2E与同提交GitHub Quality Gate继续独立验收。
- 本轮只改变本地源码、回归测试和项目记录，生产服务、数据库、飞书字段、n8n工作流与运行release均未变更；当前无需生产回滚。代码回退以Git历史为依据，不覆盖运行数据库或删除锁/恢复记录。待完整本地质量门通过后提交推送，再从最终干净提交构建、做浏览器隔离验收并等待同SHA的GitHub Quality Gate。
