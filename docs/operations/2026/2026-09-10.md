# 2026-09-10 导演证据确认与聊天审核修复（本地候选，用户暂停）

原视频分析已成功，但写入端与确认端的文本规范化不一致，导致已有38条素材证据候选仍被标记为确认冲突，且尚未形成提炼phase。用户要求修复回执与状态反馈，并明确允许聊天批准/驳回指定候选或批次、口述新增候选；新增不等于批准。

本地实现对齐NFKC/换行/空白规范化，新增仅回读的双轮验证与本地CAS回执恢复，查询成功源视频及outbox；未知候选数不再报零。聊天适配层增加完整候选预览、短批次码、真实用户确认/取消、版本校验、正式回读、部分完成与受控子进程超时。插件版本0.4.2及安装文件清单同步。平台核心未修改，未新增视频任务链或用户鉴权系统。

原控制器补充有明确源码、制品与回归证据绑定的同投影协议前向兼容声明；旧回执身份保留，普通跨契约反向回滚仍受限，发布前必须核对补偿和数据库恢复条件。需要先通过现有execve-adapter安装器更新旧launcher/auditor，不重做历史bootstrap。

验证包括真实38条只读回执演练、恢复/状态/提炼/聊天/运行一致性/制品门禁的相关回归、类型与静态检查。安装器最后一轮因用户暂停被中断，不能记为通过。各组有重叠，不累加测试数量。所有证据目前只证明本地候选或只读状态，不证明生产修复。

用户随后因额度不足要求停止，仅完成交接记录。本轮没有提交推送、构建发布、生产切换、回执写入、候选批准或飞书规则更新。原视频/证据/业务数据保留，未改远端学习文件。下一次从保留的工作树与本机待办继续，先核对最终hash和未完成安装验证，再完成Git/制品/部署/真实查询及预览取消验收；实际批准仍等待用户针对明确对象确认。


## 恢复工作

用户随后明确要求继续完成。暂停交接49文件逐一核对SHA一致，原修改完整保留；重新读取同项目根的部署阶段规则，身份核验与gaps通过，规则执行要求及决策与暂停前相同。重新SSH核验原服务与证据冲突仍在；发现并修复9.2工具上下文requesterSenderId与聊天事件senderId的映射差异，以真实SDK形状补充预览/取消测试。安装器被中断的验证继续补齐，其他未变化证据按影响范围复用。仍未通过真实用户确认的候选不得批准。

恢复后补验：导演插件安装器85/85通过，确认隔离进程退出；9.2真实工具context与hook用户字段归一化测试通过。候选数改按感知阶段投影收据条数统计，后续模型候选照常累计，避免重复或误报0。整仓ESLint为0错误/12既有警告，TypeScript通过。正式制品及生产验收尚在进行。

最终补充：基于真实38条数据的审核预览最初8259字节超出8KiB；将显示时间码收敛到秒、每条摘要32字后，38条完整预览6562字节且无内部作品ID，定向通过。同事务prepare完成登记、exact-source claim和既有回执确认，失败全回滚并保持人工审核门；原普通pending与已有任务retry语义保持。新版auditor按实际静态依赖校验新模块，旧未引用制品不被新全局必需项误拒绝，39项通过。

用户13:41会话截断：官方会话元数据为done/未abort、模型maxTokens4096，不能认定死锁；单轮完整history只读请求被自动审批拒绝，因此未声称取得精确stopReason或工具明细。对明确已有作品的第一步请求，Skill改为查询原提炼/证据状态并短答，不能重建证据或索要触发脚本。

最终prepare回归65项通过，原子完成exact-source perception待审，无视频/模型/审核副作用。部署编排在正式修改前暂停新准入并备份双库，关闭导演调度以避免旧服务读取新版插件结果；恢复后核对原候选版本并再启调度。当前可发布候选即将提交，生产状态另按后续收据记录。

首轮完整CI d5df1c5：核心分区2277通过、7失败，定位为3个现行fixture仍写0.4.1；仅测试版本数据同步0.4.2后3文件16/16通过。保留失败证据，不把失败CI当成功。后续以修正提交重新触发正式Quality Gate。

生产预检发现新增validator在旧v3控制源码中不存在，却被新adapter无条件要求。现改为以已验证Git commit的tree及provenance引用决定依赖集合：旧未引用版本按旧闭包验证；新提交仍引用却删除helper时失败关闭。adapter6/6与successor8/8通过，避免无条件exists降级。原组件未被安装器替换。

重新连接新隧道后先恢复原业务：入口15→16 active，video lane及导演调度恢复，插件仍0.4.1。d3代码在真实旧d23安装上只读管理器解析通过。d3 CI 2283项通过、1项失败，原因是新增历史兼容测试读取d23不可变提交，而GitHub默认shallow checkout不含历史对象；Quality Gate checkout设置fetch-depth:0，业务代码不变，后续重新核验完整CI。
