# 2026-08-20 视频重复门禁、正式结果工作台与组件库评估

## 需求

- 视频提交遇到同名或同真实路径历史记录时，先提示用户确认；只有后续明确回复“确认重新分析”才允许创建新任务。
- 在 `/materials` 增加正式视频分析结果明细，与历史素材检索明确分离，并支持列表、搜索、筛选、分页和完整结果查看。
- 继续收敛全站配色，主体只使用中性色与一个主色，绿、黄、红只承担成功、警告和错误语义。
- 核查 Phoenix 组件库能否直接替换当前页面组件，并把本项目缺少的通用组件登记到组件库。

## 实现内容

### 重复视频确认门禁

- `aiworker-task-flow` 在创建任务状态、复制媒体或触发 n8n 前，按 `realpath` 后的真实路径和文件 basename 检查历史任务；目录中任一视频命中时阻断整批提交。
- 首次命中只返回固定确认提示，不创建状态文件、不复制媒体、不启动 worker、不触发 n8n。只有用户后续精确回复“确认重新分析”并由插件传入 `confirm_duplicate` 后才放行；模型不得在同一轮自行确认。
- 同一任务 ID 的幂等重试继续返回原任务状态，不被误判为新建重复任务。插件候选版本由 `0.5.8` 升为 `0.5.9`，升级入口同步更新版本契约。

### 正式视频分析结果

- `/api/n8n/runs` 新增 `view=video-results`：列表查询使用 `limit/offset/query/status`，详情使用 `taskId`；两者都严格按当前 workspace/tenant 隔离。
- 接口只投影标题、状态、任务/批次信息、摘要、章节、时间线、语音转写、画面证据和正式报告，不返回原始路径、input、prompt、routing、delivery、workers 或模型路由。
- 列表由 SQLite 直接提取短摘要、章节数和时间线数，不再把每条完整报告加载到 Node；只有打开单条详情时才读取并清洗该任务的正式输出。
- `/materials` 顶部新增“素材检索 / 分析结果”切换。分析结果采用响应式主从布局，包含搜索、状态筛选、分页、刷新、摘要、章节、时间线、音画证据和完整报告；只有唯一匹配本地片源时才展示播放器和时间线定位。

### 配色收敛

- 登录、加载、导航、仪表盘、聊天、设置、任务、素材和主要管理面板统一到中性色表面与单一主色；模型类型、普通标签和装饰元素不再各自分配高饱和颜色。
- 办公室可视化保留结构性主色网格和语义状态点，移除时段渐变光效、闪烁星点、热力光斑、发光阴影和多色角色变体。遗留 `glow-*` 类仅为兼容保留，实际不再产生发光。

## Phoenix 组件库结论

- 组件库健康与就绪检查正常。现有相近的主从/列表详情组件采用 Vue 3 或 uni-app x，不能安全替换本项目的 React 19 / Next.js 16 实现；本轮没有复制不兼容源码，也没有以技术栈不匹配的组件冒充复用。
- 已按 `react`、`nextjs` 及“主从工作台、资源工作台、列表详情、详情抽屉、分页筛选”等关键词完成去重，并提交通用组件需求：`req_k63D8ffVyxZFCP4T`，标题“React/Next.js 响应式主从资源工作台”，状态 `proposed`。
- 编辑凭证只保存在本机 Keychain 的专用服务中，不写入仓库、文档、日志或聊天。组件库正式提供兼容实现后，再以独立变更替换本地组合组件并执行视觉与回归测试。

## 本地验证证据

- 固定 Node 22 环境下，主项目 `103` 个测试文件、`1064/1064` 用例通过；视频插件 `345/345`、task-flow `7/7` 全部通过。
- TypeScript 检查、`git diff --check` 和 Next.js `16.1.6` 生产构建通过，完成 `127` 个页面生成。lint 为 `0` 错误、`12` 条既有 warning；其中素材页保留 3 条既有 `<img>` 性能提示。
- 隔离服务使用 `/tmp/aiworker-ui-qa.*` 下的新 SQLite 数据库和临时管理员，不读取或修改生产 Mission Control 数据库。浏览器实测 `/materials`、分析结果空态、`/tasks`、登录页、概览页、GitHub 深浅主题和 `390px` 手机宽度均正常。
- 隔离环境缺少 Gateway 凭据，因此存在预期的 WebSocket 鉴权日志；它不影响本次 HTTP 页面和结果/任务列表功能，也不是本轮前端回归。

## 生产发布、验收与回滚边界

- 功能提交 `2b75a3eae61aad291e1a0a5daf3a12fbb5ed7c52` 已推送 GitHub `main`，并同步到
  `heisenbergs-1` 的真实运行仓库 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`。发布前确认该目录、
  Git remote、3017 监听和运行进程属于同一条生产线；生产仓库工作树干净。
- 因生产批次正在运行，先在隔离 worktree 使用生产 Node `22.22.3`、pnpm `10.33.0` 完成构建与 127 个页面验证，
  再在父批次仍为 `running`、全部已领取子项退出且 worker 尚未领取下一项的空窗切换 3017。旧构建恢复点保留在
  `.runtime/releases/7d2e8f4/.next`。
- 3017 从 PID `34153` 切换到 PID `1872`，`/materials` 与 `/tasks` 均返回 HTTP `200`。n8n PID `71374`、
  Gateway PID `27824`、video worker PID `47879` 全程连续，三者未重启；Mission Control SQLite
  `PRAGMA quick_check` 返回 `ok`。
- 生产只读接口当前返回 38 条正式视频记录。浏览器在 `/materials` 选择
  `《地球之极》第二季-第3集-世界上最孤独的小镇 .mp4` 后，确认状态为 `succeeded`，摘要 1460 字符、
  8 章、37 段时间线、完整报告 49180 字符；`/tasks` 同时正常显示 43 条真实任务链记录。
- 发布后的运行批次 `video-batch-14189b59fd7105888fd92a805ebb5b6bb6d5b2ada4845be78816134ebf176339`
  仍为 `running`、`error=null`，没有批次项进入 `recovering`。本次没有创建、重提、恢复、取消或修改任何生产任务，
  也没有修改 n8n execution、数据库、媒体、索引、模型或记忆资产。
- 页面、配色与 `view=video-results` 只读接口已经在生产 3017 生效。`0.5.9` 插件和新 task-flow 仅随源码进入
  生产仓库，因未重启 Gateway、video worker 或 n8n，重复提交确认门禁尚未在生产运行进程中激活，后续必须另行
  受控发布和验收，不得把“代码已部署”表述为“门禁已生效”。
- 已停止的 `2b75a3e` 隔离 build worktree、3018 候选、staged 目录和重复切换目录已统一移入远端废纸篓
  `~/.Trash/aiworker-video-autoworker-20260820-NHJeOk/`，可恢复；正式恢复点、当前 `.next`、数据库、媒体、索引、
  模型和任务状态均未移动或删除。清理后 3017 与受保护进程、HTTP 探针保持正常。
- 3017 回滚时恢复 `.runtime/releases/7d2e8f4/.next` 并仅重启控制台；未激活的插件和 task-flow 不需要生产运行态
  回滚。Phoenix 组件需求 `req_k63D8ffVyxZFCP4T` 仍为 `proposed`，待组件库提供 React/Next.js 兼容实现后，
  再以独立变更替换和回归，不在本次生产发布中混用 Vue/uni-app x 代码。

## 右上角系统提醒收敛

### 需求与实现

- 更新和系统提示统一放入顶栏右上角铃铛提醒，不再以正文横幅占用页面高度或推动内容布局。
- 新的提醒弹层统一承载本地模式、Mission Control 更新、OpenClaw 更新与 OpenClaw Doctor 警告，并保留原有更新、
  版本查看、命令复制、诊断修复、详情和忽略操作；业务未读数与可见系统提醒数合并显示在铃铛角标中。
- 新增可复用 `AnchoredPopover`，弹层脱离正文流定位，支持点击外部关闭和 `Escape` 关闭；桌面锚定顶栏右侧，
  窄屏限制在视口内。旧横幅组件保留用于兼容与回滚，但主页面不再渲染。
- `/notifications` 已纳入当前面板路由识别，“查看全部通知”会关闭弹层并进入完整通知页。10 种现有语言均补齐提醒文案。

### 组件库与本地验证

- Phoenix 组件库未发现可直接复用的 React/Next.js 可访问锚定弹层；完成去重后已登记需求
  `req_5IAu86LQ_hZXpNt_`：“React/Next.js 可访问锚定弹层”，状态 `proposed`、优先级 `medium`。编辑凭证只存
  本机 Keychain，不进入仓库、文档或日志。
- Node 22 下 TypeScript、生产构建和 `git diff --check` 通过；主项目 `1068/1068`、视频插件 `345/345`、
  task-flow `7/7` 通过。lint 为 0 错误、12 条既有 warning，生产构建生成 127 个页面。
- 隔离浏览器确认正文始终从 56px 顶栏后开始；390px 窄屏弹层位于 `12–378px` 且无横向溢出，深浅主题、
  `Escape`、关闭按钮和通知页跳转正常。隔离环境仅出现缺少 Gateway 签名的预期错误，与本次 UI 无关。
- 本节先记录本地候选与 Git 提交前证据，不代表远端 3017 已加载；生产发布仍只允许重启控制台，不得重启 Gateway、
  n8n 或 video worker，也不得修改生产任务、数据库、媒体、索引、模型或记忆。

### 生产发布与页面验收

- 功能提交 `93439768b689dc1388b8dd908463ef3366b79e81` 已推送 GitHub `main`，并从同一提交在
  `heisenbergs-1` 仓库外置 worktree 完成 Node `22.22.3`、pnpm `10.33.0` 的隔离安装与构建，127 个页面通过；
  候选在隔离数据目录和 `3018` 上真实启动后，登录页、任务页与原生 SQLite 加载通过，随后正常停止。
- 生产仓库 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker` 已 `--ff-only` 对齐该提交，标准 `.next`
  已持久化为新 bundle；当前运行 PID `478` 从
  `.runtime/releases/9343976-runtime/.next/standalone` 提供 `127.0.0.1:3017`，`.runtime/RUNNING_COMMIT`
  同步为 `9343976`。下次按标准启动脚本重启时会使用生产根目录的新 `.next`。
- 旧 `2b75a3e` bundle 保存在 `.runtime/releases/2b75a3e/.next`。回滚时只停止 3017，把当前 `.next`
  移到隔离候选目录、恢复该回滚点并重新启动控制台；不得联动重启 Gateway、n8n 或 video worker。
- 生产浏览器实测提醒弹层打开前后正文 `mainTop` 均为 `56px`、正文高度均为 `664px`，页面
  `scrollWidth/clientWidth` 均为 `1280/1280`；弹层宽 `448px`、独立悬浮于右上角。Escape 关闭成功，
  “查看全部通知”实际进入 `/notifications`，随后页面恢复到 `/materials`。仅观察到既有 SSH 转发环境的
  Gateway `device signature invalid`，与本次提醒 UI 无关。
- n8n PID `71374`、qwen-current Gateway PID `27824`、video worker PID `47879` 全程未重启；Mission Control
  SQLite `quick_check=ok`。功能发布验收时，生产仓库、GitHub 和 `RUNNING_COMMIT` 均为 `9343976`；本节后续作为
  docs-only 审计提交推送并在生产仓库快进，不重建或重启 3017，因此仓库 HEAD 会前移，而运行 bundle 与
  `RUNNING_COMMIT` 继续保持 `9343976`，两者不得混称。

### 发布过程异常与任务边界

- 第一次安全窗脚本因生成的 SQLite 命令中混入字面量 `+`，在停止服务前即失败，3017 与任务均未改变。
- 后续平滑切换利用 Next.js 16 的 `server.close()`：旧 PID `1872` 先停止接受新连接，新 PID `478` 接管 3017；
  但部署脚本在“监听已关闭”的预期状态下被 `set -e -o pipefail` 和无结果 `lsof` 提前终止，导致 3017 出现一次
  短时监听空档，随后以已验证 release 直接恢复。该脚本未进入仓库，也不得作为后续部署模板复用。
- 旧 PID `1872` 后续仅剩远端浏览器和 SSH 转发连接；连续采样未发现 Whisper/ffmpeg 或其他子进程，也没有 n8n
  TCP 连接，当前视频工作目录的 37 段音频检查点仍为 0。为清理已取消监听的旧控制台，最终只终止该精确 PID，
  未触碰受保护进程。
- 发布后 n8n execution `51` 仍为 `running`；父任务仍为 `accepted`，`audio` 与 `vision` 子阶段均为
  `running`、`error=null`，批次仍为 `running`、`error=null`，worker 心跳持续。没有创建、重提、恢复、取消
  或改写任何任务，也没有写 n8n execution；但鉴于音频阶段无检查点且原请求连接已消失，本次不能声称对该运行任务
  “确定无影响”。按用户边界保持只读观察，后续必须以 execution/父任务终态为准。
- 后续 3017 发布不得在 `n8n-media-node` 为 `accepted/running` 时做同进程平滑切换；必须等待所有活跃媒体回调
  终态或使用经过隔离测试、能可靠排空升级连接并保证回调重试语义的专用发布器。Shell 中读取“允许为空”的 `lsof`
  结果时必须显式 `|| true`，不得让预期空结果触发半完成切换。

## 媒体资源库检索与任务链可观测性

### 需求与方案

- 将 `/materials` 明确升级为“媒体资源库”，保留原有素材片段的关键词、向量和混合检索，同时让正式视频学习结果可按
  标题、摘要、章节、逐段语音、逐段画面、完整转写和报告内容检索；命中带时间码的结果时可打开对应学习档案并定位原片。
- `/tasks` 的顶层任务没有独立 `startedAt` 时，以 `acceptedAt` 作为耗时起点，以 `completedAt` 作为终点；运行中任务
  按当前时间持续更新。失败任务直接展示失败阶段、中文原因与排查建议，不再要求打开原始载荷判断。
- 本轮继续使用项目内最小 React/Next.js 实现。Phoenix 组件需求 `req_k63D8ffVyxZFCP4T` 仍为 `proposed`，没有可安装、
  版本锁定且兼容 React 19 / Next.js 16 的替代组件，因此不引入 Vue 或未发布 API。

### 本地候选实现

- 新增 `/api/n8n/video-library?q=...`，只检索当前 workspace/tenant 内已完成的视频分析记录，返回安全摘要、命中字段、
  章节或片段时间码和排序分值，不返回 input、prompt、routing、delivery、workers、模型路由或源文件路径。
- 新增只读原片索引，从受控视频批次状态中按任务编号解析唯一原片。索引校验状态文件类型与大小、任务编号、真实文件、
  扩展名、文件名和字节数；同一任务映射到不同文件时整条映射失效，不随机选择。前端只接收 `mediaAvailable`。
- 新增 `/api/n8n/video-library/asset?taskId=...`，先执行 viewer 鉴权和 workspace/tenant 学习记录校验，再解析原片；支持
  `GET`、`HEAD` 和单段 HTTP Range，播放前再次核对文件类型与字节数，响应采用 `private, no-store`。
- `/materials` 顶层调整为“媒体资源库 / 片段检索 / 学习内容”。学习内容采用检索命中列表与详情工作台：可查看摘要、
  章节、逐段音画证据、完整转写与正式报告；原片存在时，搜索命中、章节和时间线均可定位播放，原片缺失时保留完整档案。
- `/tasks` 新增统一耗时与失败解释模块。合法的同秒记录显示 `< 1 秒`；顶层任务缺少开始时间时明确标注“任务接收到结束”
  的计算依据；源文件不存在、语音/画面前置未完成和各模型请求中断均显示对应阶段与中文建议。

### 本地验证与生产边界

- Node `22.22.3` 下，定向测试 `27/27`；主项目 `1083/1083`、视频插件 `345/345`、task-flow `7/7` 全部通过。
  TypeScript、`git diff --check` 和 Next.js `16.1.6` 生产构建通过，构建生成 `129` 个页面。lint 为 `0` 错误、
  `12` 条仓库既有 warning，本轮没有新增 lint 问题。
- 原片接口测试覆盖未授权拒绝、workspace/tenant 隔离、Range、HEAD、非法范围、索引后文件变化和冲突映射；搜索测试覆盖
  章节与逐段画面命中、精确秒数、安全字段投影和路径脱敏。
- 部署前只读快照显示正式视频记录 `43` 条、失败顶层任务 `8` 条；失败类型包括源片不可访问、语音/画面前置未完成以及
  语音、画面或最终汇总请求中断。顶层失败记录普遍 `startedAt=null` 且 `acceptedAt/completedAt` 完整，验证了耗时回退必要性。
- 本节当前只记录本地候选，不代表生产 3017 已加载。部署前必须在远端隔离 3018 使用真实只读数据验证检索与原片定位；
  只有全部 `n8n-media-node` 退出 `accepted/running` 后才允许切换 3017。发布只可重启控制台，不触碰 Gateway、n8n、
  video worker、生产任务、数据库、媒体、索引、模型或记忆资产；旧运行 bundle 必须保留为可恢复回滚点。
