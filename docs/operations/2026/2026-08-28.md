# 2026-08-28 accepted 孤儿任务收敛与生产放通

## 当前状态

- canonical 功能提交 `542eebdd871f0d960d972e879310bec7a3d15cca` 与 supervisor 启动顺序修复 `c3859f080da02cddad9fdfd84a80d33f8261f54c` 已推送 GitHub `main`，生产源码仓库已按 `--ff-only` 同步到后者。
- 3017 与 n8n 的实际功能 runtime 均来自 `542eebd`；`c3859f0` 只修改 supervisor 安装器及其回归测试，不要求重建或重启 3017/n8n。
- 孤儿父任务 row `274` 已通过正式 reconciliation API 精确收敛为 `failed / VIDEO_CALLBACK_LEASE_EXPIRED`，没有重试、取消或重提原任务；attempt 仍为 `0`。
- 唯一 video lane 已放通。孤儿单视频批次为 `completed_with_errors`；24 集批次严格只有第 1 项进入 `running`，其余 23 项仍为 `queued`。n8n execution `68` 已启动，`prepare` 成功且 `audio` 已进入运行。

## 需求与现象

- 修复视频工作流已经返回受理、但没有建立任何媒体阶段时，父任务永久停留在非终态并阻塞全局串行队列的问题。
- 已确认 n8n execution `66` 的首个媒体回调因认证返回 HTTP `401`；对应平台 task row `274` 留在 `accepted`，且没有 `prepare`、`audio`、`vision` 或 `finalize` 子任务。
- 目标 24 集批次当前完成度为 `0/24`。worker 每次恢复都会先遇到同一条孤儿父任务，因此后续项目持续处于队头阻塞状态；这不是 24 个视频分别失败，也不是模型推理速度问题。

## 根因

- 触发端在共享 Webhook secret 缺失时仍可创建父任务并调用 n8n，未在持久化任务前 fail-fast。
- 原视频工作流先返回 HTTP `202`，再发起媒体回调。平台据此记录 `accepted`，但回调认证失败后没有路径把父任务收敛为终态。
- 原任务状态机没有面向“已受理但从未建立媒体阶段”的租约回收；worker 只能重复读取同一个非终态记录。
- 单一全局 video lane 为刻意保留的资源保护边界，但原 drain 策略会在当前批次仍需恢复时退出，因此队头孤儿会同时阻塞同批次剩余项目和后续批次。
- 仅增加超时更新仍不足以安全修复：若回收检查与迟到媒体回调并发，回调可能在检查后创建子任务，甚至重新推进已经失败的父任务。

## 设计决策

### 触发与 Claim

- `N8N_WEBHOOK_SECRET` 缺失时，触发 API 在读取生产数据库和创建父任务前返回 HTTP `503`；底层 Webhook 客户端再次校验，避免其他调用方绕过入口门禁。
- 视频工作流调整为先调用受认证的父任务 Claim 接口，Claim 成功或命中同一任务的幂等 `running` 状态后，才构造并返回 HTTP `202`。
- Claim 使用任务 ID、幂等键、binding、tenant 和 workspace 的同一作用域校验，只允许 `queued/accepted -> running`；失败、成功或取消任务不会被重新激活。

### 900 秒租约收敛

- 对 `video-analysis` 父任务采用默认 `900` 秒回调租约。
- reconciliation 使用 scope-aware SQL compare-and-swap，只处理租约已过、状态为 `accepted/running`、且四个确定性媒体子任务均不存在的父任务。
- 收敛结果写入稳定错误码和明确完成时间。只要任意媒体子任务已经落库，长视频无论运行多久都不会按 pre-media 孤儿回收。
- reconciliation 由显式内部 POST API 执行；只读任务查询接口不暗中写库。

### 迟到回调与 worker

- 媒体子任务创建改为 `IMMEDIATE` SQLite 事务：事务内重新读取父任务、拒绝终态父任务，并创建或读取确定性子任务。该事务与 reconciliation 的写锁互斥，关闭“检查后迟到创建”的竞态。
- worker 在等待已存在的非终态平台任务期间按约每分钟调用一次 reconciliation；首次检查通常在等待约 `60` 秒后发生，新提交后才成为孤儿的任务不必等待原有长超时窗口。
- reconciliation 将任务转为 `failed` 后，现有平台终态权威逻辑负责更新持久批次并继续同批次下一项；失败项目不会重新提交。

### API 契约

- 新增内部 Claim 与 reconciliation API 契约，补齐 OpenAPI 定义。
- API 路由与 OpenAPI 的 parity 检查已通过；没有通过忽略项掩盖新增接口。

## 改动范围

- n8n Webhook secret 配置检查与触发端 fail-fast。
- 视频父任务 scope-aware Claim、pre-media orphan reconciliation、确定性媒体子任务事务创建。
- Claim API、reconciliation API 与媒体回调终态拒绝。
- bundled 视频工作流的 Claim-before-202 顺序。
- task-flow 平台客户端、批次 worker 的每分钟 reconciliation 与队列继续执行逻辑。
- OpenAPI 对 Claim、reconciliation 及相关现有路由的契约补齐。
- 对应路由、SQLite 状态机、工作流结构、worker 和 API parity 定向回归测试。

本轮没有改变视频分段、Whisper、画面分析、报告合并、模型路由、素材保留或全局串行 lane 的业务策略。

## 本地验证证据

- 使用生产同代 Node `22.22.3`、pnpm `10.33.0` 运行本次改动相关的定向测试，结果为 `94/94` 通过。
- 定向覆盖包含：secret 缺失零父任务写入、Claim 身份与作用域校验、Claim 幂等、终态不可复活、900 秒边界、任意媒体子任务保护、跨 scope 拒绝、`IMMEDIATE` 事务竞争、迟到回调拒绝、worker 每分钟收敛、首项失败后继续后续项目，以及工作流 Claim-before-202 顺序。
- 主 Vitest 测试 `1130/1130`、OpenClaw plugin 测试 `87/87`、task-flow Node 测试 `21/21` 均通过。
- TypeScript、全量 ESLint（`0` error；`12` 个既有 warning）、差异检查、Bash 语法检查和 API contract parity（`282` routes / `274` OpenAPI / `8` ignored）均通过。
- Next.js 生产构建通过，共生成 `131` 个 routes；独立 standalone 候选在端口 `3018` 验证 health、n8n status、secret 配置布尔值和错误 Claim secret 拒绝均符合预期，随后已停止候选进程并清理临时文件。

## 实际生产发布与验收

1. 生产身份复验为 `HEISENBERGS-1deMac-Studio.local / heisenbergs-1`，真实仓库为 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`，Git remote、监听端口和实际进程均与项目记录一致。
2. 已生成并验证发布备份 `/Users/heisenbergs-1/ai-worker/backups/video-autoworker-orphan-reconcile/20260828-orphan-reconcile.MpsUBz`。数据库、批次主备状态、工作流和运行指针的回滚证据均保留在生产受控目录，不进入 Git。
3. 生产仓库先快进到功能提交 `542eebd`。Node `22.22.3`、pnpm `10.33.0`、冻结锁文件构建通过；3017 切换到独立 release `.runtime/releases/542eebd-runtime`，监听 PID `21862`，Webhook secret 配置状态为 true，错误 Claim secret 返回 HTTP `401`。
4. Task Flow 事务安装完成；n8n 固定 runtime 使用提交 `542eebd`，监听 PID `22826`，`5678/5679` 健康。只更新并发布视频工作流，普通任务工作流哈希前后相同；发布前最大 execution ID 保持 `67`。
5. row `274` 通过正式 scope-aware reconciliation API 唯一收敛为 `failed`，错误为 `[VIDEO_CALLBACK_LEASE_EXPIRED] n8n 视频任务已受理，但在 900 秒内未建立媒体处理阶段`，attempt 仍为 `0`。row `21` 保持 `accepted`；其余 278 行前后投影哈希均为 `47146c876d09ae317aa8cecf50cf8bfa04a441773ef72a66df39292251dc1372`，审计记录只增加 1 条。
6. 首次恢复 supervisor 时发现安装器对已 disabled 的服务先 `bootstrap`、后 `enable`，导致 `launchctl` error 5。安装器自动恢复原状态，未启动 worker、未推进队列。修复将顺序改为 `enable -> bootstrap -> kickstart`，新增 disabled-service 回归测试；Node `22.22.3` 下测试 `7/7`、ESLint、Bash 语法和 diff 检查均通过，形成提交 `c3859f0`。
7. 生产仓库再次纯快进到 `c3859f0`。supervisor dry-run 通过后 apply 成功，回滚备份为 `/Users/heisenbergs-1/ai-worker/backups/video-lane-supervisor/20260828-132127.51xOEx`。LaunchAgent 为 enabled/running，唯一 worker PID `43858`；全局锁 owner 同为 `43858`。旧死亡锁 PID `67625` 由 worker 自行接管，没有人工删除。
8. 放通后，孤儿单视频批次 `single:d096767f47dcfa63729280720c74bf7c` 收敛为 `completed_with_errors`，对应项目保持 `failed`。24 集批次 `video-batch-e09094990ec5327ff21ad8095130f056866a02fdb200d6aed72b6020a0844a66` 为 `running`，状态计数严格为 `running=1 / queued=23`。
9. 新 n8n execution `68` 使用 `aiworker-video-analysis-v1` 启动。观察点上，首项父任务为 `running`，`prepare` 子任务已 `succeeded`，`audio` 子任务已 `running`；没有创建第二个正式父任务。按验收边界不等待 24 集全部完成。
10. 3017/5678/5679 原监听 PID 保持 `21862/22826/22826`，n8n SQLite `quick_check=ok`，当前只有 execution `68` 运行。生产源码工作树干净，HEAD 为 `c3859f0`；应用 `RUNNING_COMMIT` 保持功能提交 `542eebd`，二者职责不同且均已明确记录。

## 运行时版本治理

- 用户授权环境采用最新版本原则后，已在飞书项目 `PROJ-VIDEO-AUTOWORKER` 写入并生效项目规则 `VAW-RUNTIME-VERSION-001`：默认采用目标组件明确支持且已在目标环境验证的最新稳定版本；存在兼容约束时采用最新兼容稳定版本。
- 本次事故恢复继续使用生产已安装且完整验证的物理 Node `22.22.3`。这不是长期锁定 Node 22；事故恢复不同时引入未经兼容矩阵验证的运行时大版本迁移。
- 后续升级应独立侧装 Node `24.15+` 或当时目标组件支持的更新稳定版本，补齐 CI 版本矩阵、原生模块、OpenClaw、n8n、3017、worker 锁接管及回滚验证后再切换。不得把无界 `>=22` 或运行时自动下载 latest 当作兼容性证明。

## 回滚计划

- supervisor 安装级回滚使用 `/Users/heisenbergs-1/ai-worker/backups/video-lane-supervisor/20260828-132127.51xOEx`；发布级回滚使用 `/Users/heisenbergs-1/ai-worker/backups/video-autoworker-orphan-reconcile/20260828-orphan-reconcile.MpsUBz`。回滚前先停止唯一 lane 并保留新任务现场。
- 3017 可切回上一独立 release；n8n 只恢复上一视频工作流与 runtime 指针，Task Flow 只使用事务安装器生成的 verified 备份。不得覆盖普通任务工作流或整库状态。
- n8n 回滚必须离线执行，只恢复视频工作流与上一 runtime 指针；不得重导或覆盖无关工作流、execution 或凭据。
- 若需要撤销 task row `274` 的 reconciliation，只从一致性备份中恢复该行被修改的状态字段，并以当前错误码和任务身份作为 CAS 前提；不得直接覆盖整个生产数据库。
- lane 恢复后，如果后续项目已经提交或产生新的 n8n execution，不得直接还原批次文件或整库快照，以免重复提交。应立即停止唯一 lane、保留现场并制定前向恢复方案。
- 所有回滚都保留新 release、日志和受控备份作为证据，不删除媒体、索引、任务状态或模型资产。

## 已知风险

- 24 集批次仍在生产执行，当前证据只证明首项已进入正式音画链，不表示全部 24 项完成。全局 lane 必须继续保持单 worker，不得为提速增加并行 worker。
- `900` 秒只覆盖“尚未建立任何媒体阶段”的启动租约，不是长视频执行总超时。后续若工作流在高负载下可能合法地等待超过 900 秒才创建首个媒体子任务，需要基于真实启动延迟指标调整，而不是直接扩大总任务超时。
- supervisor 安装器当前仍有少量 validator 调用依赖 PATH 中的 `node`，而 LaunchAgent 使用显式物理 Node。当前两者均为 `22.22.3`，不影响本次恢复；未来升级时必须统一到同一显式 Node 并增加 PATH/target 不同的回归测试。
- 本轮没有为验收重提 row `274` 或额外创建重复任务。后续状态以正式任务 API、批次持久状态和 n8n execution 为准，不能从旧日志或聊天记录推断。

## 下一步

- 按正式 batch/task 状态继续观察 execution `68` 和后续 23 项；不主动并行、不重提、不把阶段性运行状态误报为整批完成。
- 后续增加 pre-media 启动耗时、reconciliation 次数与收敛结果指标，为 900 秒租约提供容量和延迟证据。
- 将 Node 大版本升级作为独立任务：先建立受支持版本矩阵并统一 supervisor 的显式 Node 调用，再侧装、验证和切换。
