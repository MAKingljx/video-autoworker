# 2026-09-09 视频学习排队与导演脑审核反馈修复

## 需求与范围

用户反馈视频在前夜入队后，次日上午仍显示“已排队”，询问进度时只得到“完成后再查询”；导演脑作品候选提交后，回复“批准”却显示暂时无法读取。要求查明原因并完成优化部署。业务状态、诊断和恢复能力继续放在 AI-worker 的平台适配层，不修改 OpenClaw 基础代码。

## 生产诊断事实

- 默认生产节点为第二台 Mac Studio。检查覆盖实际监听进程、运行目录、安装合同、应用发布版本和数据库打开文件身份。
- 稳定路由和应用运行正常，任务入口接受新任务；视频执行 lane 仍处于维护状态，有一个存活的 guardian 持续续期启动锁，lane 未加载且没有视频工作进程。
- 本次原始视频任务已持久化，尚无平台执行记录，状态从提交后一直没有推进。因此不是“只在完成时读取进度”，而是没有真正开始执行。
- 提交接口忽略工作进程启动失败的返回值，查询接口没有执行服务可用性字段，造成模型把维护阻塞解释成正常排队。
- 初次使用与 Qwen Gateway 相同的服务环境，在独立 Aqua 只读任务中验证导演脑：11 张表结构校验通过；对应作品候选确实存在，当时仍为草稿且未审核。普通搜索默认只读取审核通过的记录，查草稿须明确指定状态。初次检查没有修改任何候选或审核状态，临时检查任务已卸载；用户随后明确批准后的正式审核见后文。
- 聊天工具没有批准或驳回动作。保留会话的工具参数和结果已脱敏，无法据此还原截图当时的精确异常；SSH 环境的凭据访问结果不能替代真实 Gateway 服务环境的验证。

## 修复决策

1. 分开返回任务状态、执行服务可用性和已观测到的处理阶段。维护阻塞必须如实呈现；不能推断队列前面有其他任务，也不能编造完成百分比或启动时间。
2. 提交和查询共用同一套只读状态识别。受控 guardian 不允许被普通提交请求自动删除；锁身份不完整时返回“尚未确认”。
3. 导演脑审核请求给出明确能力说明，不再诱导用户通过没有实现的聊天动作批准候选；不自动转而执行 shell 或搜索来模拟审核。
4. 在适配器日志中保留白名单错误码及动作名称，拒绝记录请求正文、标识、响应正文、凭据或堆栈。
5. 共享组件安装和维护恢复分别验证当前应用、控制层、工作流与插件来源，允许各组件有独立版本。恢复原任务时保留原始任务和幂等标识、历史隔离收据与回滚资料。

## 候选实现与验证

此节所述源码属于本地候选，生产部署事实以本文件后续验收记录为准。

- 导演脑增加只读审核能力说明，显式审核指令的短路处理，以及不含业务内容的错误诊断。完整插件 265 项测试通过，包含凭据缺失不误判为候选内容错误的回归用例。
- 视频插件 137 项、任务流 119 项测试通过；共享安装门禁、当前版本恢复基线与历史 guardian 交接共 86 项测试通过，覆盖原任务不变、拒绝漂移和多处进程中断恢复。
- 任务流和导演脑安装器分别通过安装后重复执行的验证。三个安装器均携带同一队列摘要，安装前、取得锁后和发布成功收据前检查，防止任务内容在维护期间漂移。
- 类型检查通过；Lint 无错误，保留 12 条未受本次改动影响的历史警告；源码扫描无敏感信息发现。完整 CI 和生产验收尚待执行。

新增维护恢复入口为 `scripts/verify-guardian-resume-baseline.mjs` 与现有 guard 的 `resume-guardian` 子命令。恢复基线分开绑定应用版本、历史管理器版本和新共享组件源码；逐文件核对已安装的任务流、视频插件与导演脑插件。原维护收据和隔离树不变，新恢复 attempt 独立保存 baseline、intent、admission、receipt 和提交标记。

首轮 GitHub CI 的根测试分区有 2259 项通过、1 项失败：旧任务查询断言缺少新契约的 `progress` 字段。补齐后该 CLI 文件 37 项测试通过。本地安装器测试另外发现已终止子进程的 zombie 回收竞争；断言改为检查进程消失或已停止执行，持续执行的进程仍判失败，定向重复 5 次通过。发布前同步提升视频插件与导演脑版本，避免把内容变更误判成当前版本的无操作安装。

现场 Qwen 配置已使用 OpenClaw 9.2 的 `agents.entries`。两个安装器改为复用既有 `openclaw-agent-config.mjs`，兼容旧 `agents.list` 并保留输入布局及工具权限；导演脑 entries 安装、重复安装和回滚均通过。恢复流程额外覆盖启用后尚未启动的中间态：仅对已有同一恢复 intent、精确且已退出的维护 owner、无工作进程和全局执行锁的状态续接启动，首次恢复条件保持不变。启用后被终止与首次启动失败两项回归通过。

候选 `0cd0331` 在真实 Qwen Gateway 的服务环境中完成三个安装器 dry-run，原排队任务 SHA-256 保持不变，临时预检 LaunchAgent 已卸载。后续 CI 在导演脑安装器 85 项中仅有一项旧错误名称断言失败：共享解析器已正确拒绝重复代理 ID，配置与插件均未修改。修正该测试预期后，四项代理配置拒绝用例均通过。运行源码与依赖未变，按 `SAFE-EFFICIENT-EXECUTION-001` 复用已通过的专项测试、类型检查、源码扫描和生产预检，完整 CI 继续独立执行；不得把这次失败的 CI 记为成功，也不因测试断言提交重复执行未受影响的生产预检。

`a392604` 的完整 Quality Gate（run `34313010028`）最终通过全部 21 个步骤。生产暂停入口及数据库在线备份校验完成，任务流、导演脑安装成功。视频安装先遭遇远端经本机转发访问 GitHub 超时，改为已验证的远端直连后通过真实主线检查；随后官方 CLI 要求显式接受插件能力，旧安装器缺少 `--accept-capabilities`，安装和补偿调用均失败。现场核对视频仍为旧版本、配置与备份完全一致、三个 Gateway 监听进程未变，入口继续暂停。隔离环境使用同一官方 CLI 复现原错误，并验证增加该参数后安装成功；只采用正式能力确认接口，不修改 OpenClaw 核心或关闭安装策略。

安装器同时补充失败阶段、退出码及私有日志保留，回滚失败时保留恢复 journal；公开输出只给阶段和证据路径，原始日志仍留在权限受控的运行目录。测试桩必须检查真实所需的能力确认参数，不能继续无条件接受任意安装命令。

该增量修复已通过正常升级/回滚、安装失败后补偿成功、补偿失败后恢复原版本再续装和静态合同的定向回归；安装器语法、修改文件 ESLint 与源码扫描通过。官方 CLI 隔离验证同时覆盖新版本安装和从实际备份恢复旧版本。插件与业务代码、依赖均未变化，复用上一提交的完整 CI，不重复安装已成功的任务流和导演脑。

用户要求主动使用 Clash 并沉淀规则。已更新飞书全局 `GITHUB-CLI-PROXY-001` 至 1.3，完成精确回读、规则快照、37 项相关测试、提交推送及最终版本核对（连接器提交 `dcefaad`）。规则要求检查实际执行节点的可用直连/Clash，按需使用进程级代理并区分故障链路，无需用户再次提醒；本机端口不能直接套用远端。此次本机 Clash 的 Git HTTP/1.1 命令级推送已验证，远端安装使用已验证直连，未修改全局网络或订阅。

## 部署与回滚要求

后续安装通过官方能力确认，但重启后校验和补偿校验均失败，恢复 journal 按设计保留。随后在相同 Aqua 服务环境逐项复查，旧视频版本的 RPC、运行时插件检查、严格验证器和工具目录均通过；配置字节及插件文件与备份完全一致，其余受保护监听正常。由于原失败阶段的 JSON 未被旧流程保留，不能断言当时具体哪项检查失败，也不能把暂态就绪推测当成已证实原因。

为避免重启后单次探测误判，同时保留严格门禁，仅在重启后的新版本及恢复版本检查中增加最多 8 次、间隔 2 秒的就绪重试；预检保持单次严格检查，不重复安装或重启。每次仅输出检查阶段、次数与退出码，最终失败在私有证据目录保存白名单 JSON、stderr 和就绪记录。新增回归分别验证短暂 RPC 未就绪后成功及持续失败后的正确补偿，连同两个静态合同共 4 项通过；此前正常升级、显式回滚和两类补偿恢复用例通过。语法、ESLint 与 diff 检查通过。此增量不改插件载荷或依赖，复用已通过的完整 CI 和专项业务测试。

部署前保存受限备份和当前组件、任务、进程身份；暂停新任务入口后安装经验证的 canonical 源码。安装过程中保持原有 guardian 和队列内容不变。随后通过有收据的正式交接恢复唯一视频工作进程，确认原任务出现真实阶段推进，再恢复入口。

不得重复提交任务、恢复旧隔离素材、删除数据库或索引、自动批准导演脑候选，也不得重放历史一次性部署授权。失败时保留维护状态和诊断证据，通过对应安装器回滚受影响组件。

## 生产安装、审核与原任务恢复

`50d0932` 的真实大数据库恢复基线及 guardian 交接已成功，唯一执行器恢复、旧 guardian 退出，入口按版本核对从暂停恢复。原任务首次实际执行时在平台受理前被作品有效性校验拒绝。复查原始备份发现作品绑定位于 durable state 顶层，原任务一直绑定目标作品；最初只检查 item 层而判断未绑定不准确，此处明确更正。对应作品仍为草稿，不能把本次失败误报为视频处理中。

用户随后明确确认审核通过并继续原任务。完成精确项目规则复核、唯一候选检查和私有备份后，正式 review 接口按版本从 v0.2.0 更新至 v0.2.1，状态生效、reviewed=true，作品解析读回同一对象；只审核作品登记。随后在 canonical batch lock 内核对原失败状态、唯一 item、未提交、无交接 journal、平台无该任务及全部 519 条历史记录不变；通过原子状态写入恢复同一任务，保留 taskId、幂等键、来源身份、请求摘要和顶层作品绑定。入口短暂暂停并在仍持任务锁时按版本恢复，现役执行器保持。原任务于 14:44:32 被平台受理并进入 running，attempt=1；没有重复创建新任务。

真实 running 验收发现状态时间转换把平台 Unix 秒当成毫秒。修复仅影响 `submit-task.mjs` 的纯时间转换函数，支持秒、毫秒与原有文本时间，并对越界值安全拒绝；实际秒值、毫秒值及原有状态用例共 6 项通过，语法/ESLint/diff 检查通过。完整组件安装会替换执行器树并要求无运行任务，当前视频应继续执行，因此此处仅安排范围明确的查询脚本更新：限定函数以外字节完全相同、旧/新 SHA 与 canonical 来源一致、整棵运行载荷除该文件无差异、权限/所有者/单链接与备份有效、原子替换和完整载荷读回通过，并核对原任务及执行器未被改变。该操作不代表运行中的整组件安装门禁通过；后续空闲维护可用正常安装器补做组件级 no-op 收据。

视频安装器 `afa37f8` 已在生产完成，任务流、导演脑与视频三项均有成功收据；视频版本为 0.5.15、导演脑为 0.4.1。恢复旧 journal 和安装新版本的首次 Gateway 探测均暂未就绪，第二次全部严格检查通过。原任务文件摘要在整个安装期间不变；实际安装工具回复明确呈现维护阻塞及状态更新时间，审核能力说明与预期一致。最终安装通过实际远端 Clash 的进程代理完成，未改系统网络配置。

此前首轮 Guardian 基线创建与实时核对通过，恢复 intent 已记录，但在交接之前的 successor 校验误用 16 MiB JSON 上限检查真实数据库，因数据库较大拒绝继续；该次失败时旧 guardian 仍存活，lane 未加载，入口暂停，原任务未变。修复将数据库校验独立为 64 GiB 上限，保留物理路径、单链接、所有者、权限和 SQLite 完整性检查；JSON/收据继续限制 16 MiB。实际大于 16 MiB 的 SQLite 回归通过，并验证不安全权限和硬链接仍被拒绝，专项 5 项及语法/ESLint/diff 检查通过。随后已使用新源码和独立基线成功恢复，保留原失败 intent，没有重装载荷未变的已成功组件。

历史维护恢复脚本将当前进程绑定到旧维护时的 PID，并假定各组件同一版本，与已完成的分组件升级不相容。本次增加窄化且可验证的恢复流程，不放宽原历史收据的保护规则。最终验收须区分“修复已部署”“原任务已开始”和“视频已完成”，不能混为一项结果。

## 最终验收（15:05）

查询时间修复 `ff65ba6` 已于 15:05:22 在实际任务流路径完成。旧脚本以 0400 权限备份并完成文件及目录 fsync；新旧源码限定为同一时间转换函数变更，发布锁内原子替换，整棵安装文件清单与 canonical 源码逐文件一致。新入口短暂按版本暂停后恢复，最终 active、accepting=true、revision=14。七个受保护监听 PID、全局执行锁及原任务受理/开始时间、幂等身份和 attempt=1 均不变，没有重启正在处理视频的执行器。这是独立查询文件更新，未运行整组件安装流程；备份、意图与完成收据保存在私有运行目录。

15:05:50 使用实际 Qwen 服务环境和已安装工具验收通过。原任务为 running，执行服务 available / worker_active，唯一 worker 与正式交接一致；查询实答为“任务正在处理中。处理服务已运行，任务状态以当前登记为准。当前阶段：处理中；状态更新时间：09月09日14:44。”原任务创建时间、来源身份及作品绑定保持不变。该时间是平台记录的最近状态变更时间，不能解释为每帧处理时间或精确完成百分比。

历史数据库备份与当前库逐行全字段核对，原有 519 条记录全部不变；当前共 522 条，原任务平台执行记录仅 1 条，另有处理中和已成功的链内执行记录。原视频仍在处理，尚无最终完成时间；验收没有对外发送消息。查询修复和验收的一次性 LaunchAgent 均已卸载，备份及失败诊断保留，常驻 lane 继续运行。

最终运行源码的相关时间转换回归 6 项通过，受影响安装与恢复回归按前述记录通过，源码敏感信息扫描 1206 文件无发现。完整成功基线为 `a392604` 的 Quality Gate；后续增量单独验证，`ff65ba6` 的完整 CI 在收尾核对时仍处于 Unit tests、无失败步骤，不将其记为成功。

## 已知限制

中断恢复覆盖控制进程退出与尚未创建执行进程的启动失败。若已写入 admission 的执行器自身退出、被 launchd 换成另一 PID，恢复流程会拒绝把原授权转给新进程，须保留现场重新核验；本次不自动改写不可变 admission，也不声称覆盖所有执行器崩溃场景。

聊天端新增的是明确的审核能力说明，尚未实现直接批准/驳回；本次作品审核由用户确认后通过正式人工审核接口完成。单任务失败回复仍使用概括提示，部分前置校验的详细原因需要后台查看。本次没有伪装成已实现所有聊天审核和诊断能力。
