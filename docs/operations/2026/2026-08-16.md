# 2026-08-16 cpolar 重连与视频学习运行时复验

## 范围

用户提供新的 cpolar SSH 入口后，更新默认生产节点 `heisenbergs-1` 的连接记录，并进行不触及运行数据的服务健康检查。

## 连接验证

- 新入口为 `6.tcp.cpolar.cn:10626`，仅以既有 `heisenbergs-1-cpolar` ED25519 信任记录和严格主机校验连接。
- 远端身份复验为 `HEISENBERGS-1deMac-Studio.local / heisenbergs-1`。本机 SSH 别名和 AI-worker 运维文档已同步为该入口。
- 实际生产仓库为 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`，origin 为 `MAKingljx/video-autoworker`；检查时工作树干净，HEAD 为 `758d9c581cd6ab416be69b4cdac97ba5d032fd7d`。

## 运行时证据

- 控制台监听 `127.0.0.1:3017`，对 `/api/health` 的 HTTP 请求返回 `200`。该端点仅按可达性记录，不视作结构化健康结果。
- n8n LaunchAgent 已加载，`127.0.0.1:5678/healthz` 返回 `200`；任务代理监听 `127.0.0.1:5679`。
- `qwen-current` Gateway 运行于 loopback `127.0.0.1:18889`，版本与 CLI 均为 `2026.7.1-2`，深度连通性探测返回 `ok`。

## 边界

本次只进行连接和健康验证。没有读取或修改视频、队列、任务数据库、n8n execution、媒体、日志或凭据，也没有重启生产服务。此前已入队视频的进度仍须由用户在新消息中提供完整 task ID，并经正式只读状态接口一次查询；不得由运行文件或数据库推断。

## second-original 直接会话测试

用户要求以“《地球之极》第三季第三集正式入队”测试第二个 OpenClaw 能否找到已入队任务。该短句不含完整 task ID，因而不具备正式状态查询条件。

- 通过 `qwen-current` 的本地 Gateway 向 `second-original` 进行一次不投递的直接会话。会话完成 Qwen 推理，但 CLI 未返回可验证文本答复，且没有产生任务状态。
- 该直接 Gateway 调用没有代表 Telegram 私聊入站，未覆盖 `before_dispatch` 的正式状态路由，不能作为正式查询验收。
- 测试没有读取或修改视频、任务、队列、SQLite、n8n execution 或媒体，也没有创建、重提或外发任何任务。

正式状态查询仍须由授权用户在新的 Telegram 私聊消息中携带完整 task ID。不得用标题、历史会话或运行文件代替该标识，也不得伪造 Telegram 身份补测。

## 0.5.1 状态搜索候选

### 目标与访问边界

`0.5.1` 为 `second-original` 的原生 `before_dispatch` 增加 `status_search`：已授权 Telegram
私聊可按当前消息中的视频标题、文件名、季集或关键词查询受控视频任务登记。搜索词不要求
完整 taskId，因此可用于功能展示；完整 ID 仍是精确查询入口。

当前发布只支持一个已配置的 Telegram 发送者。该授权门在搜索前生效，因此功能不是跨用户或
通用资料库搜索接口。搜索只匹配公开 task/batch 标识、显示文件名、季集别名和状态元数据；
它不匹配或返回提示词、源路径、SQLite、n8n execution、媒体、聊天历史、记忆、进程或凭据。
未来增加多个授权用户前，必须先新增持久化的任务所有者范围和受控索引；不得将本候选扩展为
跨用户全库盲搜。

### 行为与验证

- classifier 只输出严格的 `status_search` 语义建议；宿主仍验证 Telegram 私聊、
  `second-original` 会话、发送者哈希、时间戳和当前原消息中的逐字查询值。
- 搜索零命中返回简短未找到；唯一命中才调用一次正式 task/batch 状态客户端；多命中仅返回
  最多五个有界候选，不自动选择、轮询、重提或恢复任务。
- 新增 `0.5.0 -> 0.5.1` 专用升级器，apply 仅刷新 `qwen-current` 插件载荷和 gateway；
  rollback 先验证当前版本为 `0.5.1`，再恢复精确 `0.5.0`。
- 本地 Node `22.22.3` 证据：插件 Vitest `24` 文件、`312/312` 通过；任务流定向 Vitest
  `26/26` 通过；TypeScript、Node/Bash 语法和 `git diff --check` 通过。

本节记录本地候选，不声称 GitHub、生产安装或真实 Telegram 网络入口已经完成。候选验证未创建
任务、队列、n8n execution、媒体或 SQLite 记录。

## 0.5.1 生产部署与只读验证

canonical `main` 提交 `fd770314dacc67fe5c026e249b179f4603ff8d50` 已推送并在
`heisenbergs-1` 的实际运行仓库 fast-forward。远端 origin、`main`、HEAD 和工作树在
部署前后均复验一致且干净。

发布先通过 task-flow、video-lane 和插件升级器的 dry-run。task-flow 随后以其受控恢复点
安装；lane 复验后确认现有 LaunchAgent 已是当前且健康；`0.5.0 -> 0.5.1` 升级器才 apply，
只刷新 `qwen-current` 插件载荷与 gateway。升级器的运行时、配置和受保护监听校验通过，声明
配置、队列、n8n、媒体和数据库未变化。

安装后确认：插件为 `0.5.1`、已加载、只注册 `before_dispatch` 且无工具；已安装 task-flow
可执行载荷与 canonical 一致；持久 video lane LaunchAgent 正常；n8n `/healthz` 为 `ok`，
控制台 `/api/health` 返回 HTTP `200`（仅表示可达性）。

以用户指定的《地球之极》第三季第三集标题运行一次已安装的只读 `--search-status`：结果唯一，
再按匹配项调用一次正式状态客户端，得到 `succeeded`。该验证没有创建、取消、重提或恢复任务，
也没有读取 SQLite、n8n execution 或媒体。

此为生产 installed/CLI 验证，不是 Telegram 网络入站验证，也不证明一次真实 Qwen 分类或消息
投递。真实入口仍须由已授权用户在新的 Telegram 私聊中发送标题/关键词查询；不能伪造该身份。

## 0.5.2 有界状态读取候选

生产已安装 handler 的 synthetic 验证发现一项边界缺陷：标题搜索唯一命中后，runner 调用完整
`--status`，而成功任务的原始输出超过插件子进程固定 `64 KiB` 输出上限，handler 因而安全返回
不可用。相同任务的受控 task-flow CLI 搜索和正式状态读取成功，说明根因不在任务、搜索、授权或
n8n。

候选 `0.5.2` 新增 `--status-brief`。只有插件内部的唯一命中后状态读取使用该变体；它仅输出
净化且最多 160 字的 summary，不传递大任务 payload。原有 `--status` 完整运维 CLI 保持不变。
版本配套新增精确 `0.5.1 -> 0.5.2` 升级器，原 `0.5.0 -> 0.5.1` 升级器继续保留，形成可审计的
分段回滚链。

本地候选验证：相关插件测试 `28/28`、task-flow 测试 `26/26`，并覆盖超过 100 KiB 的附加输出
不会进入 brief 回包；Node/Bash 语法和 diff 检查通过。本节不表示 `0.5.2` 已提交、推送或部署。

## 0.5.2 生产修复与 handler 验证

canonical `main` 提交 `c1e988cbaa9279a96ce69b3e055ec59eca2b7bc9` 已推送并在实际运行仓库
fast-forward。发布顺序为 task-flow dry-run/apply、video-lane dry-run、`0.5.1 -> 0.5.2`
插件升级器 dry-run/apply；lane 已是当前健康版本，因此没有额外重载。插件升级只刷新
`qwen-current` 载荷与 gateway，并经恢复点、配置和受保护监听检查确认队列、n8n、媒体与
数据库未变。

安装后插件为 `0.5.2`、已加载、唯一 `before_dispatch`、零工具；已安装 task-flow 可执行载荷
与 canonical 一致，持久 lane 运行，n8n `/healthz` 为 `ok`，控制台 `/api/health` 返回 HTTP
`200`（只表示可达性）。用户指定标题仍唯一命中。

随后对已安装 handler 发起受控 synthetic Telegram 私聊：使用真实 runner 和该标题的唯一搜索
结果，但固定 `status_search` 分类值且不投递 Telegram。handler 返回“任务已完成”，摘要保持有界，
证明 `--status-brief` 已覆盖先前的 `maxBuffer` 故障。此验证不是真实 Qwen 分类或 Telegram
网络入站验收；真实入口仍须由已授权用户从新的 Telegram 私聊发起，不能伪造身份。

## qwen-current 直连 Agent 复验

按用户要求，通过已验证的 `heisenbergs-1` SSH 入口向 `qwen-current / second-original` 发起一次
独立、无投递的 OpenClaw Agent 查询会话；未指定 Telegram、WhatsApp 或其他外部回复目标。

- Gateway 深度探针为 `ok`。独立会话完成上下文编译并提交提示，但没有产生工具调用或可验证文本答复，约
  180 秒后以 `timeout` 结束；未创建、取消、重排或查询任务状态。
- 同一 Gateway 的最小模型探针在约 3.4 秒内返回“好”，因此本次不是 Gateway 或 Qwen 模型服务不可用。
  直连 Agent 没有进入 Telegram `before_dispatch` 状态钩子，而是进入普通 Agent 推理路径；该路径本轮未
  收敛，不能证明标题搜索或正式状态接口可用。
- 结论：本次仅证明远端连接和基础模型调用健康，不是 Telegram 网络入口验收，也不能把超时解释为“任务不存在”。
  正式查询仍需新的授权 Telegram 私聊入站，或另行设计不依赖普通 Agent 推理的受控直连状态接口。

本轮没有修改生产代码、配置、队列、n8n、媒体或数据库，也没有重启服务；该记录不包含会话键、任务 ID、路径、
原始日志或凭据。

## 0.5.3 直接任务链工具候选

### 目标与决策

用户要求 `second-original` 能在普通对话中直接调用视频任务链，不再要求 `/video-status` 或由插件维护
发送者白名单；同时保留目录自动识别、队列入队和标题/关键词进度查询。实现保留现有
`aiworker_analyze_video` 名称，扩展为 `submit_video`、`submit_directory` 和 `status` 三种结构化动作。
旧 `allowedSenderSha256` 仅保留在 manifest 兼容旧配置，运行代码不再把它作为工具权限门槛。状态查询仍只读
受控任务登记字段，唯一命中才读一次正式状态，多命中只返回有界候选，不做跨用户全库盲搜。

### 本地候选变更与验证

- 插件版本升为 `0.5.3`，新增直接任务链工具和受控升级器；`before_dispatch` 兼容入口与直接工具共用同一
  持久化全局串行 video lane。
- task-flow skill、workspace rules、workspace memory 已改为指导模型调用工具，不要求 slash 命令或手工 shell；
  目录动作交给任务链扫描支持的视频，提交后只回一条回执，不轮询、不重提。
- 真实本地定向证据：插件工具/manifest/Qwen hook/runtime 测试通过（38 项），task-flow 与事务安装器测试
  `41/41` 通过；Node/Bash 语法与 `git diff --check` 通过。
- 历史 `status-upgrade-installer` 隔离场景会启动多套临时 Gateway，按用户要求已终止，未将其作为验收证据。

### 发布边界

本节写入时，`0.5.3` 尚未提交、推送或部署，远端仍为 `0.5.2`；尚未复制视频、创建测试任务或修改生产
队列、n8n execution、媒体和数据库。下一步先完成 canonical Git 发布、远端插件与 task-flow 部署，再按
用户确认的受控测试目录复制少量《地球之极》样本，使用真实 `second-original` 工具验证单视频入队、目录
批次入队和标题状态查询。

### 首次升级器回滚与修正

canonical `main` 提交 `144cda8` 后，task-flow 安装完成，`0.5.2 -> 0.5.3` 升级器也确认候选插件的
runtime 与 `second-original` catalog 均存在 `aiworker_analyze_video`。随后升级器把重启后的
`qwen-current` PID 与重启前 PID 作逐字比较，错误判定受保护监听漂移并自动恢复 `0.5.2`。这不是工具
注册失败；Gateway 已恢复健康，队列、n8n、媒体和数据库未变。修复将只检查每个受保护端口是否持续存在
监听者，并新增不启动 Gateway 的静态回归测试；修正提交和再次部署完成前，不把 `0.5.3` 标为生产已部署。

PID 修正后的第二次 apply 通过了候选 runtime/catalog 校验，但 retention 代码对上次回滚留下、尚未拥有
`.verified` 标记的目录直接 `statSync`，导致清理步骤抛错。升级器以明确备份路径成功恢复精确 `0.5.2`；
未创建视频任务或修改队列、n8n、媒体、数据库。后续修正把未验证目录视为非保留候选而跳过，并在再次 apply
前保留同一 rollback 入口。

## 0.5.3 direct tool effective-access gate

### Implementation before production apply

只读生产检查确认 `aiworker_analyze_video` 已在 `qwen-current` 插件 runtime/catalog 注册，但
`second-original` 的实际 profile 为 `coding`，有效工具集合仍未包含该工具；普通 Agent 因而可能改走
`exec`，不能把“已注册”冒充“可调用”。本次新增受控 `enable-aiworker-video-command-direct-tool.sh`，
只向 `agents.list[id=second-original].tools.alsoAllow` 写入唯一的 `aiworker_analyze_video`，保留原有
`tools.allow` 原样，并拒绝全局或其他 agent 继承该工具。

脚本在目标主机、OpenClaw 版本、canonical Git SHA、插件 runtime、受保护监听和真实 Gateway
`tools.effective` 上设置 dry-run/apply/rollback 门；备份仅保留最近两个已验证恢复点。apply 只改变
`qwen-current` 配置并重启其 Gateway，不创建队列、n8n execution、媒体或数据库记录。此次本地静态
测试 2/2、Bash/Node 语法和 `git diff --check` 通过；不使用会启动假 Gateway 的隔离场景。

### Real-video test boundary

本次真实视频验证尚未复制素材或创建任务。待当前确认后，计划在生产机受控测试目录生成《地球之极》第三季
第 3、7、11 集的 APFS 克隆副本（逻辑总量约 8.6 GB），由真实 `second-original` 的
`submit_directory` 入队；该批次会创建 3 个真实任务，进入持久化全局串行 video lane，并可能触发实际
n8n/Whisper/Qwen 学习链。回滚只撤销 direct-tool 配置并保留可审计任务记录；不自动删除媒体、队列或数据库
记录，避免把已受理的真实任务当作可逆临时数据。
