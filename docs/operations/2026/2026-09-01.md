# 2026-09-01 生产遗留媒体修复准备

## 授权范围

- 已授权：恢复 2026-08-31 rename 前中断的 runtime intent；重新暂停 video lane；以同卷 rename 可恢复隔离唯一遗留媒体工作目录；生成并校验本次权威修复备份。
- 未授权：执行数据库四字段 apply。
- 受保护服务：3017、n8n、Gateway、模型服务不得修改或重启；任务不得重提；数据库保持只读。
- 备份保留：同一修复家族最多保留最近两份验证有效的恢复点。

## 本地候选加固

- 兼容 macOS `launchctl print-disabled` 的 `enabled/disabled` 与历史 `true/false` 两种输出。
- runtime guard 只允许一个已知旧工具 SHA 恢复 rename 前中断 intent；receipt/status 不接受旧工具摘要。
- 权威 Mission Control 与 n8n SQLite 采用来源进程数字 FD、guard verifier FD、better-sqlite3 新连接数字 FD 三层 path/device/inode 绑定，并在查询后和 close 前复核。
- receipt anchor 与 restore anchor 分别作为正向隔离和恢复的唯一提交点；rename 后普通失败不再自动反向移动或启 lane。
- 新增 `--hold-guardian yes`：输出 `prepared-held` 后保持同一 guardian 进程、inode 与 token 每 5 秒刷新，持续覆盖 orphan dry-run 和备份 prepare。
- guardian 正文只在创建时写入并 fsync，后续只用 `futimes` 刷新同一 inode 的 mtime；实际 holder 由独立 owner sidecar 标识，接管通过同卷私有 attempt 目录原子替换并同步两个父目录。marker、owner 或 pending 任一步被 SIGKILL 后都只能按不可变 intent/receipt 精确收敛，不覆盖唯一 marker，也不在 batch root 遗留临时成员。
- restore 改为不可变 restore intent 驱动的追加式续作；在反向 rename、guardian 交接、lane active、receipt 与 anchor 各阶段被 SIGKILL 后均可幂等收敛。

## 验证与生产事实

- Node `22.22.3`、pnpm `10.33.0` 下，runtime guard、hold、recovery、orphan reconcile 与 freeze evidence 五个相关测试文件共 `87/87` 通过；TypeScript、三个脚本的 Node 语法、目标 ESLint、全仓 ESLint 和 `git diff --check` 通过。全仓 ESLint 只有既有的 12 条 warning、无 error。
- 加固提交 `03ee3432c12b9d77a3bb86a684cbe39fce64364d` 已快进推送到 GitHub `main`，生产源码 checkout 随后从已核验基线纯快进到同一提交且保持干净。3017 继续运行原 `542eebd-runtime`，没有构建或切换应用 release。
- 写入前从 3017/5678 真实数字 FD 绑定权威 Mission Control 与 n8n SQLite；两库 `quick_check=ok`，活跃媒体节点 `1`、n8n active execution `0`、正式队列 `waiting=0 / running=0 / attention=1`。七个受保护 listener 的 PID 前后完全一致。
- 唯一旧版 intent 精确命中受控 predecessor SHA、rename 尚未发生、源目录存在且目标不存在、无 receipt/anchor、旧 owner 已死亡。新工具以 `recovered-before-rename` 收敛旧现场后，在独立持久会话中立即执行 `prepare --hold-guardian yes`；新 receipt/anchor、隔离目录 inode/树摘要与持续 mtime 刷新均经第二会话 `status` 验证为 `prepared-held`。
- video lane 最终保持 disabled、unloaded、worker-free；遗留媒体工作目录只做同卷原子 rename，现位于私有可恢复隔离区，没有删除媒体。guardian holder 继续存活并阻止 detached worker 绕过暂停。
- orphan dry-run 返回 `eligible=true / prepareRequired=true`。随后只进入备份 prepare 阶段，没有执行数据库 `--apply`；prepare 产物必须经独立复核后才可计作恢复点。
- 首次 prepare 的独立复核发现权威快照仍处于 WAL 模式，并产生了 manifest 未声明的快照 sidecar；该目录从未作为有效恢复点使用，随后在不打开其中 SQLite、不修改目录内容的前提下整目录移动到专用无效备份隔离根，保留不可变 intent、receipt 和完整树摘要供追溯，没有删除。
- 根因修复提交 `671155dacd119521fa6de3d15497b962ffb4b8d5` 已推送 GitHub `main` 并纯快进到生产源码 checkout：权威快照显式归一化为 DELETE journal，prepare 目录严格限制为六个普通文件，以 `.pending-*` 完整封存后通过 no-replace rename 原子发布，并覆盖中断恢复。对账定向测试 `36/36`、关联测试 `80/80` 以及 TypeScript、ESLint、Node 语法和 diff 检查通过；3017 运行 release 未切换。
- 修复后使用无 `apply` 能力的固定包装器重新执行同参数 dry-run 和一次 prepare；包装器在 prepare 内再次 dry-run，并独立校验目录 `0500`、六文件 `0400`/单硬链接、manifest 精确四成员及角色、成员 SHA、目标绑定、无 sidecar、`journal_mode=delete` 与 `quick_check=ok`。当前活跃备份家族为一份既有 `.verified` 恢复点加一份新 schema 恢复点，最终只读审计为 `2 verified / 0 incomplete / 0 invalid`；隔离的无效目录不计入有效家族且继续保留。
- 最终复验确认两库仍 `quick_check=ok`，活跃媒体节点仍为 `1`，n8n active 仍为 `0`，正式队列仍为 `0 / 0 / 1`；`/materials`、`/tasks`、`/api/status` 与 n8n health 均为 HTTP `200`。3017、n8n、Task Broker、模型与三个 Gateway 均未重启或修改，任务未重提。
- 本轮没有执行数据库 `--apply`，目标子记录保持原状态。prepare token 不进入 Git、日志摘要或长期记录；任何后续四字段 CAS 修复仍须重新取得适用且未过期的 prepare，并获得针对当次生产数据写入的明确确认。
- heartbeat `3017` 已改为只读监控 `prepared-held`、holder、备份和运行身份；明确禁止自动 `apply`、重新 prepare、recover/restore、启用 lane 或推进 n8n/3017/导演脑发布。发现漂移时只脱敏通知并停止。
- 所有业务 ID、PID、数据库路径、备份路径、token、nonce、manifest 内容和原始日志均不进入 Git。
