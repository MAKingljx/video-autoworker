# 2026-08-07

## 分钟级视频链生产发布

### 发布范围

- 将分钟级视频分析链 v2 从本地候选发布到默认生产节点 `heisenbergs-1`，保持 Whisper 处理语音、本地 Qwen 处理画面、两个工作节点均 `memoryMode=none`。
- canonical repo 的首个发布提交为 `2b8704813fe1688f77ea6fd7e10216ad1b08377a`；本机、GitHub `main` 与生产运行仓库沿同一 Git 历史推进。
- OpenClaw 只通过官方稳定通道从 `2026.7.1` 升级至 `2026.7.1-2`，三套 Gateway 已完成官方修复、重启与连通性检查。

### 发布前证据与备份

- 旧版 65 秒隔离基线任务一次成功：Whisper 使用 `large-v3-turbo`，画面走本地 Qwen 路由，父任务和工作节点均不写入会话记忆。
- 应用、n8n 状态/环境和 OpenClaw 状态/LaunchAgent 均在变更前完成独立备份、SQLite 在线备份校验及校验和验证；备份内容和校验清单留在受控远端，不进入 GitHub。
- 本次最终验收通过后，应用、n8n pre-release、OpenClaw upgrade 三个明确备份家族各保留最新两版，其他备份家族和运行数据不在清理范围。

### n8n 堆内存故障与修正

- 首次离线导入时，n8n `2.31.6` 在加载生产节点注册表并执行工作流列举时达到 Node 默认约 4 GiB 堆上限；SQLite `quick_check` 正常，两个固定工作流 ID 均存在，n8n 服务在修正前保持离线。
- 相同 CLI 在显式 8 GiB 堆下 1 秒完成，确认问题为运行堆上限而非数据库锁、损坏或 SSH 中断。
- 版本化运行配置现为 n8n CLI 和 LaunchAgent 提供可被外部环境覆盖的 `--max-old-space-size=8192` 默认值；同时增加静态回归测试，防止安装模板与运行时默认值再次漂移。

### 当前阶段

- 应用已运行发布候选，OpenClaw 稳定版升级已完成。
- n8n 将从包含堆内存修正的新提交重新生成不可变 release，再完成两个固定工作流的离线导入、发布与健康检查。
- 只有短片证明分钟分段、Whisper、Qwen、无记忆、检查点和清理策略均生效后，才提交 37 分钟整集任务；最终任务 ID、结果和备份保留事实将在本记录追加。
