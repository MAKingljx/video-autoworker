# 2026-08-10

## 媒体保护修复完成生产部署

- 媒体保护修复提交 `fe515ec` 已在 Node `22.22.3` 下完成目标测试、类型检查和隔离生产构建，并以短切换方式部署到生产 `3017` 应用。运行 bundle 明确来自 `fe515ec`；canonical 源码仓库随后继续 fast-forward 到只含技能、测试和文档变更的 `79131aa`，不能把更晚的源码提交误写成当前应用 bundle 的构建来源。
- 本轮只刷新 `3017`，n8n 与 `qwen-current` 进程均未重启。部署前保留了权限受控、带校验清单的完整运行 bundle 和应用数据库在线备份；正常代码回滚不覆盖生产数据库。
- 新版 `prepare` 不再执行跨任务 TTL 扫描，且在创建子任务和读取媒体前强制请求 `videoKey` 与父任务持久化值完全一致。当前任务成功后只消费自己的受控副本，`finalize` 只清理当前 task ID 对应的工作目录。
- 独立媒体保留入口只支持显式 `--dry-run`。生产复验在源码树和媒体根之外的权限受控目录生成 0600 计划，结果为零候选、零保护项、零孤儿审计项和零拒绝项；生产数据库仅做强指纹并由临时快照读取，临时快照无残留。该工具不支持恢复、删除或自动清理。

## 唯一生产视频链验收

- 只调用一次已安装原生插件的 synthetic Telegram DM harness，并创建一条真实 `delivery=none` 视频任务；没有第二次 harness、任务复投或手机回投。
- 父任务及 `prepare`、`audio`、`vision`、`finalize` 共五项全部 `succeeded / attempt=1`。输出中实际检查到的 13 处 `memoryMode` 全部为 `none`。
- Whisper `large-v3-turbo` 正确识别受控中文口令，本地 `local-qwen36-direct` 正确识别纯蓝画面。n8n 仅新增一条 video-analysis execution，generic intake 零新增，终态无活跃执行、收件副本、媒体工作目录、批次锁或 worker 残留。
- 该结果证明已安装原生处理器、确定性参数映射、一次提交和实际生产音画链闭环；入口事件仍为合成私聊形态，不冒充真实 Telegram 网络入站或真实用户 allowlist 验收。

## second-original 说明回复优化

- 视频任务完成并通过健康门后，对 `qwen-current / second-original` 做了一次独立、无通道投递的说明问答。第一次回复虽识别了 n8n、Whisper 和异步提交，但仍混入旧 `VL`/导演脑说法，错误声称完成结果会自动回投 Telegram，答复过长，并调用了一次记忆搜索。问答前后任务、execution、inbox 和 work 均零增量，因此没有误提交。
- 提交 `79131aa` 将当前说明契约写入 canonical skill 与 workspace 规则：解释类问题禁止任何工具调用和任务提交，当前规则覆盖历史视频学习笔记，并固定为当前原生插件与正式视频链，不再使用退休链路名称。
- 生产安装使用独立排他锁和外层 0700 备份，逐文件核对已安装 skill 与 Git 源一致；OpenClaw 的技能 watcher 在全新会话加载新规则，因此没有重启 `qwen-current`、n8n 或 `3017`。
- 安装后仅做一次新的无回投说明复验，零工具调用，且精确返回：

  `原生命令插件接管，aiworker-task-flow 提交 n8n：prepare→Whisper 音频＋本地 Qwen 画面→finalize。memoryMode=none、delivery=none；只回受理，结果另查。`

- 复验前后 Mission Control 任务数、n8n execution、媒体 inbox 和 work 均零增量。该回答准确区分“即时受理回执”和“后续状态查询”，不会把 `delivery=none` 误写为自动回投。

## 最终边界与已知问题

- canonical GitHub `main`、本地源码与生产源码仓库已包含 `79131aa`，生产运行应用 bundle 仍明确对应 `fe515ec`；技能和文档更新不需要重建应用。
- 最终复验时应用与 n8n SQLite 均为 `quick_check=ok`，无活跃 n8n execution；视频插件在 `qwen-current` 为 `loaded`、只注册 `before_dispatch` 且无错误 diagnostics，`aiworker-task-flow` 为 Ready。
- 历史 `accepted / attempt=0` 数据库记录继续保留。此前旧 TTL 热路径高度疑似删除的历史 inbox 文件没有可信恢复源，本轮没有伪造恢复、修改数据库或删除历史记录；该治理异常仍与本轮新 QA 的功能成功分开记录。
- 本轮只删除了精确生成的短 QA 原片目录和隔离构建目录；这些临时测试资产不可恢复。成功审计计划、部署备份和技能备份均保留，未执行广域清理。
