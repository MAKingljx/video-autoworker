# 2026-08-21 `/tasks` 全量待执行队列与耗时口径修正

## 需求与规则快照

- 用户要求解释并修正任务链“总耗时小于 1 秒”的错误观感，同时在 `/tasks` 查看全部已经加入但尚未完成的任务。
- 本轮沿用飞书工作大脑项目 `PROJ-VIDEO-AUTOWORKER` 的开发、Git 与生产部署快照；目标仓库、环境与风险阶段均已覆盖。
- 页面继续使用中性色表面与单一主色，绿色、黄色、红色只表示成功、警告和错误。本轮复用项目现有 React `Button`，Phoenix 组件库没有可直接替换的 React/Next.js 队列表格组件，也不引入不兼容的 Vue/uni-app x 实现。

## 实现与数据口径

### 待执行队列

- 新增只读 `GET /api/n8n/runs?view=queue`，在 viewer 鉴权与 workspace/tenant 隔离后合并两类事实来源：视频批次耐久状态中尚未终结的条目，以及 n8n 顶层 `queued`、`accepted`、`running` 任务。
- 合并时按任务编号去重，只展示 `openclaw`、`video-autoworker` 顶层任务，不把 `n8n-node`、`n8n-media-node` 内部阶段冒充用户任务；接口不返回 input、routing、output、源路径或其他敏感载荷。
- 活跃 n8n 查询不设置静默条数上限，避免超过 2000 项时遗漏队列；媒体子任务时间查询按 500 个任务编号分块，避免大队列触发 SQLite 参数上限。
- 没有耐久队列依据、且超过 24 小时未更新的 n8n 活跃记录标记为“异常滞留”，只做可视化提醒，不自动恢复、取消、重提或修改任务。
- `/tasks` 顶部新增“待执行队列”，显示队列顺序、任务、状态、排队等待、实际处理中时长与批次；无队列时给出明确空状态，表格在窄屏容器内独立横向滚动。

### 耗时

- “总耗时”统一按任务创建到完成计算；未完成任务按创建到当前时间计算，因此视频父任务在最终汇总阶段才写入 `startedAt` 时，不会再把整条任务链误显示为 `< 1 秒`。
- “排队等待”按创建到真实处理开始计算；视频任务的真实处理开始时间取 `prepare`、`audio`、`vision`、`finalize` 媒体子阶段中最早的 `startedAt`。
- “实际处理”按真实处理开始到完成或当前时间计算。缺少可靠处理开始证据时显示 `—`，不伪造耗时。
- 合法的同秒任务仍显示 `< 1 秒`，用于表达时间戳精度内的真实短任务，不再显示错误的 `0 秒`。

## 环境与状态边界

- 本地 canonical 候选为 `/Users/phoenix/Documents/Phoenxi/AI-worker/tmp/video-autoworker-repo`，开发基线为 `a3e5822df15bb7d15041e7ef7fe8a0925d2e0e6a`。
- GitHub canonical 为 `MAKingljx/video-autoworker` 的 `main`；生产目录为 `heisenbergs-1:/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`，生产仓库基线同为 `a3e5822`，3017 发布前运行 bundle 为 `3a878da`。
- 生产耐久视频批次目录当前仅有 17 个状态文件，远低于既有受控索引保护阈值。本轮不修改任务、批次状态、n8n execution、数据库、媒体、索引、模型、Gateway 或 video worker。

## 验证、部署与回滚

- 定向测试覆盖安全队列合并、workspace/tenant 隔离、异常滞留、耐久状态过滤、媒体处理起点、总/排队/处理耗时，以及 2001 条活跃任务不被截断；当前为 `28/28` 通过。
- Node 22 下 TypeScript 通过；lint 为 `0` 错误、`12` 条未改文件中的既有 warning；主项目 `1090/1090`、视频插件 `345/345`、task-flow `7/7` 全部通过；Next.js 16.1.6 生产构建成功生成 `129` 个路由。
- 浏览器验收结果将在同一记录的生产发布小节补齐。
- 本次无数据库 schema 迁移。发布只允许切换并重启 3017 控制台；发布前重新确认没有真实执行中的媒体任务，保留旧运行 bundle 作为回滚点，不联动重启 n8n、Gateway 或 video worker。
- 若新页面或接口回归，停止新 3017、恢复旧 `.next`/release 并重新启动控制台即可；没有生产数据回滚步骤。

## 生产发布与验收

- 功能提交 `5925d53adf927053a3d07b05b93e12c8dce540c5` 已推送 GitHub `main`，生产仓库以 `--ff-only` 对齐后，在独立 worktree `.runtime/releases/5925d53-runtime` 使用 Node `22.22.3`、pnpm `10.33.0` 完成依赖安装和 129 路由生产构建。
- 切换前再次确认 Mission Control SQLite `quick_check=ok`、24 小时内活跃顶层任务 `0`、活跃媒体节点 `0`、n8n 活跃 execution `0`、耐久批次未完成项 `0`。一条 2026-08-07 的顶层 `accepted` 孤儿保持原状，不把它当成真实运行任务，也没有修改、恢复、取消或重提它。
- 候选在 3018 使用生产数据库的一致性快照和真实只读耐久队列完成隔离验收：`/tasks=200`，51 条顶层记录可读，历史孤儿准确返回 `stale=true`；快照数据库 `quick_check=ok`，验证目录随后删除。
- 第一次切换使用 standalone 直接启动时，遗漏了包装器提供的 Mission Control 三个默认数据路径，导致 PID `71241` 临时打开 release 内新建的空隔离库；真实生产库、任务和媒体从未被该进程打开或修改。发现接口为 0 条后立即按真实路径重启同一 release，硬性校验“列表 51 条、滞留 1 条”后才验收；空隔离库及其自动生成凭证已确认无打开句柄并删除。
- 最终 3017 由 PID `72791` 从 `.runtime/releases/5925d53-runtime/.next/standalone` 运行，功能验收时 `.runtime/RUNNING_COMMIT`、生产仓库和 GitHub 功能提交一致。本文后续作为 docs-only 审计提交推送并在生产仓库快进，不重建或重启 3017，因此仓库 HEAD 会前移而 `RUNNING_COMMIT` 继续保持 `5925d53`，两者不得混称。`/tasks`、`/materials`、`/notifications` 均为 HTTP `200`；新日志没有 error、uncaught、unhandled 或 fatal。
- 生产 `view=queue` 返回 1 条记录，统计为等待 `0`、执行中 `0`、需要处理 `1`；该记录显示“异常滞留”、队列位置和 337 小时以上等待时间。队列字段契约完整，未返回内部载荷或路径。
- 51 条历史记录中 50 条已终结；45 条视频具备可追溯媒体处理起点，43 条已完成视频的创建到完成耗时大于 1 秒。唯一仍显示 `< 1 秒` 的总耗时是旧 `earth-pole-s…807-r1`：源视频真实路径在素材准备阶段立即 `ENOENT`，属于同秒快速失败，不是长视频分析耗时被截短。
- 浏览器实测第一条当前失败任务的详情为总耗时 `1 小时 3 分`、排队等待 `< 1 秒`、实际处理 `1 小时 3 分`，三段口径能区分“立即领取”和“长时间处理”。刷新后仍为 51 条记录和 1 条异常滞留。
- 浏览器在 1280×720 与 390×844 下均无页面级横向溢出；窄屏队列表格在自身容器内横向滚动。浅色与 GitHub 深色主题的可见渐变计数均为 `0`，状态色只用于失败、完成和警告。唯一控制台错误仍是 SSH 转发来源缺少 Gateway 设备签名的既有 `device signature invalid`，与任务接口和页面无关。
- n8n PID `71374`、qwen-current Gateway PID `27824`、video worker PID `47879` 全程连续，未重启。旧 `.runtime/releases/3a878da-runtime` 完整保留为控制台回滚点；回滚只需停止 3017 并从该 release 重启，不涉及数据库或任务状态回滚。
