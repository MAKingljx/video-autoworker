# 2026-09-06

## 恢复远端连接与最终工具基线验证

- 用户提供新的 cpolar SSH 入口并要求连接测试。沿既有固定主机密钥严格连接成功，真实主机与受管用户匹配；本机 SSH 别名和连接记录已更新。没有接受未知密钥、修改凭据或弱化主机校验。
- 只读核验确认 canonical repo 仍为干净 `93744b8`，远端与注册 GitHub 仓库匹配；3017、n8n 和目标 qwen Gateway 均返回 HTTP 200。应用仍运行旧 `57f6e6c` release，n8n current 仍为 `542eebd`。
- 继续此前授权的非激活准备：`e1e303e` 的独立源码已按完整提交准备并验证依赖和 SQLite 原生绑定；独立 n8n 候选为 2.31.6，受管源码清单通过，但没有替换 production current、导入工作流或生成生产确认能力。
- e1 的真实 Aqua 工具基线捕获成功：v4 catalog 为 62 项、effective 为 12 项；catalog 没有 notice，effective 只有已认可的 `browser-filtered-by-profile / info`，其原始策略哈希已纳入基线。捕获前后配置、会话注册表、受管插件/Skill 树和三个服务进程一致，没有运行 Agent、发送消息或写会话。
- 一次性捕获 job 已卸载且无 helper 残留，私有基线与脱敏验证证据已保存。该结果证明实际运行时兼容，不代表插件安装或生产切换完成；后续维护 restart 后必须重新捕获当次基线。
- 当次导演脑 migration dry-run、新的完整只读备份与独立 verify 通过，覆盖 11 表、230 字段、22 记录；计划仍为只新增 14 个空字段，catalog 未变化，未产生 apply/progress 回执。相关一次性任务已清理，服务进程保持原值；最终 apply 仍需用户对具体生产数据动作的确认和当时的备份复验。
- 持久配置复核确认 canonical `.env.local` 已保存正确的生产数据库与 token 文件路径，默认平台环境文件没有覆盖这些路径；新槽启动器会按固定顺序加载并强制正确的运行模式。数据库只读计数确认没有非空的旧 Gateway credential 行，不需要扩大当前四项数据改动范围。

## 修复共享安装门的运行目录布局校验

- 为避免已观察到的 FileProvider 异步污染，新生产 release 和 blue/green 状态将使用受管用户 home 下的独立 services/state 目录。静态复核发现 e1 的 rolling 安装门仍把运行状态与 slot release 路径分别硬编码为仓库内 `.run/blue-green` 和 `.runtime/releases`；首次 legacy bootstrap 暂不触发此分支，但后续共享组件维护会被错误拒绝。
- 将两组受信布局成对验证：仓库内运行根只配同仓库 release 根；同一受管用户的独立 state 根只配对应 services release 根。继续拒绝交叉配对、其他用户、任意目录、符号链接、权限漂移，以及 router/slot、PID/cwd 或数据库身份不一致；不通过环境变量绕过目录边界。
- 统一 preinstall、bootstrap 和 LaunchAgent 必须一致绑定选定的绝对运行路径；配置持久化要求已同步到发布手册。生产默认平台环境文件继续使用现有固定路径，未扩展非默认环境文件的支持范围。
- e1 应用包与 n8n runtime 只保留为未激活候选，后续制品与验收须重新绑定修复后的最终提交。四项生产数据确认尚未收到；没有执行父任务 CAS、飞书字段迁移、n8n 工作流导入、Mission schema 迁移、生产插件安装或 3017 切换。
- 窄修复专用回归 37/37、Node 语法和独立实现审查通过。Node 22.22.3 / pnpm 10.33.0 下最终标准全量为 2933/2933：regular 2111、task-flow installer 31、video-lane supervisor 10、director installer 82、runtime convergence 94、legacy reconcile 113、video-command 120、director-brain 261、task-flow Node tests 111。类型、API parity、敏感扫描、Lint 和 diff 检查通过，Lint 仅有 12 条既有 warning；生产构建和完整 standalone 审计通过，禁入成员 0。
- 提交前构建仍为 dirty，仅作为编译证据；最终 clean 制品、浏览器验收和 GitHub CI 必须绑定修复后的新提交，不能用 e1 的历史通过替代。生产尚未切换，旧运行 release 和既有备份保留；真正进入维护时须重新核对现场并生成有效的当次冻结/确认/回滚证据。

## 视频守护安装器测试分组

- 首次标准全量的 regular 分区为 2120/2121 通过；唯一失败是视频守护安装器的已安装 Skill 漂移检查触发默认 5 秒上限。该文件实际执行完整安装脚本和私有运行时准备，却仍与普通单测并发执行。
- 将这一个文件加入现有 heavy 串行清单，不改用例断言、生产脚本或任何超时值。独立串行 10/10 通过，原失败用例约 0.9 秒，整文件约 19.1 秒；原并发整文件约 74.6 秒。分区校验为 `175 = 170 regular + 5 heavy`，没有遗漏或重复。
- 标准全量已按新分组执行并全部通过，结果见上节；没有只复跑原失败用例或跳过其他分区。

## 导演脑清单校验的进程开销

- `173b0f0` 的同提交 CI 在已串行执行的 director installer 分区失败：两条完整安装加回滚用例分别约22.1秒和17.2秒，超过原15秒限制；均为超时，没有行为断言差异。此前regular2111、task-flow31和video-lane10全部通过，director80通过/2失败；后续分区与E2E未执行。该提交的本地通过和候选制品不作为生产发布完成依据。
- 隔离profile确认一次完整apply→rollback在五类工具上启动1038次进程，其中stat547次、Perl shasum310次。仅按相同次数进行的进程微基准约3.67秒，占本地约9.1秒墙钟的40%；两项本地原用例分别约9.4秒和8.8秒。它们已处于串行分组，不能继续把失败归因于文件间并发。
- 将task-flow已有的批量Node引擎提取成唯一共享helper，供task-flow和director两安装器使用。每个原验证点仍重新生成清单，使用O_NOFOLLOW、64KiB流式哈希和前后完整stat/目录复验；不跨验证点缓存，不减少验证或放大超时。
- 保持旧TSV的C字节序、mode、路径及shasum转义语义；task-flow保留既有absent/symlink/other记录，director限定普通文件与目录。director普通树清单与排除自身MANIFEST的备份清单复用同一严格模式，失败删除partial输出，旧有效备份无需改格式。
- 共享helper同步进入两个安装器的源码完整性/Git跟踪门、standalone的required/allowed/显式动态依赖以及部署前source gate，防止仅本机有文件而交付包遗漏。依赖计数按引用边计算，两个安装器各增加一条，总数26→28；对应fixture与行为覆盖同步更新。
- 两项原失败用例在相同Node22隔离运行下由9.394秒/8.849秒降至6.426秒/6.179秒；五类工具调用由1038次降至194次，减少约81%，剩余stat64、shasum22、find45、cmp50、cp13。15秒预算、全部验证点和回滚语义保留。
- 两个原失败项和两个新增严格清单合同用例均通过；task-flow专用31/31通过，四份受影响制品/部署集成测试合计231项通过（依赖边计数断言经校正后单独复验），独立静态审查GO。
- 最终标准全量2937/2937通过：regular2113、task-flow installer31、video-lane10、director installer84、runtime94、legacy113、video-command120、director-brain261、task-flow Node111。Node22.22.3/pnpm10.33.0下类型、API parity、敏感扫描、Lint和diff检查通过，Lint仅12条既有warning；生产构建和完整制品审计通过，禁入成员0、55文件导入闭包与28条显式动态依赖完整。提交前dirty构建仅作编译证据，最终clean构建/E2E、同提交CI和远端隔离验收继续独立绑定新提交。

## RPC 收集前插件快照与证明引用绑定

- `60e2655` 的 CI 已通过 regular2113、task-flow31、video-lane10及完整director84分区，先前安装器超时已解决；runtime convergence分区93/94通过，唯一失败为初始catalog调用改变插件树后apply仍成功，属于断言失败。总计2331/2332通过，后续分区和E2E未执行，候选保持未激活。
- 根因是第一次树快照取在runtime RPC收集之后，初始RPC中的变更已被吸收入基线。旧检测只靠Gateway启动时间与文件时间比较；测试使用下一整秒的模拟启动时间，在允许的时钟偏差窗口中可能放过修改。这是采集边界遗漏，不能仅靠调慢测试或改时间预算解决。
- 每次runtime RPC之前固定必需插件树快照，读取和处理证据前后均与该锚点严格比较；保留独立fresh restart、PID、配置快照及完整工具/策略检查。持久proof新增稳定pluginCollectionAnchor，生成时必须与runtime.plugins一致，复用时还须与当前必需插件树一致；外层proof/v1保持兼容，旧缺内证证明拒绝复用。
- 初始树变更回归增加实际写入witness和明确anchor错误断言，避免依赖时钟边界。现有proof失效用例同时覆盖缺anchor/anchor与plugins不一致时拒绝生成空白输出以外的内容，以及旧proof复用和当前树漂移拒绝；没有增加真实apply次数或放大15秒预算。
- preinstall controller将可信readiness返回的proof SHA与随后实际读取/使用的同一文件引用绑定，避免校验返回后替换成另一份证明；恢复与再次验证继续维持这项绑定，同时保留原有合法fresh proof重验流程。语义验证仍由共享proof validator承担，不另建一套浅层anchor规则。
- 核心锚点、proof生成/复用定向已通过；controller集成47/47和会话韧性矩阵23/23通过，覆盖readiness后替换proof的确定性拒绝及合法fresh proof成功handoff。核心与controller独立审查均GO。
- Node22.22.3/pnpm10.33.0下最终标准全量2939/2939通过：regular2115、task-flow31、video-lane10、director84、runtime94、legacy113、video-command120、director-brain261、task-flow Node111；类型/API/敏感扫描/Lint/diff检查通过，Lint为0error/12既有warning。生产构建与完整standalone审计通过，禁入0、闭包55/58/28。提交前dirty制品仅作编译证据，同提交CI、clean制品和远端隔离验收继续绑定新提交。尚未执行四项生产数据操作或3017切换。

## 发布就绪脚本集成测试分组

- `c9d7698` 的 CI regular 分区为2114/2115通过，唯一失败是发布就绪脚本的递归源码闭包用例耗时约6.95秒，超过原5秒限制；没有行为断言差异，后续heavy、插件、构建和E2E未执行。该候选仍未获得发布通过状态。
- 独立Node22串行profile为30/30通过，原失败用例约2.86秒，整文件测试约5.13秒。该文件每次准备三套完整私有payload，目标用例复制真实61文件闭包、创建Git仓库及提交、多次验证依赖与provenance，并移除传递依赖确认拒绝，属于实际文件系统/Git集成。
- 只将该文件加入现有heavy串行清单，保留全部断言、5秒用例上限和生产闭包校验。分区实际校验为175=169 regular+6 heavy，无遗漏或重复；修改后的定向30/30通过，独立静态审查GO。
- 本地标准入口首轮的前六分区2334项通过；legacy分区因验证进程选用了多硬链接的Node可执行文件，在fixture前置被物理身份检查拒绝，首轮命令非零。恢复原已验证的单链接Node路径后，受影响legacy113/113通过；两个Node均为22.22.3，字节数和SHA-256完全一致，未修改产品检查或测试预算。首轮日志保留，已通过分区不重复执行。
- 随后原未执行的video-command120、director-brain261和task-flow111全部通过，标准测试覆盖合计2939/2939；这是保留首轮有效结果并纠正环境后补跑的总覆盖，不宣称首轮标准命令成功。同提交CI须独立完整执行标准入口，不能用本地汇总代替。
- 类型、API契约、源码敏感扫描及lint通过，lint为0错误/12条既有警告；生产构建与其内置standalone审计通过，禁入成员0，导入闭包55文件/58静态引用/28动态依赖。提交前构建为dirty，仅作为编译证据；clean构建、E2E、远端隔离启动与至少870秒后的复验均须绑定最终提交。
- 新候选继续使用独立非Cloud运行目录。生产旧release、数据、工作流与服务保持现状，已有候选及备份保留；四项生产数据动作仍待明确确认，未执行apply或3017切换。

## 已确认生产迁移与安装入口权限修复

- `c02bc9c` 的同提交CI完整通过2939项标准测试和515项E2E；干净构建、额外OpenClaw浏览器检查、远端私有数据库probe及870秒完整性复验也通过。用户随后明确确认父任务修复、导演脑字段追加、两条受管工作流更新和Mission迁移四项操作。
- 用户确认在已展示的准备记录有效期内到达，但提前的无确认恢复计时已启动Gateway。重新生成机器准备记录时，逐项证明input、完整target、工具摘要及两库身份与用户已审记录相同，再绑定真实当前确认；没有复用旧token或倒填时间。
- 父任务生产CAS已完成：只改变唯一陈旧accepted父记录的status、固定`VIDEO_CALLBACK_LEASE_EXPIRED`错误、completed_at和updated_at，其余字段不变；数据库quick_check正常，正式队列total/attention/waiting/running均归零。该数据修复本身不代表已完成发布。
- 导演脑生产知识库实际新增14个空字段，catalog升至v3，11表合计244字段；既有记录及既有字段均未改动。真实check、verify和rollback-dry-run通过，一次性Aqua任务正常退出后已卸载并归档。
- canonical仓库已干净快进至c02；持久环境文件未变化，既有Node22与SQLite原生模块可用。官方blue/green LaunchAgent安装只生成supervisor状态和启动模板；禁用由`RunAtLoad=false`、enabled marker缺失及未加载共同保证，模板没有字面`Disabled`键。没有初始化router/slot/baseline，也没有启动新应用。
- 在统一组件安装前发现真实入口权限遗漏：video-command安装器在Git及canonical文件系统中缺少可执行位，而orchestrator的预检、安装、探测和回滚直接调用该文件，运行会报EACCES。模拟安装器fixture曾统一赋予可执行权限，因此原通过结果未覆盖真实Git入口的模式。
- 最小修复只把该安装器的Git文件模式从100644改为100755，源码字节和业务逻辑保持不变。当前仍未导入n8n工作流、安装OpenClaw组件或启动新3017；Gateway保持已批准的维护暂停，旧n8n和3017继续健康运行。修复后的CI、干净制品和后续迁移意图须绑定新的最终提交，已完成的父任务和飞书数据操作不得重复执行。
- 修复前真实入口直接执行返回EACCES，修复后统一orchestrator调用的四个真实shell入口均直接执行`--help`退出0；Git blob和完整文件SHA保持一致，内容diff为0。类型、lint、API契约、敏感扫描、生产构建与standalone审计通过，lint仍为12条既有warning、制品禁入成员0、闭包55/58/28。仅执行权限发生变化，保留同一源码字节已通过的c02全量基线，不宣称重新运行本地2939项；新提交仍须独立通过完整CI后才能发布。
- 回滚与下一步：父任务恢复限定精确反向CAS；飞书仅保留已验证的人工回退方案，不自动删字段或覆盖数据库。n8n后续使用官方stop/install切换到最终runtime后再生成短时导入能力，随后import/start/verify/attest；禁止仅从新目录调用脚本却继续沿旧current运行。

## n8n已迁移与生成器目录合同修复

- `a9e05dd`完整CI通过2939项标准测试及515项E2E，干净构建、523项本地浏览器检查和远端870秒复验均通过。canonical已干净快进至a9，四个真实shell入口的Git执行位、实际执行能力和无副作用参数检查通过。
- n8n已使用官方installer切换到a9 runtime。首次沿旧在线intent生成确认时，被停机checkpoint引起的database family变化拒绝，发生在capability/journal及workflow写入之前。未放宽首次claim的精确family校验；改为官方stop后，在完全离线状态重新备份并比对两条工作流语义/active状态，再生成intent、current-confirm并立即import。
- 两条固定工作流已通过官方幂等导入、发布、启动、canonical live verifier及transition attestation；journal完整到COMMITTED，两条workflow均active，节点数10和12，n8n健康及数据库quick_check正常。两次后置命令路径/重定向错误均在真实verifier执行前被识别，只继续未完成阶段，没有重复import或手填回执。live verifier来自canonical源码仓库，不在n8n独立runtime的受管文件清单内。
- 冻结前全面预检发现两个证明生成器仍把standalone固定到仓库内`.runtime/releases`，与已验证的独立services运行目录不一致。将可信目标解析集中到freeze-evidence的共享函数，由rollback-proof、evidence生成及FD/静态复验共同调用，只接受原仓库布局和当前OS用户的固定services布局；保留规范绝对路径、无符号链接、realpath、owner、权限、manifest和完整制品审计，不接受任意环境变量目录。
- 生产继续采用实现已支持的固定`legacy-freeze/guard.sock`和同目录`guard.token`，不扩展任意socket接口。pre/post回滚证明使用各自独立输出目录，避免固定数据库备份文件名冲突。guard建立后才恢复qwen并捕获工具基线，所有证明与统一preinstall保持同一个guard。
- Mission主库原为0644，已通过同一O_NOFOLLOW文件描述符仅收紧至0600；inode、大小、owner、nlink和mtime不变，旧3017仍持有原FD，健康和quick_check正常。父目录0755且没有group/other写权限，保持原样；没有修改数据或重启服务，也没有放宽controller的0600要求。
- 共享解析器新增回归覆盖可信外部目录生成/复验、原目录兼容、任意目录、其他用户目录、路径逃逸和符号链接拒绝，定向27项及独立审查通过。完整回归、构建和新提交CI继续执行；此时parent、Feishu和a9 n8n已真实完成，OpenClaw安装、Mission 051–059迁移与3017切换尚未执行。
- 新修复提交的后续n8n发布必须使用新的runtime/recovery/intent/确认能力和attestation。不存在数据库级全局transition锁定，官方接口允许同语义幂等发布两个固定ID；旧a9 COMMITTED证据只留作历史，不改标为新提交，也不重复parent或Feishu业务写入。
- 新实现标准全量2943/2943通过（regular2089、六heavy合计362、video-command120、director-brain261、task-flow111），较a9新增4项路径合同覆盖；定向27/27及独立审查GO。Node22.22.3/pnpm10.33.0下lint为0错误/12条既有warning，类型、API、敏感扫描、生产构建和standalone审计均通过，禁入0、闭包55/58/28。提交前dirty构建只作编译证据，最终clean制品与同提交CI继续独立验证。
