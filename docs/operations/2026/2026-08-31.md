# 2026-08-31 导演脑运行时安全收敛与发布门复核

## 范围

- 继续完成 `PROJ-VIDEO-AUTOWORKER` 内的独立飞书导演脑，不创建第二个项目或第二条视频任务链。
- 本阶段只推进非剪辑导演脑的本地候选与隔离验收；DaVinci、时间线控制、渲染、导出和其他剪辑执行继续冻结。远端投影、真实项目导演认知、审核权限治理、长期负载、多用户、管理界面和灾难恢复等非剪辑能力仍未生产化。
- 在生产运行硬门归零前，只允许本地修复、测试、Git 交付和生产只读核验，不安装或切换远端运行态。

## 安全修复

- 正式素材证据转换器仍校验分段转写字段类型，但不再投影分段原文或“声音信息”，也绝不使用包含逐段转写的 `combinedText` 作为证据摘要；只有受治理的 `output.summary` 可以进入摘要，缺失时失败关闭。
- 运行时读取要求记录包含准确项目 ID；作品业务还必须包含并匹配作品 ID，缺失或错配时失败关闭。
- `reviewed=true` 不再只看状态值，还要求领域必填字段、合法语义版本、来源、更新时间以及表结构要求的审核人、审核时间和审核原因全部有效。
- 默认搜索、六层工作流和上下文组装只接纳完整审核契约；按稳定业务 ID 显式读取候选时保留 `reviewed=false`。未审核导演案例不会输出“成片位置”。
- 请求和远端返回均拒绝所有 HTTP/HTTPS URL，防止飞书资源链接或普通外链绕过资源边界。
- 全片摘要只绑定覆盖完整媒体时长的全局证据；局部时间段只使用同段结构化视觉观察，不能把全片结论重复绑定到局部时间码。已有 `materialId` 原样复用；没有时按文件内容生成稳定 ID，随机收件箱 `videoKey` 不再冒充长期素材身份。
- 所有审核路径在实际更新前重新精确读取并比较完整记录快照、版本、状态、record identity、项目和作品归属，写后回读；搜索与六层工作流统一拒绝重复稳定 ID，请求和远端文本同时拒绝明显飞书内部资源 ID 形态。
- 本地候选已形成可恢复的媒体交接状态机：源文件绑定 `dev/ino/size/mtime/ctime`，暂存成品绑定内容 SHA-256、任务、幂等键、批次、随机所有权令牌和受控 `materialId`；持久化阶段覆盖 `prepared -> anchor_observed -> copy_observed -> source_finalized -> staged -> triggering` 以及 `discarding_prepared -> discarding`。生产者按任务维护持久 outbox，消费结果不确定时保留并复用同一任务、素材 ID 与 nonce；消费者先把交接身份写入任务级 WAL，再写入正式单任务状态并返回持久 ACK，生产者只在该 ACK 后删除 outbox。
- macOS 跨卷路径在 hard-link 完成后、复制开始前立即持久化 `anchoredIdentity`。恢复时必须同时匹配生产者 outbox、消费者状态、锚点 inode、所有权令牌和 checkpoint 身份；checkpoint 后的 `chmod`、`chown`、来源漂移或冲突均失败关闭。崩溃恢复只处理身份精确匹配对象，归属不明时进入 `attention` 并保留现场，不猜测删除、重提或串用其他媒体。
- 已存在但仍停留在 `queued` 的同一任务，只能用原任务 ID、幂等键和同一 `videoKey` 有界续派；其他非终态或终态重复请求不会重新派发。Webhook 响应不确定时以平台 Claim 后的正式状态为准，不能把已运行或已成功任务误记为失败。
- `finalize` 必须先确认 `prepare/audio/vision` 三项正式输出；最终输出携带受控 `materialId`、媒体时长和分析版本。finalize 子任务与父任务终态由同一 SQLite 事务提交，已成功 finalize 可修复旧运行时遗留的父任务未完成状态；运行中的 finalize 另有独立 `24` 小时 hard lease，超时后父子任务原子失败并阻断迟到回调。
- migration `051` 新增持久媒体清理债务：成功 finalize 或 hard lease 超时的父子终态与清理责任在同一事务记录。独立 janitor 每 `5` 分钟只读取到期债务、不扫描媒体目录；重试前校验作用域、工作流绑定、工作区摘要和父子终态，失败按有上限的退避继续保留债务，且不回滚已提交的父子终态。这套 journal 与清理债务只维护交接恢复和终态后的资源回收，不创建第二套业务任务状态机；平台 SQLite/API 仍是任务、父子阶段和最终结果的唯一权威。
- 上述媒体身份、崩溃恢复和续派增量仍是本地候选，尚未部署到当前 `542eebd-runtime`；不得把本地测试通过写成生产已生效。
- 安装器在既有 `plugins.allow` 存在时只保留原名单并追加导演脑插件，不会重新放通被排除插件；回滚点使用覆盖目录、文件、权限和摘要的全树清单，额外、缺失或漂移成员均拒绝恢复。
- standalone 清理器改为物理目录边界校验和精确准入：任何写入前先拒绝异常祖先符号链接，pnpm 源 store、依赖目标或链接边界缺失时失败关闭；构建机路径从 `server.js` 与 `required-server-files.json` 清除，source map、trace manifest 和源码扩展均不进入最终 release。
- 最终 release 生成覆盖目录、普通文件摘要与符号链接目标的 SHA-256 清单，启动前重新验证；运行数据、数据库、token 文件和 PID 必须是 release 外的绝对路径，物理解析后落回 release 的路径会被拒绝。基础 schema 同样拒绝符号链接、空白内容、缺少核心表或关键列的伪 schema，失败事务不会错误记录 `001_init`。
- 部署器只停止经 PID、监听端口、进程工作目录和旧 release 身份共同验证的进程；构建前保留独立恢复 release 和旧进程实际使用的外部数据绑定。SQLite 迁移遇到 WAL、SHM 或 journal 时必须安全合并或失败关闭，不能整目录覆盖。新进程还需验证实际数据库、token 和 PID 绑定没有被环境覆盖；页面、API、数据库和 release 清单全部验收前保留恢复副本，失败时只恢复旧 release 及其原外部绑定，不回滚任务库或其他受保护服务。

## 本机隔离验收

- 使用 Node `22.22.3`、pnpm `10.33.0` 完成主 Vitest `1533/1533`、video-command `105/105`、OpenClaw 导演脑插件 `21/21`、task-flow Node `77/77`。TypeScript 通过；API parity 为 `286 = 278 + 8`；ESLint 为 `0` error、`12` 条既有 warning，Next.js 生产构建生成 `134` 个页面条目。
- 干净构建的 release 清单覆盖 `5925` 个普通文件与 `2383` 个符号链接；最终 `.map`、`.nft.json`、源码扩展、`sourcesContent`、构建机绝对路径、cache、release 内 `.data` 和 PID 均为 `0`，禁入成员为 `0`。最终隔离启动后，登录态 `/materials` 与 `/tasks` 均为 HTTP `200`；素材后端缺失时 `/api/materials` 按预期返回脱敏的 HTTP `502` 与固定错误码，不再把 SSH 命令、路径或连接细节显示到页面。任务准入暂停、恢复均为 HTTP `200`，最终恢复为 active；迁移 `052` 至 `056` 全部存在，Mission Control SQLite `quick_check=ok`。真实浏览器确认桌面宽屏下两页均保持“左侧导航、右侧核心内容”，其中 `/tasks` 的任务链内部继续使用独立主从栏；宽度 `768 px` 与 `390 px` 的窄屏复验同样无横向溢出或区域重叠。HTTP 状态与视觉布局分别验收，不能互相替代。该结果属于本机隔离 release，不代表生产 `542eebd-runtime` 已切换，也不等于已经实现 Mission Control 导演脑管理界面。
- 此前已验收版本曾先 dry-run、再事务更新到本机隔离 `dev` profile；当前工作树中的后续安装器与私有运行时安全收敛尚未重新声明为已安装版本。所有本机安装证据均不替代远端发布。
- 新建隔离自然语言会话，只提供作品名、不提供任何作品 ID；OpenClaw 只调用 `aiworker_director_brain` 一次，参数只有 `action=workflow` 与 `query`，没有 `workId`。六层均返回“已就绪”，用户可见答案不含内部 ID，也没有补造工具未返回的事实。

## 生产只读事实

- 当前 cpolar SSH 入口通过既有固定主机指纹严格校验，远端身份仍为 `HEISENBERGS-1deMac-Studio.local / heisenbergs-1`。
- 真实仓库仍为生产 canonical checkout，分支 `main`、remote 为 `MAKingljx/video-autoworker`，工作树干净；精确本机路径仅保留在受控运维记录中。
- 生产 3017 监听健康，运行 release 仍为 `542eebd-runtime`，运行提交为 `542eebd`；Mission Control SQLite `quick_check=ok`。瞬时 PID 仅保留在本地当日记录中，不进入 Git 审计。
- 当日较早核验为活跃媒体节点 `0`、n8n 活跃 execution `0`、正式队列 `waiting=2 / running=1`；中间快照一度变为媒体 `2`、n8n `1`、`waiting=1 / running=1 / attention=1`，随后仍有一个媒体节点运行。一次预检曾误读生产 checkout 下未被 3017 打开的 `.data` 数据库并得到媒体节点为零；在任何 release 构建或进程切换前，通过 3017 的真实打开文件重新绑定独立运行数据库后立即废弃该结果。可信最新快照为活跃媒体节点 `1`、状态 `running`，创建与最后更新时间均为 `2026-08-26 11:23`；n8n 活跃 execution `0`；正式队列 `waiting=0 / running=0 / attention=1`。由于活跃媒体节点仍非零，发布门继续关闭。
- 最新复核同时确认 3017 与 n8n 健康检查为 HTTP `200`，Mission Control 与 n8n SQLite `quick_check=ok`，受保护进程前后 PID 快照一致。
- 功能提交已经推送 GitHub，生产源码 checkout 随后完成纯快进；发现真实媒体硬门仍非零后没有创建 release，也没有修改、重启或切换 3017、n8n、Gateway、模型、video worker、正式任务和数据库。3017 PID 与 `RUNNING_COMMIT` 均保持原值，源码 HEAD 与实际运行 release 继续分开记录。

## 回滚与已知边界

- 本机插件安装可使用安装器生成的已验证回滚点恢复；回滚同样不自动重启 Gateway，也不触碰飞书数据或任务链。
- 飞书审核记录当前没有密码学签名。具备直接编辑权限的人如果完整伪造全部审核字段，运行时仅凭记录内容无法识别；生产化必须补齐最小权限、独立审核入口和审计告警。
- 飞书记录更新接口没有跨 UI 的原子条件更新。服务会在写前再次比较完整快照并写后回读，但直接编辑者若恰好在最后读写窗口内并发修改，仍需依赖最小权限和异常变更告警控制风险。
- 表结构、验收样片和六层就绪只证明契约闭环，不证明真实项目上的人物、故事、七维判断、叙事脚本、案例与技法学习已经稳定生产化。
- 除剪辑执行外，仍未生产化的范围包括：远端正式完成事件到导演证据的自动投影、失败重放与告警、限流和长期负载、真实素材上的人物消歧与跨日期更新、故事与因果发现、七维判断、叙事脚本、案例与技法学习、审核真实性与最小权限、多用户授权、导演脑管理界面及灾难恢复演练。
- release 清单用于发现构建后漂移和意外运行时写入，不是代码签名；生产供应链真实性仍依赖受控 Git 提交、严格快进、目标机本地构建及发布审计。

## 新任务入口暂停与蓝绿发布安全候选

- 全局准入闸门只阻止新的顶层任务；已存在的同幂等任务、状态查询、n8n execution、模型节点、媒体节点和最终回调继续运行。暂停与新任务创建在同一个 SQLite `IMMEDIATE` 事务内线性化，管理写入使用 revision CAS 并保留原因和操作者审计；非全局发布管理员只能看到最小接收状态。
- 3017 改为稳定 loopback router 加 blue/green 不可变槽。新任务持久绑定 `runtimeSlot`、`runtimeReleaseId` 与三个 callback URL；claim、node、media 回调都校验父任务的 release affinity。迁移 `053` 至 `056` 分别提供 scheduler 单主、webhook 派发租约、子执行 lease/fencing 与父 execution owner，响应丢失时只允许同一 execution 续接，不同 execution 判重终止。
- 正常切换和自动回滚在 router generation 更新后先有界等待目标槽观察精确 generation 并成为 scheduler leader，再执行 readiness、页面与 API 验证。旧槽退役先冻结新 callback、保持 listener 存活并再次等待 release-owned callback、lease、连接和静默窗全部归零；等待失败会解冻并保留旧进程，不能先停服务再发现仍有任务。legacy `/api/n8n/execute` 只允许显式 `legacy-v1` 且非 slot runtime，只有 `reviewer` 节点可完成父任务。
- 新增统一运行错误投影：公开响应和任务持久化只保存固定错误码与安全摘要；模型 HTTP、OpenClaw、ffmpeg、n8n callback、router 以及素材 API 的原始诊断只进入受控日志并先脱敏 URL、路径、凭据、内部 ID 与高熵值。
- 本地候选新增 blue/green runtime attestation：slot 启动使用 `umask 077`，统一拒绝符号链接、非当前用户所有或组/其他用户可写的三个环境文件；已有 PID 仍存活时拒绝覆盖。启动器以 `0600` 和 fsync + rename 原子记录 PID、slot、role、release、manifest、loopback endpoint、canonical SQLite 路径与 canonical router state 路径，证明中不包含凭据；同一路径同时导出给 scheduler 交接逻辑。
- `probe` 命令仍可验收连接隔离快照的 probe 进程；`switch` 与 `rollback` 则强制目标 role 为 `active`，并要求 runtime attestation、binding、PID、cwd、release 清单及 router state 路径完全一致，同时与显式 `AIWORKER_BG_LIVE_DB_PATH` 的物理规范路径相同。probe、drain、错误数据库或错误 router state 的进程都会在写 router state 前失败关闭。
- OpenClaw `aiworker-video-command` 候选版本由 `0.5.12` 提升为 `0.5.13`；唯一安装器明确允许从生产既有 `0.5.12` 进入该版本，不再把源码已变化但版本相同的载荷误判为“已安装”。task-flow 不新增或虚构独立版本。
- 当前旧生产 release 没有 callback affinity，所以首次迁入蓝绿结构仍必须完整归零一次。首次引导成功后，常规 3017 发版只需先暂停新准入，即可在旧任务继续执行时构建、验收和切换新 release；旧 release 按自身 affinity 继续收回调，只有退役旧槽需要等待它自己的任务与连接排空，不再把全局 n8n 活跃 execution 归零作为切换前提。
- 上述均为本地候选，未安装、未切换生产。3017、OpenClaw plugin 和 task-flow 是同一暂停协议的兼容组；蓝绿脚本不自动修改 Gateway 或 skill，正式变更仍需独立兼容矩阵、真实运行门禁和回滚顺序。详见 `docs/architecture/n8n-intake-blue-green-release.md`。
