# AGENTS.md

## Workspace Purpose

这个项目用于沉淀 `AI-worker` 相关的部署方案、架构图、工作流和后续运维记录。

## Session Startup

本机整个 AI-worker 工作区统一归属 `PROJ-VIDEO-AUTOWORKER`。飞书项目档案的本地路径与主 `.PhoenixBrain` 均以本文件所在的 AI-worker 根目录为准；Git 管理边界也以此根为准，沿用 `MAKingljx/video-autoworker` 及完整历史；产品物理目录保持 `video-autoworker/`，不得为 Git 管理迁移业务源码。

从产品子目录开始任务时，也必须显式将 AI-worker 根作为 `resolve --project-root`，并使用上述准确项目 ID；不要在子目录重建主标记。源标记权限保持 0600，不进入 Git、上传或归档。其他设备的项目根另按其实际环境核验，不能照抄本机路径。

每次进入项目时，优先读取根规则与本机已有的私有记录；新克隆不包含私有记录时，通过同项目飞书规则和公共运维记录接续：

1. `AGENTS.md`
2. `TOOLS.md`
3. `MEMORY.md`
4. `memory/` 下最近两天的记录

## File Conventions

- 日常产品开发只进入实际目录 `video-autoworker/`，其内部 `src/` 放业务源码；源码历史通过 Git 提交或标签恢复，不为每次迭代复制完整源码；确需临时重建历史版本时放在 `rollback/video-autoworker/<commit>/` 并在任务结束后退役。目录职责以根 `README.md` 和 `docs/aiworker-directory-and-git-boundary.md` 为准。
- 本机生成结果统一放在 `output/` 的任务或日期子目录。共享 Git 存储在 `repositories/video-autoworker.git`（bare，是本机根工作树的对象存储，不可当缓存删除）。`tmp/` 仅按任务临时使用，任务结束后按清单核验并清理。
- 架构、部署、流程文档统一放在 `docs/`
- SSH 主机记录、连接方式、运维命令放在 `TOOLS.md`
- 长期有效的决策、约定、节点命名放在 `MEMORY.md`
- 当天发生的变更、讨论、临时决定放在 `memory/YYYY-MM-DD.md`
- 遵循 `VAW-WORKSPACE-GOVERNANCE-001` 与 `RULE-BACKUP-RETENTION-001`：当前副本外，每个同一恢复对象的备份集最多保留最近两个已校验、可恢复的历史版本，跨目录和压缩包计数。不得通过改名、套子目录或把多版本装入一包规避上限。
- 临时工作树在任务结束后核对并退役；独有改动、数据或进程导致暂不能移除时，记录准确路径、原因和解除条件，继续列为待处理项，不得宣称已全部清理。不得截断 Git 提交历史或删除源数据、唯一恢复点来凑两份。

## Project Record Management

- 后续每个非简单 AI-worker 任务都必须留下完整记录，至少覆盖需求、决策、实际改动、运行环境、验证证据、部署/回滚、已知问题和下一步；明确区分本地候选、隔离验证与生产事实
- 本地当天细节继续写入 `memory/YYYY-MM-DD.md`，长期有效的架构、约定和故障经验同步写入 `MEMORY.md`；不得只保留在聊天记录中
- GitHub 以 `MAKingljx/video-autoworker` 为可共享记录的版本控制入口，脱敏产品运维记录统一放在 `video-autoworker/docs/operations/YYYY/YYYY-MM-DD.md`；代码、工作流、部署或生产变更应把对应记录放进同一提交并推送
- 用户明确要求只分析、禁止写入或禁止外部操作时不得为了补记录擅自提交；应说明待补内容，并在下一次获准的 Git 写入中补齐
- GitHub 记录不得包含密码、Token、App Secret、私钥、聊天 ID、会话正文、原始日志、数据库、索引、模型权重、备份内容或其他运行时敏感数据；只保存脱敏结论和可复现证据

## Working Rules

- 开发、测试、构建保持在 `video-autoworker/` 执行；Git 根与产品根通过共享布局模块解析，历史 flat 提交与新带产品目录前缀的提交都要支持，不在各脚本散落路径特判。
- CI 按实际影响选择文档、相关测试或完整验证；只有核心运行、状态、安全、依赖、构建发布或无法确定影响时扩大范围。相关测试使用导入依赖图，选中的重测试在独立环境并行，不漏测、不把空选择当通过。
- 构建、相关检查、制品审计和真实只读/隔离数据库验收前置；生产维护窗口只保留必要备份、安装/切换与健康验收。失败时优先恢复原服务，完成隔离修复后再进入维护，不在停止接单期间长时间开发排障。
- 证据批量读取使用有上限的只读并发，取消该动作人为固定的总时限；保留取消、网络异常、身份与数据完整性检查。审核写入仍须用户明确确认，不把读取优化扩大为并行写入或自动批准。
- 遵循飞书全局规则 `SAFE-EFFICIENT-EXECUTION-001`：开发部署兼顾安全与效率；已验证且未改动、未受本次依赖/配置/环境变化影响的部分复用原证据，不重复全量或无关验证，只验证改动及影响范围，并保留必要的新制品和真实部署验收。
- 对不合理指令、缺陷或低效流程保持专业判断，不附和；在既有目标和授权范围内自主定位根因、选择长期可维护且可回滚的方案、修复并继续原任务。新问题本身不要求停下等授权；只有越界、尚未授权的不可逆或重大风险、强制审批、或确需用户决策时才暂停相关动作。避免重复失败循环、临时分支和过度重构，同类缺陷优先统一共享契约。
- 优先使用中文输出项目文档
- 页面视觉默认采用克制配色：以中性色为主体，只保留一个主色用于当前选择、主要操作和关键链接；绿色、黄色、红色仅用于成功、警告、错误等真实状态，不为本地/云端、模型类型、普通标签或装饰性信息分别分配高饱和颜色，避免彩虹式标签、大面积渐变和无语义的发光效果
- 新增节点、网络、模型路由变更时，同步更新 `MEMORY.md`
- 如果新增 SSH 主机，只记录别名、用途、连接方式，不要把私钥提交到仓库
- 密钥、密码、API Key 不要写入仓库，使用 Keychain、环境变量或外部密钥管理
- 涉及公网暴露、删除文件、覆盖配置前先确认风险
- 不要轻易删除文件，尤其是数据库、素材索引、记忆库、工作区数据等核心数据文件；默认先备份、先确认用途、先评估影响，除非用户明确要求或已有可验证备份
- GitHub 版本控制重点覆盖远端生产可复现所需的核心源码、工作流定义、部署脚本、LaunchAgent 模板、测试、无密钥配置模板和脱敏项目记录；Git 从 AI-worker 根管理，依赖、数据、密钥、原始运维记录与生成产物继续排除
- 本机日志、PID、运行状态、缓存、临时输出、测试截图、备份、模型权重、数据库、素材索引和本地每日原始记录不直接纳入 Git；对应的脱敏项目审计记录必须进入 canonical repo，但远端生产实际依赖的脚本或配置不得只以未跟踪文件形式存在
- 源码优先通过 canonical Git 增量同步，不因每次迭代重复整仓传输或整仓备份；制品可在独立暂存中增量传输，但目标必须完整校验成员、依赖闭包、摘要及启动情况。数据库、素材、索引和真实运行配置按各自恢复对象独立备份，密钥留在批准的密钥管理设施；继续执行两份有效历史版本上限。
- 涉及远端页面、服务或部署仓库改动时，必须先确认实际运行目录、Git remote、监听端口和当前进程；后续修改、commit、push、重启和验收都要落在同一条真实运行仓库线上，不要只在本地同名副本操作
- OpenClaw 运行时故障优先使用官方配置、上游稳定升级或带测试的可复现源码构建解决；不得把直接修改 npm `dist/` 当作常规方案。紧急临时加固必须先备份、明确退出条件，并在根因方案验证后从生产运行链移除；若必须改源码，必须使用 Git 管理、回归测试和可复现构建。

## AI 下载文件管理

- 默认生产节点 `heisenbergs-1` 上由 AI 下载或获取的普通外部文件，统一放入 `/Users/Shared/aiworker-storage/ai-downloads/<project-slug>/YYYY/MM/DD/<task-slug>/`；不得散落到桌面、系统下载目录、项目源码仓库、OpenClaw workspace 或运行时临时目录。
- 项目名和任务名使用小写英文、数字与连字符。未完成文件进入 `_incoming/`，完成大小、格式和 SHA-256 校验后移入 `files/`；任务目录保存权限受控的 `manifest.json`，至少记录下载时间、脱敏来源域名、原始文件名、字节数、SHA-256、用途和状态。
- 模型权重、数据库、备份、密钥、缓存以及视频链受控收件箱属于专用运行资产，继续使用各自受控目录，不得为了套用本规则混放到普通下载目录。
- 下载内容和包含本机路径、敏感 URL 或凭据的清单默认不进入 Git。GitHub 只管理目录规范、无密钥模板、管理脚本和脱敏审计记录。
- 本规则只约束后续下载，不自动迁移、覆盖或删除历史文件。历史整理、归档和删除前必须先盘点、备份并确认范围。
- 统一下载目录不是缓存目录，不设默认自动清理周期；保留、归档和删除必须由任务级策略或用户确认决定。

## Current Focus

- OpenClaw + Mac Studio 混合部署
- 云端模型指挥、本地模型执行
- 后续支持多人使用的节点规划

## Default Device

- 这个项目后续默认操作设备为第二台远端设备 `heisenbergs-1`
- 第一台 `heisenberg` 仅作为历史迁移来源或明确指定时才使用，不再作为默认执行节点
- 本机主要用于文档、规划、SSH 跳转和辅助运维
- 涉及执行、安装、排查时，默认优先检查第二台远端环境
