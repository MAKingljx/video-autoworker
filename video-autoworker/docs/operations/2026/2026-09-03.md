# 概览卡片固定尺寸优化

## 需求

概览页普通卡片不能再随实时内容多少变高或缩短；卡框应保持统一尺寸，消除同排卡片大小不一及框外空白，同时保证窄屏可用。

## 决策

- 只在概览组件范围内修复，不改变全局 `.panel` 行为。
- 非全宽卡片统一固定为 `20rem` 高；关键指标与快捷操作等全宽条带继续自适应。
- 普通卡片按桌面网格保留相对尺寸，尾行自动均分剩余列并撑满可用宽度；窄屏继续自动换成单列。
- 会话和事件等动态长列表改为卡内滚动；空状态在固定内容区内居中。
- 为卡片和行增加稳定的布局语义属性，供真实浏览器几何回归使用。

## 实际变更

- `WidgetGrid` 统一为普通卡片应用固定框架，并标识卡片 ID、固定或流式框架及行容器。
- 普通卡片逐行计算剩余网格列并均分，三卡、两卡和单卡尾行都完整占满所在行；窄屏仍按单列自动重排。
- 概览作用域 CSS 让卡片面板填满框架，固定标题，内容区独立滚动。
- 固定框架增加 `min-width: 0`，避免长会话名等不可断内容撑开窄屏页面。
- 会话工作台和事件流移除由内容控制的最大高度，统一接入框内滚动区。
- 新增 Playwright 回归，覆盖空数据、10 条会话、`1440px` 桌面、`900px` 平板、`390px` 手机及 Gateway 专属卡片。

## 环境与验证

- 本地候选基线：GitHub `main` 提交 `5b0b502dcf81a7c5d8b5be0885217ea990af2565` 的独立干净 worktree。
- 最终兼容验证运行时：Node.js `22.22.3`，pnpm `10.33.0`。本机默认 Node.js `25.8.1` 可完成页面构建，但项目运维测试明确锁定已验证的 Node 22 基线，因此完整门禁以 Node `22.22.3` 为准。
- 类型检查通过；全量 ESLint 为 `0` error、`12` 条既有 warning。
- 主 Vitest `1935/1935`、video-command `114/114`、director-brain `21/21`、task-flow `98/98` 全部通过。
- API parity 通过：`286 = 278 + 8`。
- Next.js 生产构建及 standalone 成员审计通过，禁入成员为 `0`。
- Playwright 定向回归 `5/5` 通过：本地与 Gateway 模式全部普通卡均为 `320px`，从空会话切换到 10 条会话后高度变化不超过 `1px`，长列表确认在卡内滚动；`900px` 下每张卡自动独占并撑满所属行，两种模式在 `390px` 下主内容区均无横向溢出。
- 应用内浏览器独立复验：`1440px` 下三卡行与两卡行均完整撑满可用宽度，五张普通卡高度均为 `320px`；`900px` 下卡片自动单列并撑满行；`390px` 下主内容区 `scrollWidth = clientWidth = 385px`。
- 生产等价隔离探针曾在 Gateway 窄屏发现长会话名把网格列撑宽；发布前为普通卡框补充 `min-width: 0` 并新增专门回归，避免修复高度后留下页面级横向空白。
- GitHub `Quality Gate` 运行 `33711526310` 在 `main@a12bec263a26277181bce43004022dbb73adc921` 全绿：单元测试 `2070/2070`、E2E `515/515`，API 契约、ESLint、TypeScript 和生产构建全部通过。
- 生产节点使用 Node.js `22.22.3`、pnpm `10.33.0` 构建 UI-only 不可变 release；禁入成员和越界软链接均为 `0`，`better-sqlite3` 加载与只读查询通过。

## 部署与回滚

- 生产源码仓库已纯快进到 GitHub `main@a12bec263a26277181bce43004022dbb73adc921`，origin 正确且工作树干净。实际 3017 运行包采用只包含本次界面差异的提交 `57f6e6cd671245c45d15439de7e39630869cc0ff`，不可变 release 为 `57f6e6c-runtime`；源码 HEAD 与运行提交是不同事实。
- 切换前从 3017 与 n8n 的真实打开文件绑定两套权威 SQLite。紧邻切换的活动门为媒体节点 `0`、n8n active execution `0`、正式队列 `waiting=0 / running=0`；两库 `quick_check=ok`，3018 无监听，video lane 保持 disabled、unloaded、worker-free。
- 隔离验收遗留的两个 loopback 测试来源已从 OpenClaw 配置精确移除。修改前创建私有备份，配置前后均为 `0600`，未输出正文或凭据，也未重启 Gateway；清理后两个测试来源均为 `0`。
- 只把 3017 从 `d3ca02e-runtime` 切换到 `57f6e6c-runtime`。当前唯一监听 PID 为 `32207`，进程 cwd、`RUNNING_COMMIT` 和两个 PID marker 均与新 release 一致。n8n、Task Broker、三个 Gateway、三个模型端口和 Ollama 的受保护 PID 前后完全一致。
- 生产 `/`、`/materials`、`/tasks`、健康接口及正式队列只读接口均为 HTTP `200`。实际静态 CSS 明确包含固定框 `height:20rem; min-height:0; min-width:0`、面板 `height:100%` 和内容区内部滚动规则。
- 应用内浏览器通过只读 SSH 转发复验真实生产：桌面三卡行、两卡行和单卡行左右余量均为 `0`，普通卡均为 `320px`；`900px` 和 `390px` 自动单列并撑满所在行，页面级横向溢出均为 `0`。测试完成后恢复浏览器视口并关闭本地转发。
- 切换后再次确认活动门仍为 `0/0/0/0`、两库 `quick_check=ok`、3018 空闲、video lane 关闭、受保护 PID 未漂移；OpenClaw 配置中的两个测试来源没有被探针重新加入。
- 上一不可变 `d3ca02e-runtime` 保留为只回滚 3017 的恢复点。使用相同参数执行 `57f6e6c → d3ca02e` 只读回滚预检已通过，工具明确确认 live runtime 未变化；未实际回滚，也不得联动重启或修改 n8n、Gateway、模型、worker、任务或数据库。

## 已知问题与下一步

- 本次布局与发布验收无阻断问题。长内容按设计在卡片内部滚动，不再通过改变卡框尺寸影响整页布局。
- 后续继续保持源码 HEAD、实际运行提交和回滚 release 分开记录；任何新 release 仍须隔离验收并保留只影响 3017 的回滚路径。
