# 2026-09-11 规则与发布收尾

飞书项目规范 `VAW-DIRECTOR-BRAIN-001` 更新至v1.2.0。网页“已关机，去唤醒”只按客户端展示处理，Safari/飞书验收忽略该字段；运行结论只依据真实消息响应、Gateway及插件、应用接口、权威数据库和受控收据。规则已精确回读，项目快照和21项相关检查通过，连接器86f5ad3已推送并完成版本复核。

发布清理严格按当前引用和两份历史上限执行。远端卸载51个已退出维护job，删除119个任务临时项、28个未引用旧应用release、9个失败协调器源码及多余插件、execve适配器和n8n备份，累计释放约6.17GiB。应用保留当前C7及475、d96两个历史恢复点；控制源码保留C7与cd80；插件、execve适配器和n8n各保留两份通过清单、摘要或归档检查的历史备份。错误生成且没有进程引用的默认`.data`数据库经quick_check和内容核验后删除，真实C7进程继续打开权威Mission Control数据库。

最终生产回读保持router blue/c7ca760 generation3，准入active/accepting revision32，活动任务0，应用healthy。原证据outbox=delivered、receipt origin=verified_read_recovery、条目38，候选批准数0。Safari只读查询已进入OpenClaw，Gateway记录模型执行、飞书流式卡片开始和正常关闭，`replies=1`；最终回复正确说明素材证据已整理并等待用户确认。Safari窗口在最终视觉回读前关闭，未为截图重复发送；“已关机”展示不参与该结论，也未批准真实候选。

用户随后明确要求全部批准原38条素材证据。本次先冻结原stable ID和版本，确认没有混入新候选，并创建通过quick_check的MC在线备份。38条通过应用正式review接口逐条串行执行“候选→已核验”，每条使用版本CAS并写后回读；最终批准38、驳回0，全部reviewed=true且版本各递增一次。原视频任务整行及output保持，outbox仍为delivered。调度器自动进入understanding阶段后，首次执行因`director_extraction_phase_input_too_large`失败为conflict/progress20；批准结果有效，不得重复审核。下一步压缩与历史种子重复的完整候选字段，保持有界输入并定向验证后重试。新增恢复点验证后轮换最旧MC备份，仍只保留两个有效历史版本。

根因是understanding阶段同时携带受控视频历史种子和38条已审核证据的完整飞书字段，二者语义重复并超过128KiB硬边界。修复保留每条stable ID、素材ID、时间范围、证据摘要、置信度、分析版本和记录版本；完整记录继续由reviewedReferenceDigests绑定，并在投影前重新读取校验。没有扩大输入上限，也没有降低引用、作品归属或审核状态检查。真实规模新增回归与提炼相关三组共51项通过，TypeScript和相关ESLint通过；生产增量发布及失败阶段重试结果另行记录。

OPT-017～OPT-022 的后续架构改进目前仅为本地候选，尚未提交或部署。导演脑应用入口新增显式 `director-brain-application/v1` 合同；旧薄插件的无版本查询、审核和 `operate+propose` 仍可读，新合同的未知版本失败关闭。OpenClaw 0.4.4 把审核批次的固定成员、确认码和逐条结果交给同一3017应用持久化，插件重启、响应丢失和重复确认均沿原批次正式回读，不增加鉴权或任务链。口述新增仍只产生候选并先展示审核预览。

长阶段输入超过128KiB时按受控视频时间项分段，每段独立保存输入、输出摘要和完成状态；中断后只续未完成分段，全部完成后才合并并执行一次既有候选投影。原始摘要、时间线、章节和导演感知不以截断冒充完整结果，单项本身超限仍明确失败。状态投影只以已交付阶段、segment checkpoint、任务租约和错误码为依据，返回已知性、最近进度、心跳、阻塞条件和下一步；没有数据时不编造百分比。

审核批次与segment使用三个集中定义、幂等且完整校验的SQLite附加表。它们不写入 `schema_migrations`，权威数据库版本仍为059；新应用启动或首次使用时安装并核对列、约束和索引，旧059应用可忽略附加表并冷回滚。相关应用、状态、可选schema和兼容测试127项、导演脑插件289项、安装载荷单项及TypeScript、ESLint、API parity、源码敏感扫描均通过；远端运行收敛全套未在本轮沙箱内重跑，既有 `ps` 权限型失败不能当成业务断言失败。

## 09-11系统优化清单本地实现

32项清单已收敛到发布操作、运行控制、验证证据、平台适配、导演脑长任务和运维治理六组共享模块，逐项实现映射见 `2026-09-11-optimization-coverage.csv`。发布计划升级到v2，绑定Git提交、动作、完成条件、物理run/releases目录、端口、平台配置摘要和权威数据库dev/ino；canonical run目录只允许一个全局operation owner。暂停和恢复原因绑定operation ID，journal任何阶段写失败都会阻止下一副作用。取消传播到受管进程组；route commit按精确generation+1、previous和两槽release元组认定，验收及资源恢复分别记账。

应用退役、绑定、启动、探测、候选预热、切换和最终验收改为同一受管进程、同一发布锁与同一验证cache。目标启动后若探测、预热或切换前失败，trap只通过正式manager停止本operation绑定的候选；路由已提交则只记录事实，不盲停或盲回滚。候选预热仅请求健康接口与release manifest绑定的静态资源，尚无前后业务计数时明确标记side effects未验证。受管active/drain槽位缺显式外部数据路径时拒绝启动，不再生成checkout内默认数据库；只读doctor核对release、PID、cwd、配置摘要、数据库路径身份和实际打开FD。

源码闭包由逐文件Git子进程改为一次tree/blob/mode/物理身份批量验证。制品继续在源和复制边界各执行一次完整审计；原子rename后及同一transition进程内复用绑定目录inode、manifest/provenance摘要和全树metadata的快速证据，任何漂移回退完整审计并失败关闭。正常测试计数为full=2、fast=3、fallback=0；故障注入为full=3、fast=3、fallback=1。CI保持影响范围选择，三组root partition并行，缓存键绑定OS、架构、Node ABI、pnpm、lock和构建配置，并在依赖安装后实际打开内存SQLite验证原生ABI。

运维合同新增完整manifest增量传输计划、严格TLS和有界退避、阶段耗时与资源采样、恢复对象inventory、最新事实revision、故障记录和集中脱敏字段。代码、配置和数据恢复明确分开；删除仍需授权，当前副本外只保留两份有效历史。RPO、RTO、资源预算及预热p95在没有业务决策和实测前保持未设置，不用猜测数字冒充优化结果。

本地影响验收已通过：蓝绿控制70项、发布操作与协调器37项、导演脑应用/可选schema/分段与状态相关检查、导演脑插件289项、旧3db execve恢复兼容8项、0.4.4薄安装真实路径1项，以及TypeScript、目标ESLint、API parity、源码敏感扫描、Node/Bash语法与diff检查。一次使用错误Node ABI的尝试被better-sqlite3真实加载探针拒绝，切换至`.nvmrc`规定的Node 22后同一蓝绿70项通过，证明新增ABI门有效。以上仍是本地候选事实；Git提交、远端构建、生产部署、原失败提炼重试和最终清理结果在完成后另行补记。

原任务的旧`understanding`超限冲突增加一次性前向修复入口。它只接受准确错误`director_extraction_phase_input_too_large`、首次attempt/revision、retryRevision=0、既有素材审核引用存在且本阶段没有lease、checkpoint、projection、segment或审核记录的同一phase，并在事务内复核完整来源/作品/合同/审核身份与updatedAt后CAS重排。再次发生同一错误、其他阶段或其他确定性冲突均不放宽。该入口不修改感知阶段、素材outbox或38条审核记录；相关runs/resilience 49项通过。

用户确认后续发布允许短时服务中断，采用维护替换模式：新版先离线构建并封存，部署窗口暂停接单、确认活动任务为零并备份权威数据库，然后直接停止旧服务、启动新版和完成影响范围验收。显式维护参数只免除旧槽投影兼容复核，其余排空、调度、路由、PID、端口、数据库、停止确认、新版身份与功能验收仍失败关闭；普通在线热切换保持原有完整门禁。

生产首轮执行在插件安装副作用前暴露了操作日志命名缺陷：内部组件名 `directorBrain` 直接拼入 journal step 后含大写字母，被安全名称合同拒绝。安装器没有执行，路由和数据库未改变，入口继续保持暂停。发布协调器现将三类插件稳定映射为 `install-task-flow`、`install-director-brain` 和 `install-video-command`，续跑和安装收据查找复用同一映射；相关日志失败门及三组件顺序测试通过。
