# 2026-09-20 音频转写无固定超时修复

## 需求与根因

5 个视频的源文件均包含可读的 PCM 音轨，手工调用 `large-v3-turbo` 也能完成转写。失败来自媒体节点给 Whisper 子进程设置了固定超时；模型加载或长片段处理超过该预算时，任务被误杀并记录为 `N8N_MEDIA_COMMAND_FAILED`。

## 实际改动

- `src/lib/n8n-media-execution.ts` 的音频转写不再传入固定 `timeout`。子进程正常退出、非零退出、输出超过缓冲区或系统终止时仍按原错误合同失败；ffmpeg 探测、抽音轨和抽帧的保护超时保持不变。
- GitHub 提交：`5404a60c1dee7e18dd63ad012cca400cde6ae095`。
- 生产 Web 制品：`5404a60c1dee7e18dd63ad012cca400cde6ae095-runtime`，蓝绿 generation 14，当前槽为 green。
- 生产独立 Worker 制品：`ea8d0bbcac9d4a43a9c38634526fb3d8ea3f8b08e6406f06fbdb116bdf80ef99`，PID 89206，健康并持有唯一租约。

## 验证

- 本地媒体定向测试 21/21 通过，类型检查通过。
- 生产 health 返回 healthy，数据库检查 healthy，接单恢复 revision 110。
- 新恢复批次 `video-batch-c632d5c3494bd38250809c4a0581af565f82c209cdc3eaf36a0eed235bcf7741` 的 5 个片段均在新版本下成功完成；原批次 24 个成功结果与本批 5 个恢复结果合计覆盖全部 29 段素材。

## 回退与限制

旧 Web/Worker 制品与本次绿色槽修复收据保留在生产受控目录。中途 cpolar SSH 隧道曾只接受 TCP 连接但不返回 SSH banner；恢复连接后只读确认 5 个片段全部完成。未重复提交批次，也未修改原任务业务数据。

## GPT-6 自动剪辑与 Resolve 成片验收

用户认为原先 5 个长区间只能说明“极寒露营过夜”，不足以讲完 29 段素材的完整故事。生产 `gpt-main` 使用 `openai/gpt-6-astra`、`medium` 思考强度重新读取 29 份已保存摘要，自主生成并核验最终方案；主控未替模型指定镜头。最终方案使用 10 个源文件、20 个镜头，视频帧时长 206.64 秒，结构为极寒搭帐与过夜、冬季到访、赴河破冰、工具意外、协作捕鱼和渔获收束。

GPT-6 在远端生产 Mac 上通过已授权的 System Events 打开 Resolve 内置 Lua Console，并调用运行中 Resolve 的 MediaPool、Timeline 与 Render API。它新建独立项目 `AI-worker Oymyakon GPT6 r3 20260920-1439` 和时间线 `Oymyakon GPT6 r3 Final`，导入 20 个编号中间片段，保持直切、单视频轨和双声道音频轨；未重试已失败的 FCPXML 源媒体匹配，也未修改其他 Resolve 项目、AI-worker 路由、数据库或网关配置。

成片由 DaVinci Resolve 21.1 实际渲染到远端受控目录：

- 文件：`/Users/heisenbergs-1/ai-worker/output/resolve-edit/2026-09-20/oymyakon-gpt6-r3/oymyakon-gpt6-r3.mp4`
- SHA-256：`eb6ca1e5353dbcf99e9094c17e7734de0bac2fe3f1185ddcdaa41dcad0f998fa`
- 容器：206.72 秒、646767680 字节；H.264 1920×1080、25fps、yuv420p；AAC 48kHz 双声道。容器比视频多 0.08 秒是 AAC 尾部填充，视频为 5166 帧。
- Resolve 项目备份：同目录 `oymyakon-gpt6-r3.drp`。
- 执行收据：同目录 `resolve-execution-receipt.json`，SHA-256 `76bf3c4e6ce15ff755e3561808ff57b2a981b2513426da74fbefdb9daf6fadec`。

验收确认 20 个片段全部在线、顺序正确、无时间线空洞，音视频边界一致；全片、片头和片尾解码通过，渲染任务状态为 `Complete`、100%。初次片段生成暴露 FFmpeg 7.1 平均帧率元数据不一致，失败样本移入诊断目录后使用 FFmpeg 8.1.1、显式 CFR 25fps 和 25000 timescale 重新生成，20/20 通过。一次 UI 焦点漂移缩短了首个视频项，GPT-6 只重建自己的 r3 时间线并逐项复核后再渲染。

本轮没有直接听觉试听能力；对白边界依赖本地 Whisper `large-v3-turbo` 时间戳和画面核验，不能声明逐字切点已由人耳确认。全片做了解码与每镜头图像抽样，但没有真人端到端观看验收。成片、独立项目和完整执行收据已保留，旧 r1/r2 均未作为最终成片来源。

## r4 成片质量修复

GPT-6 对 r3 成片执行独立只读复核后确认 0 个阻断问题、3 个主要问题和 2 个次要问题：开场前四镜头左右声道失衡，证书对白与雪地摩托段响度相差约 12.3 LU，最终 AAC 真峰值约 +2.2 dBTP，工具落水支线未交代就转入捕鱼，以及片尾重复相同天气评论。用户随后明确授权修复并记录这些问题。

生产 `gpt-main` 使用 `openai/gpt-6-astra`、`medium` 思考强度，新建独立项目 `AI-worker Oymyakon GPT6 r4 20260920` 和时间线 `Oymyakon GPT6 r4 Final`。r3 的项目、成片、中间片段、方案和审计证据共 240 个文件逐项复核未变。r4 保持 20 个镜头，缩短到 189.72 秒：

- 开场四镜头按左右分路转写和电平证据，使用 `0.9L+0.1R` 居中双单声道，左右 RMS 差收敛到测量精度范围内，没有放大弱路噪声。
- 证书与雪地摩托镜头增益分别调整 `+7/+5/-6 dB`，三段响度从 `-29.6/-27.8/-15.5 LUFS` 调整为 `-22.6/-22.7/-21.5 LUFS`，最大相邻差由 12.3 LU 降至 1.2 LU。
- 片段链使用 192kHz 四倍采样限峰并补偿延迟，最终 AAC 真峰值约 `-4.0 dBTP`，左右超满刻度样本均为 0；全片 `-19.1 LUFS`、LRA 14.3 LU，未统一压平环境动态。
- 重新读取 29 份摘要并核对 AA0724 源 180–1140 秒。删除无结果的木杆与悬问段，使用真实原声“去砸另一个冰窟”及走离画面结束工具受阻插曲，再用下网、捞网和见鱼原声建立捕鱼新阶段；没有采用身份不明的“终于拿出来了”，也没有暗示冰锥已找回。
- 片尾只保留一次天气评论和完整举鱼动作。

DaVinci Resolve 21.1 首次渲染即通过五项门禁。最终输出：

- 成片：`/Users/heisenbergs-1/ai-worker/output/resolve-edit/2026-09-20/oymyakon-gpt6-r4/oymyakon-gpt6-r4.mp4`
- SHA-256：`d095165e27d835ae3bbebafea4d8630e78c50d49a31377cfaca161f8b5eead0e`
- 规格：H.264 1920×1080、25fps、4743 帧；AAC 48kHz 双声道；593310657 字节；视频 189.72 秒，容器 189.802667 秒。
- 项目备份与收据：同目录 `oymyakon-gpt6-r4.drp`、`resolve-execution-receipt.json`、`quality-fix-report.md`。

全片 `-xerror` 解码通过；帧间隔恒为 40ms，blackdetect/freezedetect、纯色、闪帧、重复帧候选均为 0。20 个视频/音频项全部在线、边界匹配、无空洞；80 张成片抽帧和 20 个片段中点对应检查通过。最终成片 ASR 只出现一次天气评论，并能读回工具受阻、离开原位置和开始下网捕鱼三个阶段。本次仍没有人耳试听或真人全片连续观看，ASR 个别同音词不作为字幕或事实依据。

问题已写入项目私有 NeedCreate：`VAW-0036` 至 `VAW-0040`，全部状态为“已完成”。CSV 保持固定 16 列，原 35 行字节未变，新增后共 40 条且事项 ID 唯一；该私有文件不进入 Git。

## 固定 5 秒视频学习粒度

用户要求将视频学习片段从约 60 秒改为固定 5 秒。实现提交为 `d0013553feaf76e2c6cc750c7ae42af2a0d095c7`，已推送 GitHub。学习窗口现在固定为 5 秒，连续 12 个片段共用一次视觉请求和一次音画摘要请求；每个片段仍独立保存、独立校验和独立续跑。Whisper 改为整条音轨只加载一次，输出词级时间戳后投影到 5 秒窗口，避免 5 秒粒度导致模型启动次数放大。

本地与生产等价环境验证通过：相关 Vitest `56/56`、TypeScript、ESLint、敏感内容扫描和 Node 22 standalone 构建通过；远端现有 Whisper 对 12.5 秒样本返回 3 个时间片段、29 个词级时间戳；Qwen3.6 视觉批量和音画摘要批量各返回 2 个独立片段且结构校验通过。Qwen3.8-VL 主路由隔离探测超过 6 分钟未返回，本次没有把该慢响应记为通过，代码保留当前主路由失败后回退 Qwen3.6 的边界。

生产部署预检已执行但未切换：

- 生产任务入口从 revision `110` 暂停到 `111`，活动任务为 0；数据库备份 `/Users/heisenbergs-1/ai-worker/state/video-autoworker/operations/2026-09-20-video-learning-5s/mission-control-before.db`，0600、`quick_check=ok`、SHA-256 `4fc96554221413cb51169bb0fb88ec40820a1bbbf7ba7265d5b5517ad19f0480`。
- Worker 交接预检因已有已完成 handoff 收据不接受新的目标提交，返回 `worker_handoff_scope_mismatch`；OpenClaw qwen-current 的 Gateway token Exec SecretRef 当前无法解析，运行时收敛证明不能安全刷新。
- 未停止现役应用、未切换路由、未替换 Worker、未修改任务链配置。入口已恢复到 revision `112`、`accepting=true`，现役仍为 green `5404a60c1dee7e18dd63ad012cca400cde6ae095-runtime`、router generation 14、Worker `ea8d0bbcac9d4a43a9c38634526fb3d8ea3f8b08e6406f06fbdb116bdf80ef99` PID 89206，健康且持有租约。
- 生产任务链当前仍为 `segmentSeconds=60`、`segmentOverlapSeconds=1`；d001355 已构建但尚未生产生效。完成 Worker 交接和 qwen-current SecretRef 修复后，需沿同一计划更新为 `5/0` 并真实回读。

## 固定5秒视频学习生产部署完成

- 生产 checkout 对齐 GitHub 主线提交 `54c93949ebd47231d2b1adfe5f4b3fb06d23da6a`；Node 22 standalone 构建、敏感扫描和制品审计通过。应用 release 为 `54c93949ebd47231d2b1adfe5f4b3fb06d23da6a-runtime`。
- 独立 Worker 已通过受控排空和 LaunchAgent 原子替换更新：PID `3971`，制品 content SHA-256 `1b3f8d745432db823f2cdceb6ee1a7c21bceaeff50fd82f3e49cf40b42648bbf`，healthy/leaseVerified 均为 true，活动任务为 0；旧 plist、handoff 和 Whisper 入口备份留在生产操作目录。
- 蓝绿路由已切换到 `blue`，generation `15`，旧 `green` 保留为回退槽；生产 health 为 `healthy`、版本 `2.0.1`，数据库检查 healthy，Gateway healthy。切换过程中的 `/api/materials` 瞬时探针失败已由后续 acceptance 与路由回读确认，不影响最终路由事实。
- 生产绑定 `n8n_workflow_bindings.id=2` 已写回 `media.segmentSeconds=5`、`media.segmentOverlapSeconds=0`，配置备份和写回收据在 `/Users/heisenbergs-1/ai-worker/state/video-autoworker/operations/2026-09-20-video-learning-5s/`；接单已恢复至 revision `122`，`accepting=true`，活动任务为 0。
- NeedCreate `VAW-0041` 已更新为“生产已部署/已完成”。
