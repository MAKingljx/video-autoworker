# 2026-08-27 视频链路稳定性修复生产部署

## 需求与边界

- 目标：将已验证的视觉故障转移与父任务状态一致性修复部署到远端生产，保障视频任务链稳定。
- 生产继续只保留一条 current 视频任务链；飞书、网页、API、CLI 和 OpenClaw 仍只做薄适配。
- 本轮不重试、不恢复、不取消、不重提、不改写任何已有视频任务、媒体或 n8n execution；视频调度器继续保持“只等待、不派发”。
- 发布范围仅为 3017 控制台代码。n8n、Task Broker、Qwen 模型、OpenClaw Gateway、任务数据库、素材和索引不在变更范围内。

## 规则快照与环境

- 项目：`PROJ-VIDEO-AUTOWORKER`；沿用单链路规范 `VAW-SINGLE-CHAIN-001`、生产最小变更和可回滚发布边界。
- 当前连接：`20.tcp.cpolar.top:10628`，远端身份 `HEISENBERGS-1deMac-Studio.local / heisenbergs-1`，固定 SSH 主机指纹已严格校验。
- 真实生产仓库：`/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`。
- 生产工具链：Node `v22.22.3`、pnpm `10.33.0`。

## 发布前只读核验

- 目标批次文件已经是 `completed_with_errors`，目标视频 item 已是 `failed`；n8n execution `65` 已结束为 `error`，错误为 `finalize: 画面分析节点尚未成功完成`。
- 平台登记中目标视觉子任务仍有一条 `running` 数据行，但对应 video worker 无进程、无 TCP 连接，n8n 活跃 execution 为 `0`；该遗留行未被清理或改写。
- 应用数据库和 n8n 数据库 `PRAGMA quick_check` 均为 `ok`；n8n 最大 execution ID 为 `65`。
- 发布前 3017 PID 为 `7298`，监听 `127.0.0.1:3017`；n8n `71374`、Qwen3.6 `41347`、Qwen3.8 `58013`、Gateway `86284/10337/86306` 正常监听。
- video-lane supervisor 在发布前被精确卸载并禁用，状态为 `disabled + unloaded`，无 video worker 进程；历史 `accepted` 任务保持不动。

## 实施

- canonical 功能提交：`dd724f3655911cdc87375ddd7bc7991fbdd64112`（`fix: harden video media retry and vision fallback`）。生产仓库以 `git fetch` + `--ff-only` 快进到该提交，工作树保持干净。
- 在 `.runtime/releases/dd724f3-runtime` 建立独立 detached worktree，使用 Node `v22.22.3` / pnpm `10.33.0` 安装冻结依赖并完成 Next.js 生产构建；生成 129 个路由，`better-sqlite3` 原生模块加载通过。
- 精确停止旧 3017 PID `7298`；其 30 秒后仍未退出时，仅对该 PID 发送 SIGKILL。随后启动新 release，PID `4279` 从 `dd724f3-runtime/.next/standalone` 监听 `127.0.0.1:3017`。
- `.runtime/RUNNING_COMMIT` 更新为完整提交号。切换脚本遗漏 PID 文件更新后，依据 3017 监听 PID 将 `.runtime/video-autoworker-3017.pid` 原子修正为 `4279`。
- 旧 `.runtime/releases/de0af20-runtime` 保留为仅 3017 的回滚点；没有回滚或修改生产数据。
- 随后将本脱敏记录最终提交为 `3fc11c51a0be117f8e7f8af62dc28e1d3e103a01` 并推送 GitHub、快进生产源码仓库；该 docs-only 提交不重建 3017，因此运行 bundle 仍明确为功能提交 `dd724f3`，两者不混称。

## 发布后验收

- 3017 `/profiles`、`/tasks`、`/materials`、`/notifications`、`/api/n8n/runs`、`/api/n8n/status`、`/api/n8n/workflows`、`/api/openclaw/profiles` 均 HTTP `200`。
- `/api/openclaw/profiles` 返回 `gpt-main`、`qwen-current`、`qwen-weixin-new` 均为 `online / ok`。
- n8n `/healthz`、Qwen3.6 `18091/v1/models`、Qwen3.8 `18094/v1/models` 均 HTTP `200`。
- 应用和 n8n SQLite `quick_check=ok`；n8n 活跃 execution `0`、最大 ID 仍为 `65`；目标任务行未新增、未重试、未恢复、未取消、未重提或改写。
- n8n、Task Broker、Qwen3.6、Qwen3.8、三个 Gateway 的监听 PID 与发布前一致。video-lane supervisor 保持 `disabled + unloaded`，worker 无进程。
- 新 3017 日志未出现 `error`、`uncaught`、`unhandled`、`fatal` 或 `exception`。

## 回滚与已知边界

- 若 3017 后续验收失败，只停止新 PID 并从 `.runtime/releases/de0af20-runtime` 启动；不回滚数据库、媒体、索引、n8n execution 或模型服务。
- `/api/n8n/runs` 仍会显示目标视觉子任务的历史 `running` 行以及一条历史 `accepted` 记录；它们分别是失败后的遗留登记和既有历史记录，本轮遵守生产数据保护规则未做清理。后续如需修正，必须先单独只读分析、备份、说明影响与回滚并取得当前确认。
- 本轮只证明修复代码已在生产 3017 生效和运行环境稳定，不代表重新分析该失败视频；任何重跑需用户另行明确授权。

## 失败视频隔离重测验收

### 需求与决策

- 用户已明确授权使用此前失败的同一视频重新测试，要求持续执行到通过。
- 测试使用独立批次目录和新幂等任务编号，保留原失败任务、历史 accepted 任务及其登记不变；不重试、恢复、取消或改写旧任务。
- 继续遵循单链路约束：临时 worker 只调用当前 3017 -> n8n -> prepare/audio/vision/finalize 链，未启动第二条派发链。

### 隔离验证事实（2026-08-27）

- 测试对象为此前失败视频的同一成片，文件大小 `50,872,576,963` 字节；仅记录文件大小和脱敏任务信息，不将视频或原始日志纳入仓库。
- 新任务：`video-command-92c55b9e283a13fd5bdf3a0992da2c3f688ac9d489f99e98ae4dbe7cdddb30f0`；n8n execution `67`。
- 批次于 `12:07:16 +0800` 开始，`13:16:39 +0800` 完成，总耗时约 `69 分 23 秒`。
- `prepare` 成功（约 19 秒）；`audio` 成功，37/37 段（约 8 分 59 秒）；`vision` 成功，37/37 段、111 帧，全部使用 `local-qwen38-vl-direct`，未使用 fallback（约 38 分 19 秒）；`finalize` 成功（约 21 分 43 秒）。
- 父任务、四个阶段子任务和 n8n execution 均为 `succeeded/success`，子任务均为首次执行（`attemptCount=1`）；父任务最终结果包含 37 条时间线、8 个章节、音频转写、视觉分析和合并报告，`memoryMode=none`。
- 通过 `result` 任务编号接口只读核验成功，正式报告可分页读取；结果摘要为约 2.9 KB，合并正文约 93 KB。未向 OpenClaw 会话回投内容。

### 收尾与边界

- 验收后仅停止临时 worker PID `43385`；3017、n8n、Task Broker、Qwen3.6、Qwen3.8 和 OpenClaw 常驻服务保持运行，视频调度器仍为“只等待、不派发”。
- 本次隔离批次状态文件保留作审计证据，当前任务产生的媒体工作区已清理，临时锁已释放。收件箱中仍有一份更早 accepted 任务的历史收件文件，未因本次验收擅自删除或改写。
- 按名称搜索默认只读取默认批次注册表，因此本次独立批次的成功记录使用任务编号验收；后续若要求按名称直接发现隔离批次结果，应另行设计并验证注册表归并，不在本轮变更范围内。
- 本轮未修改源码、n8n 工作流、生产任务数据或模型配置；运行 bundle 仍为 `1a53d34-runtime`，本记录仅作脱敏验收审计。
