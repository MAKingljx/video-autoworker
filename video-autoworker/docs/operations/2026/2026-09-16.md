# 2026-09-16 Feishu 模型路由切换

用户授权将 `qwen-current / second-original` 的 Feishu 对话默认模型切换为 `qwen36-tools-local/default_model`，以降低普通对话和工具调用的等待时间。视频分析视觉链路保持 Qwen3.8：`qwen38-vl-local/qwen38-27b-vl`、`127.0.0.1:18094`、PID `9062` 未变；Qwen3.8 文本服务 `127.0.0.1:18092`、PID `30589` 保留给复杂文本分析。

切换前的模型端点、Gateway、配置布局和备份目录均只读核对。原切换入口只读取 `agents.list`，与当前 OpenClaw 9.2 的 `agents.entries` 不兼容，已修复为调用共享 `openclaw-agent-config.mjs` 保留原布局。模型切换代码提交 `b50cd83`、`9a9f663` 已推送并同步远端，入口源码和安装到 `~/ai-worker/bin` 的 SHA 一致。脚本的 Qwen3.8 回切默认保持 `medium`；健康验收采用 Gateway `/healthz` 与目标本地模型端点短探针，不依赖当前 CLI 无法解析的 SecretRef health RPC。

第一次切换在配置热加载后因 `openclaw health` CLI 缺少 SecretRef 读权限而自动恢复，备份为 `~/ai-worker/backups/qwen-model-switch/20260916-111620`。第二次切换成功，备份为 `~/ai-worker/backups/qwen-model-switch/20260916-112209`；该备份家族保留两份。最终配置回读确认：

- `agents.defaults.model.primary=qwen36-tools-local/default_model`
- `agents.entries.second-original.model=qwen36-tools-local/default_model`
- `agents.entries.second-original.thinkingDefault=off`
- `agents.defaults.compaction.model=qwen36-tools-local/default_model`
- `qwen38-vl-local/qwen38-27b-vl` 仍为视频视觉模型
- Gateway `18889` 返回 `{"ok":true,"status":"live"}`，Feishu WebSocket 启动完成

Qwen3.6 直连合成短请求实测 `145.8ms`、2 completion tokens、`stop`。该值是模型端点短探针，不代表带完整历史上下文的 Feishu 对话总耗时。持久化的旧 Feishu direct session 仍记录 Qwen3.8；未删除或直接修改历史会话，后续新会话按 Qwen3.6 默认路由。旧会话如需切换，使用用户明确触发的 `/new` 或受支持的逻辑 reset。

本次没有修改视频应用、3017、n8n、Qwen3.8视觉服务或任务链，没有发送外部消息，没有重跑真实视频，没有更新 NeedCreate。机器私有验收细节保存在远端受控运行目录。

## Safari 飞书真实对话测速

用户授权通过 Safari 飞书当前对话发送一条不触发任务的探针：`速度测试：只回复“收到”`。页面最终显示 `收到`，卡片明确显示 Provider `qwen36-tools-local`，证明当前飞书会话已沿新模型路由。

远端 Gateway 时间线为：11:47:35.274 收到消息，11:47:35.306 开始 dispatch，11:47:37.673 向 Qwen3.6（`18091`）发起请求，11:48:06.358 返回（模型请求耗时 `28.683s`），11:48:07.699 开始飞书流式卡片，11:48:07.722 dispatch 完成，11:48:08.479 关闭流式卡片。从接收至完成约 `32.448s`，调度和流式发送合计约3.8秒，主要等待仍在模型上下文处理和生成。

该真实消息将当前会话记录更新为 `qwen36-tools-local`；此前旧消息上的 `qwen38-local` 是历史路由标签，不代表本次测试仍使用旧模型。直连 Qwen3.6 的短请求此前为 `145.8ms`，不等同于带长历史的真实飞书对话总耗时。

## 素材图谱生产部署

用户随后授权部署此前暂缓的素材数据可视化。本次提交为 `a481f1b92670fd8849d37295cfc27ab23c1231b2`，只包含只读 `GET /api/materials/graph`、素材面板“素材图谱”入口、场景/情绪维度、有限确定性布局、节点详情与素材预览回跳，以及 API/UI/读取器测试和架构记录。图谱只读现有素材索引和结构化标签，不调用模型、不写任务、审核或知识状态；应用专用错误投影与共享 Worker 文件解耦，Worker 内容 `c7de95a0...` 未重建。

远端干净构建与制品审计通过，Next 应用列出 `/api/materials/graph`。发布计划耗时 `1.112s`，应用切换耗时 `96.283s`，旧绿槽退役耗时 `16.469s`。最终生产状态：路由 `blue/a481f1b92670fd8849d37295cfc27ab23c1231b2-runtime`、generation `11`、应用 PID `35327`、接单 `active/accepting=true/revision90`；doctor `aligned/drift=[]`，应用实际打开权威数据库，数据库身份未变。旧绿槽 `3417/PID68413` 已停止，`3317/PID35327` 为唯一应用槽监听。

浏览器通过受控 SSH 回环访问 `/materials?view=graph` 完成真实视觉验收：页面加载、当前作品/全部作品、场景/情绪切换、标签高亮、节点详情和“回到素材预览”均通过。生产 API 返回 `schemaVersion=1`、15 份素材、189 个已扫描场景、111 个未匹配场景、1 条不可读管线、`truncated=false`；未匹配和不可读项在页面明确提示，不做猜配。原有视频任务链、Worker 租约、Qwen3.6/3.8文本和Qwen3.8视觉服务保持运行。

图谱相关定向测试 `28/28`、类型检查、ESLint、远端构建和完整 Quality Gate 均通过；最新 CI：
`35053716706`（图谱提交）、`35054305274`（错误投影解耦提交）。本次为用户明确授权的生产发布，未发送外部消息、未修改业务数据、未更新 NeedCreate，模型和视频分析链路未切换。

## 项目级摘要真实验证

用户授权检查历史对话并通过 Safari 飞书验证已保存片段能否直接生成项目级摘要。历史记录显示，最近一次学习任务为 29 段、24 成功/5 失败；此前 Word 摘要生成失败且没有落盘回读，后续又出现输出截断和导演脑不可用，摘要没有形成可复用闭环。

本次请求明确限定只读已保存结果、不重新学习视频、不生成 Word。Qwen3.6 路由下，飞书成功识别《地球之极》第七季第12集并读取 8 个片段，最终返回项目级摘要：7 段完整、1 段失败/截断（片段2正文只到“本段视频围绕”）。数据读取和统计可用，结论内容可用。

性能上，Gateway 记录到该次请求进行了 10 次以上 Qwen3.6 往返，每次约 29–41 秒；单聊 300 秒上限于 13:06:21 触发，最终在 13:09:32 返回。此次未重新学习视频、未生成或发送 Word，也未修改业务数据。当前主要瓶颈是 Feishu 编排仍逐段读取并逐次调用模型，而不是摘要数据缺失。长期优化应提供一次确定性的“最近任务项目摘要”聚合调用，由程序返回片段计数、失败列表和受限摘要输入，模型只做最后短文润色；截断片段进入定向补偿队列，避免重复扫描全部片段。
