# 2026-08-18 Qwen3.8 视觉固定升级与 Feishu 通道准备

## 需求

- 通过新的 cpolar SSH 入口连接默认生产节点 `heisenbergs-1`。
- 将 Qwen3.8 原始视觉运行时的升级、OpenClaw 接入、n8n 路由校验和回滚边界固定为可复用流程。
- 准备 OpenClaw 的 Feishu/Lark 对话能力，但不迁移或暴露任何凭据。

## 变更

- cpolar `22.tcp.vip.cpolar.cn:14524` 通过固定 ED25519 主机记录和远端身份校验；当前实际运行仓库为 `/Users/heisenbergs-1/Documents/Phoenix/video-autoworker`。
- canonical `185d1e0` 让 Qwen3.8 视觉升级严格幂等：binding `2` 已是 `local-qwen38-vl-direct`、回退 `local-qwen36-direct` 时只读核对并跳过等价 n8n 写入；升级成功后才标记各边界恢复点，并按家族保留最近两份已验证版本。
- canonical `3a8fa22` 新增 `scripts/qwen38/aiworker-openclaw-qwen38-vl-test`，使用完整模型 ID `qwen38-vl-local/qwen38-27b-vl` 和临时 PNG 完成 Gateway 图片输入测试；短别名不用于能力探测。
- qwen-current 安装官方 `@openclaw/feishu@2026.7.1` 插件，安装前备份目录为 `~/ai-worker/backups/openclaw-feishu/20260818-164601`。由于远端没有 Feishu 应用凭据，通道保持 `installed, not configured, disabled`。

## 验证证据

- 固定升级入口真实完成：运行时 doctor、18094 直接视觉测试、OpenClaw Gateway 图片测试、binding 状态复核和最终状态复核均通过；图片测试回答“红色”。
- 生产服务健康：3017、n8n `5678`、任务代理 `5679`、Qwen3.6 `18091`、Qwen3.8 文本 `18092`、Qwen3.8 视觉 `18094` 返回健康或 `/v1/models` HTTP `200`；qwen-current Gateway `connectivity=ok`。
- binding 2 保持 `local-qwen38-vl-direct` + `local-qwen36-direct`，脚本输出 `no production binding write`；本轮未新增 n8n execution、任务、媒体或数据库记录。
- 生产仓库已快进到 `3a8fa22`，工作树干净；本地、GitHub `main` 和生产仓库均以 canonical 记录为准。

## 回滚与边界

- 视觉运行时失败时，使用最近已验证的 `qwen38-vl-runtime` 恢复点，并将视频路由切回 `local-qwen36-direct`；不重提原任务、不修改任务状态、不删除媒体。
- Feishu 要真正上线仍需在受控密钥存储中提供应用 `appId`、`appSecret`（webhook 模式还需校验密钥），再单独完成真实私聊验收；本轮不从飞书规则大脑迁移 App Secret。
