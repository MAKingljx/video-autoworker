# 2026-09-10 根仓库治理与导演证据确认

原视频分析已经成功，38条素材证据也已写入飞书。写入端进行了NFKC、换行及首尾空白规范化，原回执确认端却比较未规范化文本，导致已有证据被记录为确认冲突。修复统一比对规则，通过两轮完整回读核对原记录、版本和候选状态，再以本地CAS保存确认回执；不重跑视频，不重复投影，不自动批准候选。

用户明确允许聊天批准或驳回指定候选、口述新增候选；新增不等于批准。聊天适配提供完整候选预览、批次确认码、同用户/同会话确认与取消、版本校验和正式回读。既有38条真实候选仍须用户审核。全部业务变更位于本系统及适配层，未修改OpenClaw基础代码，未增加任务链或用户鉴权体系。

## 根目录与构建

同一canonical仓库的管理范围扩展到AI-worker根，保留完整Git历史。产品物理目录和构建工作目录仍为`video-autoworker/`，业务源码仍位于其中的`src/`。共享布局模块分离Git根和产品根，历史flat提交与新产品目录前缀提交均可读取；发布要求整个Git根保持干净。Shell入口先独立验证共享模块，再读取其余源码、摘要和运行声明。

根级CI、公共规则、文档、辅助脚本与产品源码统一纳入版本管理。私有身份标记、原始记录、运行数据、依赖、缓存和生成文件继续排除。源码通过Git增量同步及提交/标签恢复，不为每次迭代重复制作整仓备份或常驻回滚源码副本；数据库、素材、索引、真实配置和必要线上恢复制品按各自恢复对象独立管理。

## 按影响验证

CI不再默认每次全跑。文档变更只检查差异和敏感内容；普通功能根据Vitest导入关系选择相关测试，并执行所需静态检查、构建及界面验收；影响不明确的输入先要求构建与静态检查并注明缺口，不自动扩大验收。完整测试仅显式触发。运行Skill文档不能按普通Markdown跳过。

完整验证的六组重测试放在独立执行环境，最多三组并行；普通分组独立运行。相关测试包含依赖图选中的重测试，并在该环境内串行执行，避免遗漏或同机进程冲突。最终检查要求每个选中任务成功。默认本地完整测试命令继续保留。

本次覆盖Git布局、构建和发布框架的实际变更及直接影响。已完成的未受影响证据按规则复用，不把换提交、换代理或重新打包作为重复验证理由。

## 真实读取与维护窗口

938d17e的完整Quality Gate于本日通过，第二次运行约39分钟，其中单测约32分钟。后续生产维护中，插件0.4.2安装、Gateway重启和实际运行核验通过，但原回执恢复在真实批量读取处被中断：同一服务读取20条证据实测约53.095秒，超过应用原先为operate设置的30秒总时限；单条作品读取约3.63秒，来源摘要和文件权限均正常。

0.4.3把get_many改为最多4路只读并发，结果保持输入顺序；首错停止派发新查询并等待活动读取结束，缺失、重复、作品归属和版本检查保持。按用户明确要求，取消该证据批量读取动作的应用总时限，不将其简单改为更大的固定值。其他操作边界及单次网络请求失败处理保持。

本次维护未完成应用切换，也未写入恢复回执或提炼阶段。已通过官方安装器和验证备份恢复原插件0.4.1，重启Gateway，恢复原导演调度及视频执行器，准入恢复到revision18。当前生产仍运行green/475840d，原视频结果和38条候选均未改变、未批准。

后续先对候选进行构建、制品审计和真实隔离数据库验收，再进入一次生产切换。生产维护窗口仅保留必要备份、安装/切换和健康验收；失败优先恢复原服务，避免长时间停止接单后继续开发排障。实际发布结果以之后的验收记录为准，不把当前本地实现、CI成功或暂存制品等同于生产已修复。

## 组件化与发布提速

Feishu业务CLI和证据状态逻辑统一由应用服务承载。新增受既有OpenClaw loopback身份和项目范围约束的导演脑接口；插件只保留HTTP接入、聊天预览及同用户同会话的明确确认，移除原有业务运行副本和子进程调用。普通Feishu业务实现变更只更新应用，插件及Gateway无需随之重装或重启。

投影兼容使用稳定协议描述，接口、ID规则、文本规范化和回执语义单独版本化；具体源码摘要仍完整绑定构建来源和制品。已知历史outbox保留原ID、幂等键及来源摘要，通过明确兼容映射读取；不重写历史行、不降低未知格式校验。尚未提炼、待人工审核及可恢复的既有投影冲突不再自动阻断无关发布，数据损坏或实际不兼容继续拒绝。

新增薄发布入口 `scripts/release-impact-deploy.mjs`，按组件内容选择应用、插件和控制工具操作，复用既有安装器与蓝绿控制器。应用独立更新时不运行插件安装预检，不停止视频lane，不重启Gateway；候选暂存先于暂停准入。失败先恢复原应用，再根据正式receipt反序恢复已安装组件，健康确认后才CAS恢复本次暂停的准入，无法确认恢复则保留暂停并报告。

CI按产品逻辑路径和Git内容比较，目录层级变化不会把纯重命名误判成所有源码变化。选中变更模块的依赖测试及变更测试自身，插件Skill与清单变更由明确suite映射覆盖；不再为每个选中分区重复执行全部测试收集与分区验证。

`VAW-SINGLE-CHAIN-001`更新为v1.1.0：解耦、组件复用、单一权威实现、稳定协议与实现摘要分责、同制品验证发布及分批优化成为核心开发规范。按本轮范围完成读回与版本化快照；实际生产结果将在激活后补充。

构建前的定向验证已通过：应用回执/接口与权限46项、薄插件52项、薄安装单项、发布17项、CI选择器17项；readiness原51项与新增兼容断言通过，实际Node22下两项数据库断言另行复验，运行一致性的幂等安装与typed hook证明两项通过。TypeScript、相关ESLint、API parity、语法与差异检查通过；源码扫描1273文件无敏感内容发现。这些是本地候选证据，未代替后续真实制品、隔离数据库与生产验收。

## 真实隔离结果与历史制品兼容

b7cef66已推送GitHub，并完成clean构建和远端完整审计，两端制品内容摘要一致。完整制品94,334,808字节，使用已校验938供体与`rsync -rlpcz --safe-links --copy-dest`后实际发送3,485,135字节，约32秒；远端源通过3.7MB Git增量bundle同步，保留同一canonical remote与产品子目录。

真实应用在MC在线副本和3518完成38条候选的两轮回读、确认恢复与状态检查：两轮读取分别17.368秒、11.505秒，副本修复27.124秒，结果为awaiting_evidence_review/candidateCount38。原任务、output、生产outbox及38条飞书版本与候选状态均保持；隔离进程和3518已退出。首次运行副本由Node复制时受0700工作进程umask影响，内容未变但成员权限改变而被制品审核拒绝；改用保留权限的rsync复制后同摘要通过，未放宽制品完整性校验。

后续控制预检发现新增协议字段不能无条件要求历史v2清单具备，正式部署前须按writer实际版本区分历史v2与新v3，保持原字段校验并严格拒绝新制品缺少协议绑定。业务实现及上述真实38条验证不受清单格式修正影响，继续复用。现役0.4.1插件已经通过新运行兼容检查并产生fresh session proof，OpenClaw配置SHA未变。生产仍为green/475、准入active18，尚未切换或批准候选。

根依赖排除模式同时覆盖真实node_modules目录与本机/远端使用的依赖软链接，避免克隆后因共享依赖链接显示未跟踪而重复维护Git状态。

现役旧v2制品已通过新审计器实际完整审核。完整控制预检还识别出旧插件配套Skill被错误要求等于新Skill；现改为对受支持旧插件从同canonical Git历史核对该版本真实Skill字节，任意未登记改动仍拒绝。新插件继续要求当前Skill精确一致，避免为普通应用更新强制提前改变旧插件提示规则。

旧475还早于投影兼容声明文件；退役只读检查改由当前已验证源码中的读取兼容声明识别历史outbox，历史制品仍按其原有来源证明验证。前向切换的目标声明依然必须由目标制品显式携带，未借用当前声明绕过目标协议检查。

发布控制已在生产成功更新为c7ca760，恢复原green/475并确认实际打开权威MC数据库，准入恢复revision26。期间发现历史管理器10秒退出观察早于服务真实退出、旧目录.env.local隐式数据库路径两个运维耦合点，已在受控续行中等待原退出并将三项数据路径提升到平台配置；失败分支均恢复了原应用与本次准入，未覆盖真实数据库。

应用发布计划证实任务流与视频插件未变，只需app与director组件；计划器传递manager对象而非manager.path导致已安装控制被误判，已修正。发布协调器可以通过AIWORKER_RELEASE_PRODUCT_ROOT使用同canonical仓库、具有共同历史的独立clean应用源码，协调器与应用各自验证来源；本次应用继续使用已构建验收的c7ca760制品，不因协调器修复再次构建。

发布协调器将具体失败步骤与经过既有敏感信息过滤的stderr一并返回，避免只显示退出码而反复盲试；此项仅改变控制工具诊断，应用制品继续复用。

当协调器与应用源码分离时，插件安装检查和运行一致性操作使用协调器所在的最新canonical源码；执行前逐组件确认插件/Skill内容摘要与已验证应用源码一致。应用蓝绿操作仍绑定原应用提交，避免未改动插件的GitHub主线检查迫使重建应用。

共享插件安装使用离线批次检查，不能在仍存活的空闲video worker旁直接替换；应用单独更新不受此约束。协调器仅在共享组件变化、准入暂停且任务清空后核对并暂停自己持有的supervisor，在应用/组件恢复成功后先恢复worker再恢复准入，已有停用或未知归属不被自动启动。导演安装器的确定性写前gate拒绝保留无变更标记，避免误称安装不确定而长期停止接单。

未变化的task-flow/video组件先通过现有完整本地payload核对复用，内容精确一致时不再执行其联网安装预检；有变化或无法确认时仍进入对应安装器检查。此改动解决已验证视频插件反复受GitHub网络抖动阻断的问题，不跳过真实组件更新的检查。

发布续行遇到已存在目标release时，协调器先验证目录物理身份、完整制品及与本次源制品相同的清单SHA/提交，再复用已暂存内容；不覆盖原release，也不因不可变stage拒绝重复而重做传输。

Gateway restart命令异步返回；协调器在正式运行一致性检查前等待18889出现唯一真实监听PID，重启及回滚后均使用此等待，不用固定睡眠冒充服务就绪。完整版本、hooks与会话验证仍由原convergence工具执行。

候选槽位启动也统一通过installed-manager解析实际受管入口，避免把应用源码目录中的同名管理脚本误当成当前安装管理器。

## 生产部署与原证据回执恢复结果

最终发布复用此前已完整审计的c7ca760制品，不因协调器修复重复构建。生产router切换至blue/c7ca760、generation3；应用和Gateway健康，3317监听进程实际打开权威Mission Control数据库。准入从本轮维护revision31恢复为active/accepting revision32，active任务0；共享视频worker由原受管LaunchAgent恢复。导演组件安装收据绑定协调器cd80aba，未变化的task-flow和video组件未重装。

原38条证据随后经正式应用入口完成两轮全量回读。第一次确认38条均为候选且未审核，回执恢复耗时23.568秒；第二次确认stable ID顺序、版本和状态未变。原outbox由conflict恢复为delivered，receipt origin为verified_read_recovery且含38项；状态为awaiting_evidence_review，批准数0。原任务整行、视频output和飞书版本保持，没有重跑视频或重复投影。

外层私有维护runner原900秒总时限曾在内部协调器成功结束后留下ETIMEDOUT误报，真实生产状态及完整收据证明切换成功。该外层总时限已移除；具体网络、安装和服务操作仍各自保留有界故障检测。Safari已实际发送只读状态查询，OpenClaw会话显示该轮执行并产生模型输出，但飞书页面尚未显示回复，出站展示仍待核对；页面“已关机”展示按VAW-DIRECTOR-BRAIN-001 v1.2.0忽略，不作为故障依据。后续仍只执行候选预览后取消，不批准真实候选。
