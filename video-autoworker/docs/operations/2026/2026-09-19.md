# 2026-09-19 Resolve 剪辑节点架构设计

用户要求完成远端 GPT / OpenClaw 操作 macOS DaVinci Resolve 的架构设计。本次完成架构设计和批次 A/B 的本地实现，未安装、修改生产配置、剪辑、渲染或部署。

通过新的 SSH 入口严格核对既有主机身份，确认默认远端 Mac 为 macOS 26.5.2 / arm64，Resolve 应用版本21.1.0（build21.1.00017）且存在运行进程。目标机器的官方 Scripting README 与类型文档可读。SSH Python 只读 scriptapp('Resolve') 返回空，Studio授权、Local设置、解释器和GUI会话仍待验证。gpt-main配置的默认模型是openai/gpt-6-astra；本次没有请求模型或读取用户工程正文。

设计采用同机独立 Resolve Executor，通过官方 Python API 操作任务时间线副本；GPT负责计划，业务状态、批准、幂等、恢复与结果索引由现有AI-worker单一应用服务管理。现有模型会话RuntimeProvider不充当剪辑执行接口，现有模型node-execute路由也不直接接收剪辑命令。新增设备能力需通过同一任务claim/finalize与取消合同。

架构文档明确素材身份、分段证据复用、计划修订、帧率与时间码、单实例互斥、结果未知对账、长渲染job归属、预览和文件验收、OpenClaw薄入口以及按组件发布。统一运行收据仍为后续实现目标，不把本次设计写成既有能力。社区MCP仅作候选参考，官方21.1的AI助手集成需按实际安装验证，不引入并行可写后端。

验证范围为源码合同、官方目标SDK说明、只读软件探测、EditPlan/Executor 定向测试、TypeScript、Python 语法和变更文件 ESLint。当前通过 Resolve edit-plan 4/4、fake-Resolve Executor 2/2、TypeScript、变更文件 ESLint 与 Python py_compile；没有运行无关测试、完整构建或生产写验收。设计见[Resolve剪辑节点架构](../../architecture/resolve-editing-node.md)。

本轮新增 src/lib/editing/edit-plan.ts、resolve-executor.ts 和 resolve-executor-protocol.ts：计划包含素材内容摘要、证据完整性、项目/时间线基线、帧率、左闭右开帧区间、能力需求和计划摘要；执行合同提供稳定 operationId、payload 摘要、能力/项目/时间线预检，以及“调用后响应丢失”返回 unknown 的边界。新增 ops/resolve/resolve_executor.py，可在 Resolve 本地 GUI 会话中只读探测、创建计划副本并按已提供的 Resolve MediaPool 唯一 ID 追加片段；缺少身份、工程或素材唯一 ID 时失败关闭。该执行器尚未接入 OpenClaw 或生产任务入口。

远端 Mac 通过临时 GUI LaunchAgent 运行同一 Executor 的 inspect，仍返回 `resolve_not_connected`；临时文件已清理，没有修改 Resolve 设置、工程、数据库或生产服务。该结果把 VAW-0026 的后续问题收敛到 Studio/External Scripting/桌面会话配置核验，不把 Resolve 进程存在误报为 API 已接通。

本轮在用户授权的测试范围内继续完成了一次真实故事线规划与隔离预览：从批次 `video-batch-d9903371e8275140a9275aacc999ed69a6994df6c14a82067c65fb6b1b707dfc` 的29个视频中，使用 `gpt-main` 的 `openai/gpt-6-astra`、思考强度 `medium` 生成17个片段、180秒计划；计划读取已保存的批次证据，不重新学习视频。计划与临时结果位于远端 `/Users/heisenbergs-1/ai-worker/tmp/resolve-test-20260919/`，其中 `story-plan.json` 通过时长与素材路径校验。

新增 `ops/resolve/story_artifacts.py`，可把同一计划生成Resolve可导入的FCPXML，并在Resolve不可用时生成受控低分辨率预览。预览已完成，时长 `00:03:00.02`，1280x720、25fps、H.264/AAC，文件约99.7MB，SHA-256为`f9717b1fd771b97b2a7acdf66d6f47327227641221c9dda4a8408dedb4deadd2`；FCPXML SHA-256为`f4c1af30033ec5fcf4d83678e534dc9d67d74d6a4a583ecf9b09007ba11e7aca`。该预览不是Resolve实机验收，不能替代正式Render Job证据。

预览和FCPXML已回收到本机受控输出目录：[resolve-story-test-20260919.preview.mp4](../../../../output/2026-09-19-resolve-story-test/resolve-story-test-20260919.preview.mp4) 与 `../../../../output/2026-09-19-resolve-story-test/resolve-story-test-20260919.fcpxml`，本机文件摘要与远端一致。

Resolve Executor 已补充隔离项目/时间线创建、按源路径导入MediaPool、渲染设置与状态轮询；本地定向测试仍为Executor 2/2、EditPlan 4/4、TypeScript、Python语法通过。远端Resolve 21.1.0在原配置备份后以隔离`-nogui`方式和临时`System.Scripting.Mode`探测，官方 `davinci-resolve-mcp` doctor 仍判定 `scriptapp`为空、外部脚本不可用；探针脚本随后删除，原`config.dat`已恢复，Resolve进程已恢复为普通启动。官方MCP源码仅以固定提交`e9ff99868bcd6d8af2892672e777903e33c2cd96`留在远端受控vendor目录，未写入生产OpenClaw配置。

当前结论：GPT计划链、FCPXML和非Resolve预览已可复现；真实Resolve导入/粗剪/渲染以及OpenClaw生产工具尚未部署，阻塞条件是远端安装的Resolve 21.1外部脚本权限/版本能力和无可用桌面GUI会话。不得把本轮预览或Resolve进程存在写成生产剪辑完成。后续一旦提供Studio外部脚本或在GUI内启动兼容桥接，只需复用当前`story-plan.json`，无需重新让模型读取29个视频。

2026-09-20复测：通过当前 `ResolvePython` 直接调用 `scriptapp("Resolve")` 返回 `None`；通过当前 `ops/resolve/resolve_executor.py` JSONL `inspect` 返回 `status=failed,errorCode=resolve_not_connected`；官方 MCP doctor 同样报告脚本模块可导入但连接为空，Resolve应用本身为OK，免费版桥接未启动。未执行任何项目、时间线、媒体池或渲染写入。

2026-09-20继续执行启用准备：已备份 Resolve `config.dat`，写入 `System.Scripting.Mode = 1`，设置用户 LaunchAgent 的 `PYTHON3HOME`，重启普通 Resolve 进程；`scriptapp`仍为空。已安装官方 MCP bridge 到 Resolve 脚本目录，bridge 配置已按本次素材范围加入 `/Users/Shared/aiworker-storage` 和隔离输出目录，配置权限为0600。当前还缺一次有桌面的 Resolve GUI 操作：打开已保存项目后运行 `Workspace > Scripts > resolve_bridge`；若脚本菜单不显示 Python bridge，则该安装为免费版21.1能力边界，需要启用/安装Studio。

用户明确要求由生产 GPT 自己操作生产电脑，不依赖本机。已在生产 Mac 启动并批准 `production-resolve-gpt-node`，生产 GPT 使用 `openai/gpt-6-astra`、thinking `medium` 通过 `node_exec` 完成只读桌面探测；node 已改为生产 LaunchAgent `ai.openclaw.production-resolve-node` 常驻，Gateway 和 node 均为远端本机回环链路。GPT 的探测结果为 Resolve 进程存在、Accessibility 为 true，但 Screen Capture 预检为 false、`screencapture` 无法创建显示图像；因此生产 GPT 的电脑控制链已接通，当前阻塞只剩生产 Mac 的活动桌面/屏幕捕获权限，尚未写入任何 Resolve 工程。
