# 2026-09-18 P0 幂等与摘要完整性优化

用户确认优先实施两项 P0 优化：普通任务派发使用稳定幂等 attempt；已保存摘要显式区分内容完整性。当前为本地候选，尚未部署生产。

普通任务派发在首次外部调用前把稳定 `dispatch_request_key` 写入任务 metadata，后续重试复用同一请求键，避免平台已接收但本地响应丢失时重复执行。原有任务状态和 OpenClaw 调用边界保持不变。

视频结果读取新增摘要完整性语义：结构化片段摘要默认为 `complete`，历史章节标记为 `unknown`，历史音画时间线标记为 `incomplete`；直读消息对非完整摘要显示提示，不补写内容。旧正文保持不可变，后续只对确认不完整的片段做定向修复。

定向验证：task-flow `129/129`；video-command 相关测试 `53/53`；video-segment-report `7/7`；TypeScript、变更文件 ESLint 和 `git diff --check` 通过。根测试全量尝试未作为本次通过证据，发现若干既有 Node 版本、缺少工作流 fixture 和运行时构建边界失败，未归因于本次变更。

本次未修改 OpenClaw 基础代码、Pipeline、OpenClaw 原始日志、数据库数据或可视化页面；生产发布需按组件差异走增量发布并完成真实回读。

使用 cpolar `20.tcp.cpolar.top:13488` 的真实生产发布预检已完成。第一次 apply 在安装视频插件阶段失败，原因是源码载荷已变化但插件版本仍为 `0.5.18`，安装器按同版本重装路径拒绝载荷差异；路由未切换，入口曾暂停后已用 revision `93 -> 94` 恢复为 active，未产生业务计数。失败期间视频 worker 被 hold，随后按 hold 收据校验 plist SHA-256 并重新 bootstrap，当前 worker 已恢复为 `available/worker_active`，生产路由仍保持原蓝版本 generation 11。

为使后续增量发布可识别真实载荷变化，视频插件及安装器版本统一提升至 `0.5.19`，并保留 `0.5.18` 作为可升级来源。版本变更后的 manifest、runtime-inspection、保存摘要直读和 scheduler 定向测试 `66/66` 通过，TypeScript、变更文件 ESLint 通过；安装器长时回滚测试未纳入通过证据，待新版本发布时用真实安装收据验收。

续接发布时又发现两个发布入口约束：远端 canonical checkout 必须处于 `main` 分支（不能是 detached HEAD），且运行时收敛合同的 approved manifest 必须同步到插件 `0.5.19`；两项已修正。第二次操作在插件安装完成后因未提供真实会话键失败，已恢复 worker 与入口（revision `97 -> 98`），没有切换路由；下一次计划将复用已安装插件和已构建制品，只补齐会话键与新合同。
