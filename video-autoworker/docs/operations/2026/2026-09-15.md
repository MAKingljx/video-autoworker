# 2026-09-15 运维记录

## 千问聊天思考强度调整

用户要求核实 1M 上下文能力，并降低思考强度。生产节点 heisenbergs-1 的 qwen-current 使用 Qwen3.8-27B BF16 MLX 文本服务。模型模板支持 low、medium、xhigh，缺省为 xhigh；此前聊天模型参数没有显式传入 reasoning_effort。

本次仅在 qwen38-local/default_model 的 params.chat_template_kwargs 中增加 reasoning_effort=medium，保留 thinking 开启。OpenClaw 的请求封装会传递此字段，MLX 将请求参数合并到模型聊天模板。未改变上下文或输出上限，未变更应用制品、路由、Worker、业务数据库或视觉功能。

2026-09-15 08:59:14（UTC+8），生产 Gateway 日志确认该精确配置字段热加载生效；配置验证命令退出码为 0。向同一 MLX 服务发送不含业务内容的 medium 简单请求，2.85 秒返回 OK，finish_reason=stop，输入 14、输出 22 tokens。此验证证明参数受支持和基本生成成功，不代表原长任务、工具调用或 Word 导出已完成回归。

配置变更前校验现有 last-good 恢复副本与当前配置字节一致，使用摘要比较防止并发覆盖并原子替换，权限保持 0600。未新增恢复副本，未清理既有历史备份。必要时仅回退该模型参数；本次无需重启或回滚。

在上午 08:59 的这次调整时，生产尚未启用 YaRN，OpenClaw 的 contextWindow 为 131072，maxTokens 为 4096。此后的 1M 变更见下节；扩大上下文不直接解决输出耗尽或工具调用截断。

参考：https://huggingface.co/Qwen/Qwen3.8-27B

机器收据：currentState=medium_hot_reload_applied；errorCode=none；nextAction=观察后续真实请求，针对长任务截断另行核验输出预算；sourceCommit=db8ed722043ff636cfdb5c72a636e66b82890526；applicationRelease/routerGeneration/databaseIdentity=本次未变更、未重新验收。

## 启用 1M 上下文与速度测量

用户随后明确要求启用 1M 并测速。目标为同一生产文本服务，未更换模型权重或部署视觉功能。模型 text_config.max_position_embeddings 改为 1000000；rope_parameters 移除旧 type=default，设置 rope_type=yarn、factor=4.0、original_max_position_embeddings=262144，保留 rope_theta=10000000、partial_rotary_factor=0.25 和原 mrope 设置。当前 MLX 模型实际调用 initialize_rope，隔离运行确认实例为 YarnRoPE，offset=999998 的两个位置输出有限值。

模型重启前检查 18092 无活动推理连接，校验恢复配置和启动脚本，随后只重启文本服务。端点 2.5 秒恢复，短请求 2.9 秒正常返回 OK。OpenClaw 仅在新服务可用后启用 contextWindow/contextTokens=1000000；2026-09-15 09:08:14.900（UTC+8）确认热加载，Gateway 无需重启。调用安装版本的上下文解析函数，返回 tokens=1000000、source=modelsConfig，即使传入旧模型缓存的 98304 也采用当前配置。

原 maxActiveTranscriptBytes=128kb 是独立的提前压缩触发器，已改为 16mb；按 token 的上下文保护仍保留。模型请求与服务默认 thinking 均保持 medium。此次先保守设为最多 2 项，并传入 128GB 缓存预算，使用 MLX 原生参数。后续源码审查确认该字节预算仅在批量请求准入阶段裁剪闲置缓存，不是活动缓存、生成增长或进程总内存的全程硬上限；不能表述为“合计始终不超过128GB”。机器总内存 512GiB；该轮测试末尾系统 swap 使用约 94.94MiB，不据此声称满 1M 压测的内存峰值。

固定输入、temperature=0、medium、最多生成 192 tokens 的单次测速如下。首 token 包含思考，首正文表示正文内容首次到达；速率以服务实际 completion_tokens 和生成阶段墙钟时间估算。前三次无前缀缓存命中，最后一次复用完全相同的 32K 输入，命中 32818 tokens。

| 场景 | 实际输入 tokens | 首 token 秒 | 首正文秒 | 总秒数 | 生成 tokens/秒 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原配置，4K 输入 | 4136 | 10.551 | 23.695 | 26.659 | 11.92 |
| 1M 配置，相同 4K 输入 | 4136 | 10.842 | 24.034 | 27.012 | 11.87 |
| 1M 配置，32K 输入 | 32820 | 86.418 | 100.474 | 103.634 | 11.15 |
| 1M 配置，32K 输入重复请求 | 32820 | 1.513 | 15.390 | 18.511 | 11.30 |

测速请求主动限制为 192 输出 tokens，finish_reason=length 是测速预算耗尽，不作为正常任务完成证据；短请求另行验证正常 stop。结果说明本组测试中短输入生成速度基本持平，长输入的等待主要来自预填充。未执行填满 1M tokens 的吞吐、显存峰值或检索质量验收，不能把配置与位置编码检查称为百万 token 实际任务通过。

可重复使用 scripts/qwen38/benchmark-context.py；运行在模型节点、默认端口 18092，只产生合成文本请求。--pairs=2048/16384 对应约 4K/32K 输入；精确数量与缓存命中以服务 usage 为准。源码通过 Python AST、shell 语法与安装器嵌入 JavaScript 语法检查；安装器增加 YaRN 前置核对，防止重新安装时声明未配置的 1M。

新增合成工具调用验证：7.09 秒返回 finish_reason=tool_calls，函数名和 JSON 参数完整正确，未执行该工具。模型端点和 Gateway /healthz 均返回 HTTP 200。Gateway CLI 一次性推理验证未完成：该命令路径不能直接解析 SecretRef，复用既有窄 Keychain wrapper 时系统返回退出码36；未修改凭据、解锁策略或重启 Gateway。配置热加载、上下文解析与模型测试已通过，但不宣称已完成飞书真实消息或原 Word 任务的端到端回归。

机器收据：currentState=one_m_enabled_benchmarked_32k；errorCode=GATEWAY_CLI_SECRETREF_UNAVAILABLE（仅独立CLI验收）；nextAction=观察下一条真实聊天及长任务，满1M压力/质量测试尚未执行；modelConfigSha256=6eabb65dd1f37d0a8b0c4f52e4edd04eb03964a3cf5b35d46dff6c19f3eac434；launcherSha256=4f104e69c390ee0865b0a4c27b109992daed616655a79f773b9103ba48460d4e；applicationRelease/routerGeneration/databaseIdentity=本次未变更、未重新验收。

## 缓存交替复用与容量测试

用户要求保持 medium，不更换27B BF16，并完成缓存优化与接近1M实际输入验证。使用同权重、相同YaRN参数的独立18096测试服务；共享GPU，但不占生产18092请求队列。测试服务单请求串行，增加进程内MLX内存遥测及300GiB保护，主机压力升高或新增swap超过2GiB时只停止该测试进程，不修改安装包源码。用户随后明确取消百万级压力测试，优先真实项目问答；已完成结果保留，取消部分不记为通过。

实际A→B→A测试使用相同2047-token合成输入与medium，三条检索记录均正确。2项缓存回到A时命中0 tokens，首token为5.052秒；8项时命中2046 tokens，首token为0.344秒。输出内容与129输出tokens一致，生成速度仍约11.9tokens/s，收益来自复用输入。该测试通过。

长请求采用stream、不传seed。取消探针在10.649秒关闭客户端，后台在后续预填充块完成后停止，活动内存回落且连接排空；取消不是瞬时抢占。之后先测试131071输入tokens，再测试约999000输入tokens并预留输出空间；用当前tokenizer精确计数，并在约5%、50%、95%处放入三条可校验记录。结果以API真实usage、完整stop、检索值、首token/正文延迟及内存峰值为准。

131071-token请求完整通过：首token521.142秒、首正文536.257秒、总539.319秒，输出172tokens，三条记录全部正确，finish_reason=stop。近1M请求实际构造998999输入tokens，在处理到176128tokens时按用户要求停止；没有最终usage或完整检索结果，不能宣称满1M验收通过。独立测试服务和驱动已停止，18096关闭，任务PID文件已核对进程退出后清理。

## 8项缓存生产部署与项目资料新鲜度验收

确认生产无活动推理后，仅替换文本服务启动脚本并重启该服务，约4.05秒恢复端点。实际运行参数为缓存8项、128GB准入预算、medium；1M容量、原BF16权重和输出上限4096保持不变。脚本SHA-256为b719c3e96cd31cf5af717b6177ff886f5e288774d797df94842d3d233c950bba，对应提交9601a57；可从b140626恢复已核对摘要的2项配置脚本，无需另复制业务源码或数据库。Gateway无需重启，模型与Gateway健康均HTTP200。

通过已安装的真实before_agent_reply入口分别询问“导演脑是什么架构”“导演脑当前功能边界”，4.436秒和6.023秒读取正式蓝图并返回，未调用语言模型或发送飞书消息。两次请求cache=no-store、响应Cache-Control=no-store、HTTP200，复用现有唯一应用读取链。

使用真实插件→应用客户端→导演脑业务服务的依赖注入构造隔离故事节点，只有底层记录源是测试内存副本，不修改生产业务记录。每次均重新执行findExact；同一节点由已确认v0.2.0“信任”变更为已确认v0.2.1“资源分配”，最后模拟未确认候选边界。定向契约测试3/3通过。再把当前项目公开背景与这些工具结果交给生产模型；V2请求保留V1历史回答，候选请求保留V2历史回答，验证不能沿用旧事实。

| 场景 | 输入tokens | 命中tokens | 首token秒 | 首正文秒 | 完整回答秒 | 结果 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| V1首次 | 1534 | 0 | 4.588 | 17.696 | 18.800 | 正确回答信任 |
| V1重复 | 1534 | 1533 | 0.282 | 13.310 | 14.397 | 内容一致 |
| V2更新 | 1976 | 1704 | 1.194 | 16.906 | 18.077 | 正确改为资源分配 |
| V2重复 | 1976 | 1975 | 0.282 | 16.000 | 17.171 | 内容一致 |
| 变为候选 | 2449 | 2179 | 1.191 | 9.322 | 10.748 | 明确尚待审核、未确认 |

五次均正常stop，保持medium。上述模型数据为生产模型与隔离业务资料验收，不能表述为真实业务记录被更新或整条飞书消息链实测。缓存只复用不变的输入前缀；资料变化后必须本轮重新调用正式读取工具。现有no-store不是每条任意问题自动重读的全局保证，不得把旧聊天结论当作当前项目状态，也不把候选当已确认事实。此次无需新增RAG或第二套知识权威。

最终状态：currentState=cache8_deployed_project_freshness_verified；errorCode=none；nextAction=在真实对话中继续使用正式读取工具并观察时延；applicationRelease/routerGeneration/databaseIdentity=未变更、未重新验收。只发布模型启动脚本，未部署下一批可视化、未写业务数据、未写外部消息。执行收据位于生产受控run目录，本地脱敏记录与测试源码进入同一Git仓库。

## 按片段保存摘要与OpenClaw按需读取

用户授权将逐片段摘要、增量复用和程序导出落实到系统及OpenClaw。新媒体合成每次仅处理一个时间片段，checkpoint绑定输入与模型配置，保存完整segmentSummaries；程序生成短索引与明确覆盖范围的导演感知索引，取消必经的全片模型汇总。旧有效final checkpoint直接复用，已完成任务通过历史章节/音画观察适配读取，不重跑。

新增共享片段报告读取/导出模块与现有CLI选项，OpenClaw视频工具增加segments、segment、export。目录最多20项、预览160字符；单段12KiB；导出由程序生成Word/Markdown，完整正文不回送模型。导出采用固定受控目录、内容摘要寻址、原子发布与回读校验，拒绝模型路径、符号链接及内容篡改，不自动发送。文件名只用于显示，任务身份隔离不变。插件升至0.5.16，支持现役0.5.15升级及失败回退；工具描述、Skill和受管工作区说明同步更新。

下游种子适配200段章节与完整分段感知，绑定片段时间码/输入摘要，保留原视觉与融合音画来源区分。新分段结果的存储种子上限为8MiB，旧结果仍256KiB；模型输入仍逐批128KiB，不将存储容量扩大解释为模型单次输入扩大。

本地验证包括媒体21项、媒体路由32项、种子与导演阶段45项、OpenClaw片段/导出106项、报告读取及CLI17项，以及插件版本和相关运行时219项。两项共享目录/进程时序导致的瞬态失败单独重跑通过。200段测试证明单请求只含一段、重试复用、缺失/变化段单独补做；旧结果读取、UTF-8边界和导出全量按序均通过。Word样本通过OOXML校验和两页中文渲染检查；渲染器单独配置系统中文字体，不修改生产字体或依赖。

本节提交时为已验证发布候选，生产发布与真实只读/隔离结果验收完成后追加收据。下一批可视化不纳入制品，也不为验收重新学习真实视频、批准候选或发送文件。

## 长期迭代发布：组件证据复用

本次真实发布暴露出计划与执行前组件检查重复调用安装器预检、指定测试文件被入口忽略而扩大为全量测试，以及输出目录缺失被通用失败掩盖。用户授权统一优化。计划与执行前复核现在使用同一逐组件只读检查器，分别核对技能/插件源码摘要、受管AGENTS/MEMORY段及相关配置；完全一致的组件复用，不调用安装器。变化组件仅在实际安装边界调用自身安装器与真实验收。即使Git源未变化，已安装目标漂移仍会被发现；计划保存目标指纹，执行前漂移拒绝复用。旧兼容版本不能冒充当前精确版本，不安全路径与权限不能转为可复用证据。

同一已验证应用制品继续发布，控制脚本变化使用控制源码角色，不触发应用重构建。发布收据叶目录在昂贵检查前创建并核对私有权限；失败返回有界脱敏原因。测试入口现在严格接受指定测试文件，收集结果必须完全匹配，空选择/无效参数失败，重测试沿用隔离分区。未改动且未受影响的验证复用，实时身份、数据、租约、CAS、制品完整性及真实部署验收不省略。

本地逐组件与测试入口13项隔离测试通过，发布协调器49项通过；指定发布测试实际仅运行1个文件。首次部署与真实耗时验收尚待追加，不以本地通过宣称生产已完成。

远端首轮新版计划耗时1.433秒，识别导演脑无需更新，应用制品复用7f9befef。apply在task-flow安装前被shared gate拒绝，准确原因是协调器未绑定AIWORKER_BG_N8N_DB_PATH；逐组件指纹确认无目标写入、路由仍generation9，核对本operation的Worker暂停凭据后恢复Worker与接单revision82。未切流，未回滚应用。随后将共享数据库物理路径/dev/ino纳入计划与执行环境绑定，变化组件在维护前要求该字段，执行前重新核对身份；不把缺环境参数拖入暂停窗口。另修复源.mjs符号链接被过滤遗漏的边界。补充发布测试50项、组件检查6项通过。
